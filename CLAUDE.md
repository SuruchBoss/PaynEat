# คำแนะนำสำหรับ Claude Code ในโปรเจกต์นี้

โปรเจกต์นี้ยังมี [`CONTRIBUTING.md`](CONTRIBUTING.md) และ [`docs/CODING_STANDARDS.md`](docs/CODING_STANDARDS.md)
อ่านไว้ด้วยถ้าจะแก้โค้ด — ไฟล์นี้เก็บเฉพาะกฎที่ผู้ดูแลโปรเจกต์ขอให้ทำซ้ำทุกครั้งโดยไม่ต้องถามซ้ำ

## ทุกครั้งก่อน push — เช็คและอัปเดต README เสมอ

อย่ารอจนจบ ticket ใหญ่แล้วค่อยแปะ README ทีเดียวตอนท้าย ให้เช็คทุกครั้งก่อน push (ไม่ว่าจะ push
ฟีเจอร์ใหม่ หรือแก้เล็กๆ) ว่า **README.md** (หลัก) และ **README.en.md** (ต้อง sync กันเสมอ) ยัง
ตรงกับโค้ดจริงไหม โดยเฉพาะจุดเหล่านี้:

- **หัวข้อ "🗺 ทัวร์ 5 นาที"** — นี่คือส่วนที่คนอ่านจริง ๆ ก่อนส่วนอื่น ถ้าฟีเจอร์ใหม่มีหน้าจอ/ปุ่มที่
  กดลองได้ ให้เพิ่มเป็นขั้นตอนในทัวร์ หรือเพิ่มในหัวข้อย่อย "อยากลองกฎทางธุรกิจที่ซ่อนอยู่?" ด้วย —
  ห้ามฝังไว้แค่เป็น bullet เดียวในหัวข้อฟีเจอร์ลึกๆ ที่คนเลื่อนผ่านง่าย (บทเรียนจาก ticket 06:
  inventory/stock ตอนแรกมีแค่ bullet เดียว ผู้ใช้อ่านแล้วบอกว่า "ไม่เห็นมีพูดถึงเลย")
- **หัวข้อ "✨ ฟีเจอร์"** — เพิ่ม/แก้ bullet ในหมวดบทบาทที่เกี่ยวข้อง (พนักงานเสิร์ฟ/ครัว/แคชเชียร์/
  ผู้ดูแลระบบ/ระบบ) ทุกครั้งที่มีฟีเจอร์ใหม่หรือพฤติกรรมเปลี่ยน
- **Badge จำนวนเทสต์บนสุด + หัวข้อ "🧪 การทดสอบ"** — รันจริงแล้วนับให้ตรง (`cd backend && npm test`,
  `cd app && flutter test`) อย่าเดาเลขหรือคัดลอกเลขเก่า อัปเดตทั้ง badge, บล็อกโค้ดตัวอย่างคำสั่ง,
  และย่อหน้าอธิบายไฟล์เทสต์ใหม่ที่เพิ่ม
- **หัวข้อ "🔭 สิ่งที่จะทำต่อ"** — ติ๊ก `[x]` ให้ตรงกับ ticket ที่เพิ่งทำเสร็จ พร้อมอธิบายสั้นๆ ว่าทำ
  อะไรไป (ห้ามปล่อย `[ ]` ค้างไว้ทั้งที่ทำเสร็จแล้ว)
- ถ้าเปลี่ยนการตัดสินใจเชิงออกแบบสำคัญ ให้เพิ่มข้อใหม่ใน [`docs/DECISIONS.md`](docs/DECISIONS.md)
  และอัปเดต [`docs/FEATURE-GAP-ANALYSIS.md`](docs/FEATURE-GAP-ANALYSIS.md) คู่กันไปด้วยเสมอ (ไม่ใช่
  แค่ README) ตามรูปแบบที่ทำมาทุก ticket ก่อนหน้า

ทำคู่กับ checklist ที่มีอยู่แล้วใน `CONTRIBUTING.md` และ `docs/CODING_STANDARDS.md` ข้อ 7
(`flutter analyze` / `dart format` / `flutter test` / `npm run format:check` / `npm run lint` /
`npm test` ต้องผ่านครบก่อน commit อยู่แล้ว) — README เป็นเช็คลิสต์ **เพิ่ม** ไม่ใช่แทนที่
