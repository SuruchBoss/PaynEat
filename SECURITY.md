# Security Policy

## Supported Versions

PaynEat POS ยังไม่มีระบบเวอร์ชันแบบ semantic release — ให้ยึด commit ล่าสุดบน branch `main`
เป็นเวอร์ชันเดียวที่ได้รับการดูแลด้านความปลอดภัย

## รายงานช่องโหว่ (Reporting a Vulnerability)

**ห้ามเปิดเป็น public Issue** สำหรับช่องโหว่ด้านความปลอดภัย เพื่อไม่ให้ผู้ไม่หวังดีรู้ก่อนที่จะแก้ได้

ใช้ช่องทาง **GitHub Private Vulnerability Reporting** แทน:

1. ไปที่แท็บ **Security** ของ repo นี้
2. กด **Report a vulnerability**
3. อธิบายรายละเอียด: ขั้นตอนทำซ้ำ, ผลกระทบที่อาจเกิดขึ้น, และเวอร์ชัน/commit ที่พบ

ถ้าไม่เห็นปุ่มดังกล่าว (Private Vulnerability Reporting ยังไม่ได้เปิดใช้งานสำหรับ repo นี้)
ให้เปิด Issue ทั่วไปแจ้งว่า "พบช่องโหว่ด้านความปลอดภัย ขอช่องทางติดต่อแบบส่วนตัว" โดยไม่ต้องระบุ
รายละเอียดช่องโหว่ในตัว Issue เอง

เราจะพยายามตอบกลับและประเมินภายในเวลาที่เหมาะสม แต่โปรเจกต์นี้ไม่มี SLA อย่างเป็นทางการ
เนื่องจากเป็นโปรเจกต์โอเพนซอร์สที่ดูแลนอกเวลางาน

## ข้อควรระวังสำหรับผู้ที่จะ deploy ใช้งานจริง

โปรเจกต์นี้เริ่มต้นจากพอร์ตโฟลิโอ ค่าตั้งต้นบางอย่างจึงไม่ปลอดภัยพอสำหรับ production
ถ้าจะนำไป deploy ใช้งานจริง (ไม่ใช่แค่รันดูในเครื่อง) ต้องทำอย่างน้อยนี้ก่อน:

- **เปลี่ยน `JWT_SECRET`** ใน `.env` เป็นค่าสุ่มที่ปลอดภัย (`openssl rand -hex 32`) — ค่าใน
  `.env.example` เป็นแค่ placeholder (`change-this-secret-in-production`) ห้ามใช้ตรงๆ
  ถ้าเคย deploy ที่ไหนไปแล้วด้วยค่า placeholder หรือค่าที่หลุดออกไป ให้เปลี่ยนทันที (จะทำให้
  ทุก session ที่ login ค้างอยู่หลุดออกจากระบบ ต้อง login ใหม่)
- **เปลี่ยนรหัสผ่านบัญชีเดโม** (`admin123`, `waiter123` ฯลฯ ใน `backend/src/db/seed.js`) หรือ
  ปิด `AUTO_SEED` แล้วสร้างบัญชีจริงเอง — ค่าเหล่านี้เผยแพร่อยู่ใน README เพื่อให้กดทดลองใช้งานได้
  ทันที ไม่เหมาะกับระบบที่ใช้งานจริง
- ตั้งค่า `CORS_ORIGIN` ให้เจาะจงโดเมนจริงแทน `*`
- ตรวจสอบว่าไฟล์ฐานข้อมูล SQLite (`backend/data/*.sqlite`) และ `.env` ไม่ถูก commit หรือเปิดเผย
  สู่สาธารณะ (มีอยู่ใน `.gitignore` แล้ว แต่ควรตรวจซ้ำก่อน deploy)

## ขอบเขต (Scope)

Policy นี้ครอบคลุมโค้ดในโฟลเดอร์ `app/` และ `backend/` ของ repo นี้เท่านั้น ไม่ครอบคลุม
dependency ของบุคคลที่สาม (เช่น patch ของ npm/pub package ต้นทาง) — กรณีนั้นให้รายงานตรงไปยัง
โปรเจกต์ dependency นั้นแทน
