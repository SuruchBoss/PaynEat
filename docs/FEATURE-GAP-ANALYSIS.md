# PaynEat POS — Feature Gap Analysis (มุมมอง PO)

วันที่วิเคราะห์: 2026-09-09 (re-verify ล่าสุด 2026-09-19 — ดู `docs/DECISIONS.md` #35)
ผู้วิเคราะห์: PO review (ผ่าน Claude Code) โดยอ่านโค้ดจริง + README + `docs/DECISIONS.md`

เอกสารนี้สรุปว่าฟีเจอร์ POS พื้นฐานอะไร **มีอยู่แล้ว** และอะไร **ยังขาด** เทียบกับสิ่งที่ POS
ร้านอาหารทั่วไปต้องมีเพื่อใช้งานจริง (ไม่ใช่แค่ demo/portfolio) ใช้เป็น input สำหรับตัด ticket
พัฒนาต่อ — ดู tickets ที่เกี่ยวข้องใน `docs/tickets/` (สร้างจากเอกสารนี้)

## สรุปสำหรับ PO (2026-09-19): พร้อมให้คนโหลดไปใช้จริงหรือยัง แม้จะฟรี?

**พร้อมแล้วสำหรับร้านอาหารเดี่ยว (ไม่ใช่เชนหลายสาขา)** — 🔴 Critical และ 🟠 High ทุกข้อ (ticket
1-8, 16) ผ่านการตรวจโค้ดจริงซ้ำอย่างน้อย 2 รอบแล้วว่าทำจริงครบ (DB + backend + UI ที่ผู้ใช้กดได้จริง)
ไม่ใช่แค่ป้ายชื่อ — ล่าสุดสุ่มตรวจ shift/refund/inventory/tax-invoice อิสระอีกรอบ (2026-09-19) ยืนยัน
PASS ทั้งหมด ไม่พบร่องรอยของปลอม/cosmetic แบบที่เคยเจอใน ticket 16 (PromptPay QR) ตอนแรก บวกกับ
ticket 15 (AI ถามตอบข้อมูลร้านด้วย LLM) ที่เป็นจุดขายที่คู่แข่งฟรี/เสียเงินส่วนใหญ่ในตลาดไทยยังไม่มี —
ครบทั้งเรื่องพื้นฐานที่ POS ทุกตัวต้องมีและเรื่องที่ทำให้ "ฟรีแต่ไม่ด้อยกว่าของเสียเงิน"

**อัปเดต (ยังวันเดียวกัน 2026-09-19):** ตอนแรกระบุว่า gap #12 (export รายงาน/Z-report) เป็นสิ่งเดียว
ที่ยังเป็นแรงเสียดทานจริง แล้วเขียน full spec ให้ทันที — **หลังจากนั้นพบว่ามีอีก session ทำ #12 จริง
เสร็จไปแล้วขนานกัน** บน branch `claude/pos-restaurant-project-3djpp6` (ยังไม่ merge เข้า main) และ
พบเพิ่มว่า **#11 multi-branch ก็กำลังถูกทำอยู่ขนานกันเช่นกัน** (backend เสร็จ ยังไม่มี UI) ทั้งที่
เพิ่งยืนยันไปว่า "ยัง out-of-scope ไม่มี drift" — บทเรียน: การ "ตรวจโค้ดจริง" ที่ทำมาตลอดเซสชันนี้
ตรวจได้แค่โค้ดที่มองเห็น ณ ตอนนั้น (`main` + branch ของเซสชันนี้เอง) ไม่ครอบคลุม branch อื่นที่
session อื่นกำลังทำขนานกันอยู่ในโปรเจกต์แบบ multi-session นี้ — ดูรายละเอียดที่
`docs/tickets/12-report-export.md`, `docs/tickets/11-multi-branch.md`

**สรุปที่ยังยืนอยู่**: 🔴 Critical + 🟠 High เดิม (ticket 1-8, 16) ครบและ verified จริง, ticket 15
(AI) เป็นจุดขาย — เพียงพอสำหรับร้านเดี่ยวให้คนโหลดไปใช้จริงได้ ไม่มี gap ที่ยังค้างจริงในกลุ่มนี้แล้ว
(12 เสร็จ, 11 กำลังทำ ทั้งคู่รอ merge เข้า main)

**อัปเดต 2026-09-23 (มีผู้สนใจจะเอาไปใช้ในร้านจริง):** ก่อนส่งให้ร้านจริงใช้ เขียนชุด E2E ที่ให้แอปตัวจริง
คุยกับ backend ตัวจริงผ่าน HTTP (`app/test_e2e/` 30 เคส) — รอบแรกเจอ**บั๊กจริง 5 ตัว**ที่ทั้งการตรวจโค้ดซ้ำ
2 รอบข้างบนและเทสต์เดิม 659 ตัวไม่เจอ แก้ครบแล้ว (ดูหมายเหตุ #43–#47 ด้านล่าง) — บทเรียนต่อจากย่อหน้า
ก่อน: "ตรวจโค้ดจริงว่าทำครบ" ยืนยันได้แค่ว่าแต่ละฝั่งมีของ ไม่ได้ยืนยันว่า**สองฝั่งตีความตรงกัน** — เทสต์
Flutter ทั้งหมดวิ่งบนโหมดสาธิตซึ่งเป็นตรรกะอีกชุดที่เขียนเลียนแบบ backend ส่วนเทสต์ backend ไม่เคยให้
โค้ด Dart อ่าน JSON จริง ช่องระหว่างสองฝั่งนี้จึงไม่เคยถูกเทสต์เลยจนถึงวันนี้

**อัปเดต 2026-09-23 (ผู้สนใจรายแรกเป็นร้านเนื้อ + ผู้กระจายสินค้า):** ผู้ที่จะเอาไปใช้จริงรายแรก (MEET365)
ไม่ใช่ร้านอาหารล้วน — มีเคาน์เตอร์ขายเนื้อสดเป็นกิโลและส่งเนื้อให้ร้านอาหารอื่นแบบเครดิต ตรวจแล้วเจอ gap
ใหม่ 3 ข้อที่ไม่อยู่ในรายการเดิมเพราะรายการเดิมคิดจากมุมร้านอาหารอย่างเดียว: ขายตามน้ำหนักไม่ได้, ไม่มี
บาร์โค้ด/ฉลากตาชั่ง, ไม่มีขายเชื่อ/วางบิล — **ทำครบทั้งสามข้อแล้ว** (ticket 18–20, ดูหมายเหตุ #48–#51
ด้านล่าง) พร้อมชุด E2E ใหม่ที่เดินเคาน์เตอร์เนื้อ → ขายเชื่อ → วางบิล → รับชำระ → ปิดกะเงินตรง
รอบต่อมาผู้ใช้ขอให้ทำ 3 ข้อที่เคยตั้งใจไม่ทำต่อด้วย — ตาชั่งต่อสาย + สแกนด้วยกล้อง, ดอกเบี้ยผิดนัด +
ใบลดหนี้, ใบวางบิลเป็น PDF ส่งอีเมล — **ทำครบแล้ว** (ticket 21–23, ดูหมายเหตุ #54–#57 ด้านล่าง)

ระหว่างตรวจซ้ำรอบนี้ยังพบบั๊กจริง 1 จุดใน ticket 15 (AI assistant เคยหลุดตอบข้อความดิบที่ไม่ผ่าน
`submit_answer` ได้ในบางเงื่อนไข ขัดกับสัญญา "ห้ามเดา/แต่งคำตอบ" ของฟีเจอร์เอง) — **แก้แล้วในรอบนี้**
พร้อมเทสต์คุม ไม่ใช่แค่บันทึกทิ้งไว้ ดู `docs/DECISIONS.md` #35

## สิ่งที่มีอยู่แล้ว (ยืนยันจากโค้ดจริง)

- ผังโต๊ะ + สถานะ (available/occupied/reserved/billing), ย้ายโต๊ะ, รวมบิล (merge)
- สั่งอาหาร + modifier/option group, บันทึกครัว, ตะกร้ารวมรายการซ้ำอัตโนมัติ
- ลูกค้าสแกน QR ที่โต๊ะสั่งอาหารเองได้โดยไม่ต้อง login ดู `docs/tickets/17-qr-self-order.md`
- ครัว (KDS) realtime ผ่าน Socket.IO, แจ้งเตือนออเดอร์ค้างนาน
- ชำระเงิน 4 ช่องทาง (cash/qr/card/transfer — `qr` แสดง QR พร้อมเพย์จริงให้ลูกค้าสแกน ดู
  `docs/tickets/16-promptpay-qr.md`), แบ่งจ่ายหลายช่องทาง, แบ่งบิลตามรายการ/ต่อคน
- ส่วนลดแบบ manual ต่อบิล (%, บาท) และโปรโมชันแบบตั้งเงื่อนไขล่วงหน้า (percent/amount/bogo,
  เงื่อนไขวัน/เวลา/เมนู-หมวดหมู่/ยอดขั้นต่ำ, auto-apply หรือรับโค้ดส่วนลด)
- ใบเสร็จบนหน้าจอ + พิมพ์จริงผ่านเครื่องพิมพ์ความร้อน ESC/POS บนวง LAN/WiFi
- เปิด/ปิดกะ + กระทบยอดเงินสด, คืนเงินหลังชำระเงินแล้ว (เต็มจำนวน/บางส่วน)
- Offline mode สำหรับสั่งอาหารเพิ่มเข้าออเดอร์เดิม (queue ในเครื่อง + sync อัตโนมัติ)
- Dashboard + รายงานย้อนหลัง (ยอดขายรายวัน, สินค้าขายดี, สัดส่วนการชำระเงิน)
- จัดการเมนู/หมวดหมู่, จัดการพนักงาน + RBAC 5 role (admin/manager/waiter/cashier/kitchen)
- ตั้งค่าร้าน (VAT rate, service charge, VAT-inclusive toggle)
- คำนวณ VAT + service charge ตรงกันทั้ง frontend/backend มี test คุม
- สต๊อก/วัตถุดิบตัดอัตโนมัติตอนขาย + ใบกำกับภาษี (ย่อ/เต็มรูป) + audit log ครบทั้งระดับผู้จัดการร้าน
  และบัญชี/การเงิน + ลูกค้า/แต้มสะสม + takeaway/delivery flow ในระบบ
- **ผู้ช่วย AI ถามตอบข้อมูลร้านด้วยภาษาพูด** (admin/manager) ผ่าน Claude API + tool-calling ที่เรียก
  endpoint จริงในระบบเท่านั้น กันตอบมั่ว/แต่งตัวเลข ดู `docs/tickets/15-ai-ask-your-data.md`
- **ขายตามน้ำหนัก** (ราคาต่อกิโลกรัม เก็บน้ำหนักเป็นกรัม) + **สแกนบาร์โค้ด/ฉลากตาชั่ง EAN-13** +
  **ขายเชื่อ/ใบวางบิล/รับชำระหนี้** สำหรับลูกค้าขายส่ง ดู `docs/tickets/18-sell-by-weight.md`,
  `19-barcode-scale.md`, `20-b2b-credit.md`
- **สามภาษาทั้งระบบ: ไทย / English / 한국어** สลับได้จากหน้าตั้งค่าหรือหน้าโปรไฟล์ จำค่าไว้กับเครื่อง
  ฝังฟอนต์เกาหลีแบบ subset มาในแอป (ไม่พึ่งฟอนต์ของเครื่อง) และผู้ช่วย AI ตอบภาษาเดียวกับคำถาม
  ได้ทั้งสามภาษา — ดู `docs/DECISIONS.md` #39

ที่มา: `README.md`, `docs/DECISIONS.md`, `backend/src/db/schema.sql`,
`app/lib/features/**`, `backend/src/modules/**`

## Gap ที่พบ (เรียงตามความสำคัญ)

### 🔴 Critical — บล็อกการใช้งานจริง (ร้านจริงต้องมี ไม่ใช่ของเสริม)

✅ **ทั้ง 5 รายการทำครบแล้ว** (ดูรายละเอียดที่ `docs/tickets/01-04-*.md` และ
`docs/DECISIONS.md` #11/#13 สำหรับขอบเขตที่ตั้งใจจำกัดไว้) — ระหว่างตรวจโค้ดจริงซ้ำอีกรอบ
(2026-09-15) เคยพบ gap ใหม่ที่ไม่เคยอยู่ในรายการนี้มาก่อนคือ **#16 PromptPay QR ยังเป็นของปลอม**
ซึ่งกระทบความน่าเชื่อถือมากกว่ารายการอื่นที่เหลือทั้งหมดเพราะเป็นสิ่งที่ลูกค้าเจอวันแรกที่ใช้งาน —
**ปิดแล้ว** ดู `docs/tickets/16-promptpay-qr.md`

| # | ฟีเจอร์ที่ขาด | ทำไมสำคัญ | สถานะ |
|---|---|---|---|
| 1 | เปิด/ปิดกะ + กระทบยอดเงินสด (Shift & Cash Drawer Reconciliation) | ไม่มี table `shift`/`cash_session` เลย ร้านจริงต้องนับเงินตั้งต้น/ปิดกะเทียบยอดระบบ ไม่งั้นตรวจสอบเงินหายไม่ได้ | ✅ เสร็จแล้ว |
| 2 | คืนเงินหลังชำระเงินแล้ว (Refund) | มีแค่ "cancel" ก่อนเสิร์ฟ/ก่อนจ่าย ไม่มี flow คืนเงินหลังจ่ายสำเร็จ ไม่มี table `refunds` เลย | ✅ เสร็จแล้ว |
| 3 | พิมพ์ใบเสร็จจริง (Thermal/ESC-POS printer) | ปัจจุบันมีแค่ใบเสร็จบนจอ (ตั้งใจไม่ทำตาม `docs/DECISIONS.md` เพราะเป็น demo) แต่ร้านจริงต้องมี | ✅ เสร็จแล้ว (LAN/WiFi — Bluetooth/USB ยังไม่ทำ ดู #11) |
| 4 | Offline mode | เน็ตร้านอาหารหลุดบ่อย ตอนนี้ order พึ่ง backend ตลอด ไม่มี local queue + sync กลับ | ✅ เสร็จแล้ว (เฉพาะสั่งเพิ่มเข้าออเดอร์เดิม ดู #13) |
| 16 | **PromptPay QR / payment gateway จริง** | `qr` ใน `PAYMENT_METHODS` เคยเป็นแค่ label ให้ cashier กดยืนยันเอง เหมือน `card`/`transfer` ทุกอย่าง — ไม่มี generate QR ตามมาตรฐาน PromptPay คู่แข่งในตลาดไทยทุกเจ้ามีเป็นมาตรฐานขั้นต่ำ | ✅ เสร็จแล้ว (generate QR จริงตามมาตรฐาน EMV QR — ยังไม่มี payment gateway/callback ตรวจสอบอัตโนมัติตามที่ตั้งใจแยกขอบเขตไว้ ดู `docs/tickets/16-promptpay-qr.md`) |

### 🟠 High — ควรมีเร็ว ๆ นี้เพื่อแข่งขันได้

| # | ฟีเจอร์ที่ขาด | ทำไมสำคัญ | สถานะ |
|---|---|---|---|
| 5 | ส่วนลด/โปรโมชันแบบมีเงื่อนไข (Promotion Engine) | ตอนนี้ลดได้แค่ manual ต่อบิล ไม่มี happy hour, โค้ดส่วนลด, buy-1-get-1, ตามช่วงเวลา/เมนู | ✅ เสร็จแล้ว (ดู `docs/tickets/05-promotion-engine.md`, `docs/DECISIONS.md` #14) |
| 6 | สต๊อก/วัตถุดิบ (Inventory) | ไม่มี table stock ขายของหมดก็ยังสั่งได้ (มีแค่ toggle sold-out มือ) ไม่ตัดสต๊อกอัตโนมัติ ไม่แจ้งเตือนของใกล้หมด | ✅ เสร็จแล้ว (ดู `docs/tickets/06-inventory-stock.md`, `docs/DECISIONS.md` #15) |
| 7 | ใบกำกับภาษี (Tax Invoice / e-Tax) | ยังไม่รองรับข้อกำหนดใบกำกับภาษีของไทย จำเป็นถ้าจะขายให้ร้านที่จด VAT จริง | ✅ เสร็จแล้ว (เฉพาะเอกสารอย่างย่อ/เต็มรูป ยังไม่ทำ e-Tax ยื่นอิเล็กทรอนิกส์ — ดู `docs/tickets/07-tax-invoice.md`, `docs/DECISIONS.md` #19) |
| 8 | Audit log | ไม่มี log ว่าใครลบ/ยกเลิกออเดอร์ ใครให้ส่วนลดพิเศษ/แก้ราคา — จำเป็นป้องกันทุจริตหน้าร้าน | ✅ เสร็จแล้ว (ดู `docs/tickets/08-audit-log.md`, `docs/DECISIONS.md` #21) |
| 17 | **QR สั่งอาหารเอง (QR Self-Order)** | ลูกค้าต้องเรียกพนักงานทุกครั้งที่จะสั่งเพิ่ม ไม่มีทางสั่งเองผ่านมือถือ — POS คู่แข่งในตลาดไทยส่วนใหญ่มีสแกน QR สั่งเองเป็นมาตรฐานแล้ว | ✅ เสร็จแล้ว (ดู `docs/tickets/17-qr-self-order.md`, `docs/DECISIONS.md` #37) |

### 🟡 Medium — เพิ่มมูลค่า/ขยายธุรกิจ

| # | ฟีเจอร์ที่ขาด | ทำไมสำคัญ | สถานะ |
|---|---|---|---|
| 9 | ลูกค้า/สมาชิก/แต้มสะสม (Customer & Loyalty) | ไม่มี table customer ผูกประวัติซื้อ/สมาชิกไม่ได้ | ✅ เสร็จแล้ว (ดู `docs/tickets/09-customer-loyalty.md`, `docs/DECISIONS.md` #22) |
| 10 | Takeaway/Delivery flow เต็มรูปแบบ | schema มี `type: dine_in/takeaway/delivery` แต่ UI/logic ทำแต่ dine-in ไม่มีคิวรับอาหาร ไม่เชื่อม Grab/LINE MAN | ✅ เสร็จแล้ว (เฉพาะ flow ในระบบ — ยังไม่เชื่อม Grab/LINE MAN ตามที่ตั้งใจแยกเป็นทิกเก็ตย่อย ดู `docs/tickets/10-takeaway-delivery-flow.md`, `docs/DECISIONS.md` #23) |
| 11 | Multi-branch/multi-store | ตั้งใจ scope สาขาเดียวไว้ก่อน (repository layer แยกไว้รองรับ Postgres ในอนาคต) | ✅ เสร็จแล้ว (backend scope 4 entity: โต๊ะ/เมนู/ออเดอร์/วัตถุดิบ + เลือก/สลับสาขาใน Flutter — โหมดสาธิตยังมีสาขาเดียวโดยตั้งใจ ดู `docs/tickets/11-multi-branch.md`, `docs/DECISIONS.md` #36) |
| 12 | Export รายงาน (Excel/CSV/PDF) + End-of-day / Z-report | ปัจจุบันดูได้แค่ในแอป ยังส่งบัญชีไม่ได้ | ✅ เสร็จแล้ว (export CSV แยกทีละรายงาน + Z-report ต่อกะ/ต่อวัน — ดู `docs/tickets/12-report-export.md`, `docs/DECISIONS.md` #35) |

### 🟢 Nice-to-have

- ระบบจองโต๊ะ (reservation) ผูกกับผังโต๊ะ
- แจ้งเตือนสต๊อกใกล้หมด/ยกเลิกออเดอร์ผิดปกติ ผ่าน push/LINE Notify
- ~~Flutter integration test กับ backend จริง~~ ✅ เสร็จแล้ว — ชุด E2E `app/test_e2e/` (30 เคส, job
  แยกใน CI) ดู `docs/DECISIONS.md` #47
- เอา pagination ที่ backend รองรับอยู่แล้วมาใช้ฝั่ง app (ตอนนี้โหลดเมนูทั้งหมดมา filter ฝั่ง client — จะมีปัญหาเมื่อเมนูเยอะขึ้น)

## คำแนะนำลำดับพัฒนา (Roadmap)

1. ✅ **Sprint แรก (เสร็จแล้ว)**: 4 รายการ 🔴 (เปิด-ปิดกะ, refund, พิมพ์ใบเสร็จจริง, offline mode) — เป็น "ของที่ POS ทุกตัวต้องมี" ไม่ใช่ของเสริม
2. ✅ **#16 PromptPay QR จริง** (เสร็จแล้ว) และ ✅ **#17 Audit ระดับบัญชี/การเงิน** (เสร็จแล้ว) —
   ทั้งคู่เจอระหว่างตรวจโค้ดซ้ำ (2026-09-15) เคยเป็น gap ที่เหลือใน 🔴 Critical/🟠 High ตอนนั้น
   ตอนนี้ audit ที่ผู้ใช้ขอไว้ตั้งแต่ต้นครบทั้งสองฝั่งแล้ว (ผู้จัดการร้าน ✅ / บัญชี ✅)
3. ✅ **เฟส 2 (เสร็จแล้วทั้ง 4 รายการ)**: โปรโมชัน + สต๊อก + ใบกำกับภาษี + audit log — เพื่อแข่งขันกับ POS เจ้าอื่นได้
4. ✅ **#33 AI ask-your-data** (เสร็จแล้ว, 2026-09-16) — จุดขายเพิ่มเติมนอกเหนือ gap เดิม ดู
   `docs/tickets/15-ai-ask-your-data.md`, `docs/DECISIONS.md` #33
5. **เฟส ขยายธุรกิจ**: 🟡 ลูกค้า/loyalty ✅, takeaway/delivery ✅, multi-branch ✅ และ export
   รายงาน ✅ เสร็จครบทั้ง 4 รายการแล้ว
6. ✅ **เฟสทัดเทียมตลาด**: ระบุ 3 gap ที่เหลือเทียบกับ POS คู่แข่งในตลาด (QR สั่งอาหารเอง, เชื่อม
   แพลตฟอร์มเดลิเวอรี, payment gateway อัตโนมัติ) — เลือกทำ **ticket 17 QR สั่งอาหารเอง** ก่อนเพราะ
   ทำได้เร็วที่สุดโดยไม่ต้องพึ่งบริการภายนอก (เสร็จแล้ว ดู `docs/tickets/17-qr-self-order.md`) ส่วน
   อีก 2 รายการต้องพึ่ง merchant/สัญญากับผู้ให้บริการรายที่สาม ตั้งใจเลื่อนไว้ก่อน (ดู
   `docs/DECISIONS.md` #23/#26)
7. **Backlog**: 🟢 nice-to-have ตามความเหมาะสม

---

## หมายเหตุการอัปเดต — `docs/DECISIONS.md` #60 (ตรวจ UI ของ gap ตาชั่งสด/ดอกเบี้ย/ใบลดหนี้/อีเมล ทุกภาษา × ทุกขนาดจอ)

ไม่ได้ปิด/เปิด gap ใหม่ ฟีเจอร์ ticket 21–23 ใช้งานได้ครบทุกภาษา ไม่มีอะไรล้นจอใน 96 หน้าจอ แต่ต้องแก้
ฉบับเกาหลีที่ปุ่มยกเลิกเอกสารเขียนว่า "취소" ซ้ำกับปุ่มยกเลิกทั่วไป (กดผิดแล้วยกเลิกใบเสร็จได้) และคำว่า
"대변전표" ที่เป็นศัพท์บัญชีคู่ → "감액 전표", ช่องตั้งค่าดอกเบี้ย/แต้มบนมือถือ และรายการบิลในฟอร์มลดหนี้ที่ถูกตัด

## หมายเหตุการอัปเดต — `docs/DECISIONS.md` #16 / #17 / #20 / #21 / #22 / #23 / #25 / #26 / #27 / #28 / #33 / #35 / #36 / #37

หกข้อ #16/#17/#20 เป็นการแก้ความถูกต้อง/เครื่องมือ/ความปลอดภัย ไม่ใช่ฟีเจอร์ใหม่ จึง **ไม่ได้ปิด
gap ข้อใดในตารางด้านบนและไม่ได้เปิด gap ใหม่** ส่วน #21 (audit log) **ปิด gap ข้อ 8**, #22
(ลูกค้า/แต้มสะสม) **ปิด gap ข้อ 9**, #23 (takeaway/delivery) **ปิด gap ข้อ 10**, #26
(PromptPay QR) **ปิด gap ข้อ 16**, และ #27 (audit บัญชี/การเงิน) **ปิด gap ข้อ 17** โดยตรง — #25
(order audit trail) เป็นคำขอเพิ่มเติมนอกเหนือจาก gap analysis เดิม ไม่ได้ปิด/เปิด gap ข้อใดในตาราง
เช่นกัน (ดู `docs/tickets/13-order-audit-trail.md`) — #28 (ปิดรู audit log ที่เหลือ: เปิด/ปิดกะ,
รับชำระเงิน, กรอก/ถอดโค้ดส่วนลด) เป็นการแก้ต่อยอดจาก #27 พบระหว่าง self code-review ไม่ใช่ gap ใหม่
ในตารางเช่นกัน — บันทึกไว้เพื่อให้เอกสารตรงกันตามกฎใน `CLAUDE.md`

- **#16 ใบเสร็จแสดงเงินที่ลูกค้ายื่นมา** — แก้ตัวเลขบนใบเสร็จที่บวกแล้วไม่ตรงกับเงินที่ยื่นให้จริง
  เกี่ยวข้องกับข้อ 12 (Export รายงาน / Z-report) ตรงที่ถ้าจะทำใบกำกับภาษีหรือรายงานปิดวันในอนาคต
  ต้องแยกให้ชัดว่ามุมมองไหนใช้ "เงินที่ยื่นมา" และมุมมองไหนใช้ "ยอดที่ตัดเข้าบิล"
- **#17 AppClock + ตรึงนาฬิกาตอนถ่ายภาพ** — ทำให้ภาพหน้าจอถ่ายซ้ำได้ผลเหมือนเดิมทุกไบต์
  ส่งผลต่อข้อ "Flutter integration test กับ backend จริง" ใน 🟢 Nice-to-have ตรงที่ตอนนี้มีฐาน
  ให้ทำ visual regression test ได้แล้ว (ภาพนิ่ง → ภาพที่ขยับใน diff แปลว่า UI เปลี่ยนจริงเสมอ)
- **#20 Security hardening batch (OWASP Top 10)** — แก้ช่องโหว่ 7 จุดที่พบจาก security review
  (broken access control, hardcoded secret, broken authentication, security misconfiguration)
  ไม่เกี่ยวกับข้อ 8 (Audit log) โดยตรงเพราะเป็นคนละเรื่อง (ป้องกันช่องโหว่ vs บันทึกว่าใครทำอะไร)
  แต่เกี่ยวเนื่องกัน — ตอนนั้นข้อ 8 ยังเป็น gap ที่เปิดอยู่ (ปิดแล้วโดย #21 ด้านล่าง)
- **#21 Audit log** — **ปิดข้อ 8** โดยตรง บันทึก action เสี่ยงครบตามที่ทิกเก็ตกำหนด พร้อมขยาย
  scope เพิ่ม `user.role_change`/`user.password_reset`/`tax_invoice.void` ซึ่งเป็นการกระทำที่
  เสี่ยงแบบเดียวกันและต่อยอดโดยตรงจากช่องโหว่ privilege escalation ที่แก้ไปแล้วใน #20
- **#22 ลูกค้า/แต้มสะสม** — **ปิดข้อ 9** โดยตรง ผูกออเดอร์กับลูกค้าแบบ optional + สะสม/ใช้แต้ม
  ตามอัตราที่ตั้งค่าได้ + หน้าประวัติการซื้อรายคนสำหรับ admin/manager ครบตาม acceptance criteria
- **#26 PromptPay QR** — **ปิดข้อ 16** โดยตรง generate QR พร้อมเพย์จริงตามมาตรฐาน EMV QR
  ให้ลูกค้าสแกนจ่ายได้ (ยังไม่มี payment gateway/callback อัตโนมัติตามขอบเขตที่ตั้งใจไว้)
- **#27 Audit ระดับบัญชี/การเงิน** — **ปิดข้อ 17** โดยตรง แก้ราคาเมนู/สร้าง-แก้-ลบโปรโมชัน/ปรับสต๊อก
  มือถูกบันทึก audit log ครบ (ต่อยอดจาก #21/#25) เพิ่ม export CSV (`GET /audit-logs/export` —
  เว็บเท่านั้น ดู `docs/DECISIONS.md` #27) และ date range picker บนหน้า "ประวัติการทำรายการ" ที่
  ค้างมาตั้งแต่ #21
- **#28 ปิดรู audit log ที่เหลือ** — ต่อยอดจาก #27 ไม่ได้ปิด gap ใหม่ในตาราง เพิ่ม audit log ให้เปิด/
  ปิดกะ (`shift.open`/`shift.close`), รับชำระเงิน (`payment.pay`), และกรอก/ถอดโค้ดส่วนลด
  (`order.promotion_redeem`/`order.promotion_remove` — จุดหลังเพิ่ม transaction ห่อด้วยเพราะเดิม
  ไม่มี) ดู `docs/DECISIONS.md` #28
- **#33 ผู้ช่วย AI ถามตอบข้อมูลร้าน** — เช่นเดียวกับ #25 เป็นฟีเจอร์เชิงจุดขาย/นวัตกรรมนอกเหนือจาก gap
  analysis เดิม (`docs/tickets/15-ai-ask-your-data.md`) **ไม่ได้ปิด/เปิด gap ข้อใดในตาราง** — เชื่อม
  Claude API เข้ากับ endpoint เดิมของระบบผ่าน tool-calling เท่านั้น (ไม่แต่งตัวเลขเอง) ดู
  `docs/DECISIONS.md` #33
- **#23 Takeaway/Delivery** — **ปิดข้อ 10** โดยตรง เปิดออเดอร์กลับบ้าน/เดลิเวอรีได้โดยไม่ต้องผูกโต๊ะ
  พร้อมเลขคิวรับอาหาร (เฉพาะ takeaway) + KDS แยกป้าย/ไอคอนทั้ง 3 ประเภทชัดเจน — ยังไม่เชื่อม
  แพลตฟอร์มเดลิเวอรีจริง (Grab/LINE MAN) ตามที่ตั้งใจแยกเป็นทิกเก็ตย่อยต่างหาก
- **#35 Export รายงาน + Z-report** — **ปิดข้อ 12** โดยตรง export CSV แยกทีละรายงาน (สรุปยอดขาย/
  เมนูขายดี/ยอดขายรายวัน) + Z-report ปิดกะ/ปิดวันพร้อมกระทบยอดเงินสด (เฉพาะระดับกะ ไม่ใช่ระดับวัน
  เพราะหนึ่งวันอาจมีหลายกะ/แคชเชียร์) — ยังไม่มี export Excel/PDF ตามที่ตั้งใจแยกขอบเขตไว้ (CSV
  เปิดด้วย Excel ได้อยู่แล้ว ครอบคลุม use case หลัก) ดู `docs/tickets/12-report-export.md`
- **#36 Multi-branch/multi-store** — **ปิดข้อ 11** โดยตรง เพิ่ม `branches`/`user_branches`, scope
  `branch_id` ให้ 4 entity (โต๊ะ/เมนู/ออเดอร์/วัตถุดิบ), เลือกสาขาตอน login (หลายสาขา) + สลับสาขา
  ภายหลังทั้ง backend และ Flutter (การ์ด "สลับสาขา" ที่หน้าโปรไฟล์) — โปรโมชัน/ลูกค้า/การตั้งค่า/กะ
  ยังคงเป็นระดับเชนทั้งหมดตามที่ตั้งใจ (ดูรายละเอียดเหตุผล+ของที่เลื่อนไว้ทั้งหมดใน
  `docs/DECISIONS.md` #36) โหมดสาธิตยังมีสาขาเดียวโดยตั้งใจ ไม่ได้ทำ `branch_id` ให้ demo store
- **#37 QR สั่งอาหารเอง** — **ปิดข้อ 17** โดยตรง ลูกค้าสแกน QR ที่โต๊ะสั่งอาหารเองได้โดยไม่ต้อง
  login ผ่าน endpoint สาธารณะที่ reuse business logic เดิม (สต๊อก/โปรโมชัน/realtime) 100% พร้อม
  ให้พนักงาน (admin/manager) ดู/คัดลอกลิงก์/เปลี่ยน QR ใหม่ได้จากหน้าผังโต๊ะ — ยังไม่รองรับชำระเงินเอง
  (self-checkout) และไม่พิมพ์ QR standee จริงจากในแอปตามที่ตั้งใจแยกขอบเขตไว้ ดู
  `docs/tickets/17-qr-self-order.md`

## หมายเหตุการอัปเดต — `docs/DECISIONS.md` #48 / #49 / #50 / #51 (ร้านเนื้อ + ขายส่ง B2B)

gap สามข้อนี้**ไม่เคยอยู่ในตารางเดิม** (ตารางคิดจากร้านอาหารล้วน) เพิ่มเข้ามาตอนผู้ใช้จริงรายแรกเป็นร้านเนื้อ
ที่ขายส่งด้วย — ทำครบแล้วทั้ง backend, แอป, โหมดสาธิต และ E2E:

| # | Gap | สถานะ | ที่ทำ |
|---|---|---|---|
| 18 | ขายตามน้ำหนัก (ราคา/กก., จำนวนไม่เต็ม) | ✅ | `weight_grams` เป็นกรัม, สูตรราคาเดียวทุกที่ (#48), ตัดสต๊อกเป็น กก. |
| 19 | บาร์โค้ด/ตาชั่ง | ✅ | บาร์โค้ดสินค้า + ฉลากตาชั่ง EAN-13 ตั้งรูปแบบได้, อ่านรหัสในเครื่อง (#49) |
| 20 | ขายเชื่อ/วางบิลลูกค้าส่ง | ✅ | วงเงิน/เครดิตเทอม, ใบวางบิล, ใบเสร็จรับชำระตัดบิลเก่าก่อน, อายุหนี้ (#50) |

- **#51** แก้ช่องที่เปิดขึ้นพร้อมกัน: เดิมตัดสต๊อกตอนส่งครัวเท่านั้น บิลหน้าเคาน์เตอร์ที่เปิดแล้วเก็บเงินเลย
  (ไม่มีอะไรต้องให้ครัวทำ) จึงไม่ตัดสต๊อกเลย — ตอนนี้ตัดตอนชำระครบสำหรับรายการที่ยังไม่เคยตัด
- **ต่อยอด #44** — เงินสดที่รับชำระหนี้เข้าลิ้นชักของกะ ยอดที่ควรมีตอนปิดกะนับรวมให้ และ Z-report แยก
  หัวข้อ "รับชำระหนี้" ไม่ปนกับยอดขาย
- ~~**ที่ตั้งใจยังไม่ทำ**: อ่านน้ำหนักสดจากตาชั่งผ่านสาย, สแกนด้วยกล้อง, ดอกเบี้ย/ค่าปรับ/ใบลดหนี้แยก
  เอกสาร, ส่งใบวางบิลเป็น PDF ทางอีเมล~~ → ทำครบแล้วใน ticket 21–23 (หมายเหตุด้านล่าง)

## หมายเหตุการอัปเดต — `docs/DECISIONS.md` #54 / #55 / #56 / #57 (ตาชั่งต่อสาย, ดอกเบี้ย/ใบลดหนี้, PDF อีเมล)

สามข้อที่ ticket 18–20 เคยตั้งใจไม่ทำ ผู้ใช้ขอให้ทำต่อ — ทำครบแล้วทั้ง backend, แอป, โหมดสาธิต และ E2E:

| # | Gap | สถานะ | ที่ทำ |
|---|---|---|---|
| 22 | อ่านน้ำหนักสดจากตาชั่ง + สแกนด้วยกล้อง | ✅ | เซิร์ฟเวอร์ร้านอ่านตาชั่ง TCP/Serial (A&D/CAS, MT-SICS) กระจายผ่าน socket.io ใช้ได้เฉพาะตอนนิ่ง; กล้องมือถือเข้า `BarcodeResolver` เดิม (#54) |
| 21 | ดอกเบี้ย/ค่าปรับจ่ายช้า + ใบลดหนี้ | ✅ | ใบแจ้งดอกเบี้ยผิดนัด (ดอกเบี้ยธรรมดาจากเงินต้นค้าง, ยกเว้นได้) (#55); ใบลดหนี้ออกอัตโนมัติทุกครั้งที่ลดหนี้บิลขายเชื่อ (#56) |
| 23 | ใบวางบิลเป็น PDF ทางอีเมล | ✅ | PDF ภาษาไทยของเอกสารลูกหนี้ทุกชนิดสร้างที่ backend, SMTP ของร้านเอง, ประวัติการส่ง + audit log (#57) |

- **ยังไม่ทำ (ตั้งใจ)**: ค่าปรับเหมาจ่าย/ส่วนลดจ่ายเร็ว, คำสั่ง tare/zero ไปที่ตาชั่ง, PDF ของใบเสร็จหน้าร้าน/
  ใบกำกับภาษี — เหตุผลอยู่ใน ticket แต่ละใบ

## หมายเหตุการอัปเดต — `docs/DECISIONS.md` #58 (ตรวจ UI ของ gap 18–20 ทุกภาษา × ทุกขนาดจอ)

ไม่ได้ปิด/เปิด gap ข้อใหม่ แต่ gap 18–20 ที่ขึ้น ✅ แล้ว**เคยใช้งานไม่ได้เต็มที่ในภาษาอังกฤษ/เกาหลี**:
ตะกร้าขึ้นชื่อเมนูภาษาไทย ชีทตัวเลือกเสริมเป็นไทยทั้งชีท วันครบกำหนดชำระเป็นสตริง `2026-10-11` ดิบ
ช่องสแกนตัดคำใบ้บนมือถือ และบรรทัดชั่งน้ำหนักในตะกร้าล้นเมื่อตัวอักษรกว้าง — แก้ครบพร้อมเทสต์กันถอย
และเครื่องมือถ่ายภาพหน้าจอพังตั้งแต่ ticket 18 (ภาพใน README/หน้า Landing จึงค้างอยู่ก่อนมีฟีเจอร์นี้)
ซ่อมแล้ว ภาพ 23 ใบ + ภาพในหน้า Landing 5 ใบอัปเดตตามหน้าจอจริง หน้า Landing ทั้งสามภาษามีหัวข้อ
เคาน์เตอร์เนื้อ · ขายส่ง แล้ว (เดิมไม่มีเลย)

~~**คำถามทางธุรกิจที่ยังเปิดอยู่ (ไม่ได้แก้เอง)**: บิลขายเชื่อได้แต้มสะสมทันทีตอนลงบัญชี ทั้งที่ยังไม่ได้เงิน
(ticket 20 ห้ามแค่ "ใช้แต้ม" กับบิลขายเชื่อ) — ต้องให้เจ้าของร้านตัดสินว่าควรได้แต้มตอนไหน หรือไม่ได้เลย~~
→ ตัดสินแล้ว: ได้ตอนรับชำระครบ (หมายเหตุ #59 ด้านล่าง)

## หมายเหตุการอัปเดต — `docs/DECISIONS.md` #59 (แต้มสะสมของบิลขายเชื่อ)

ไม่ได้ปิด/เปิด gap ข้อใหม่ แต่ปิดช่องโหว่ใน gap 20 (ขายเชื่อ) ที่ #58 เจอ: ลูกค้าขายเชื่อที่ยังไม่จ่ายหนี้ได้แต้ม
ไปแลกส่วนลดได้ทันที — ตอนนี้บิลขายเชื่อได้แต้มเมื่อยอดค้างทุกบิลของออเดอร์ (รวมดอกเบี้ยผิดนัด) เป็น 0
คิดจากยอดสุทธิหลังลดหนี้ ไม่รวมดอกเบี้ย และยกเลิกใบเสร็จที่ปิดยอดดึงแต้มคืนเท่าที่ลูกค้ายังมี บิลเงินสดยังได้
แต้มตอนปิดบิลเหมือนเดิม ทำครบทั้ง backend, Demo Mode และ E2E

## หมายเหตุการอัปเดต — `docs/DECISIONS.md` #62 (ตรวจ UX ก่อน UAT แบบไม่มีคนสอน)

ไม่ได้ปิด/เปิด gap ด้านฟีเจอร์ แต่เป็นช่องที่ตารางนี้ไม่เคยวัด: **คนที่ไม่เคยเห็นระบบกดเองแล้วไปต่อได้ไหม** ไล่ 666 หน้าจอ
(ทุกบทบาท × 3 ภาษา × 4 ขนาดจอ) แก้ล้นจอ 64 จุด, ภาษาเปลี่ยนได้ก่อนล็อกอิน/หน้า QR, ยืนยันก่อนทำสิ่งที่ย้อนไม่ได้, ปุ่มเทา
บอกเหตุผล และกันแอดมินล็อกตัวเองออก ที่ยังเป็น gap จริงเมื่อเทียบกับ POS เชิงพาณิชย์: ข้อความ error จาก backend
ยังไม่แปล (ร้านที่พนักงานไม่อ่านไทยจะเจอตอนต่อ backend จริง) และข้อความมาตรฐานของ Material ยังเป็นอังกฤษ

## หมายเหตุการอัปเดต — `docs/DECISIONS.md` #61 (รันจาก clone ใหม่ตาม README)

ไม่ได้ปิด/เปิด gap ข้อใหม่ แต่ข้อ "พร้อมให้คนโหลดไปใช้จริง" ในสรุปด้านบน**ไม่จริงอยู่ช่วงหนึ่ง**: ทำตาม README บนเครื่อง
เปล่าแล้ว backend ไม่ขึ้น (ขาดขั้นสร้าง `.env`), แอปเว็บล็อกอินไม่ได้เมื่อมี `.env` (CORS `*`), Docker API ไม่สตาร์ต
(compose ตั้ง production โดยไม่มีรหัสผ่าน seed) และ APK release ไม่มีสิทธิ์ INTERNET — แก้ครบพร้อมเทสต์ CORS
แต่ยังไม่มีอะไรใน CI ที่ build Android หรือ image Docker จริง ถ้าจะปิดช่องนี้ถาวรต้องเพิ่ม job ทั้งสองอย่าง

## หมายเหตุการอัปเดต — `docs/DECISIONS.md` #43 / #44 / #45 / #46 / #47 (ชุด E2E + บั๊กที่มันเจอ)

**#47 ปิดข้อ "Flutter integration test กับ backend จริง" ใน 🟢 Nice-to-have** โดยตรง ส่วน #43–#46
เป็นการแก้ความถูกต้องของฟีเจอร์ที่ปิด gap ไปแล้ว **ไม่ได้ปิด/เปิด gap ข้อใดในตาราง** — แต่ทุกข้อเป็นจุดที่
ร้านจริงจะเจอในวันแรก จึงบันทึกไว้ให้ชัดว่า gap ที่ขึ้น ✅ ไปแล้วเคยมีรูอะไร:

- **#43 ใบกำกับภาษี (ข้อ 7)** — บรรทัด "มูลค่าสินค้า/บริการ" ไม่รวมค่าบริการ บวก VAT แล้วไม่เท่ายอดรวม
  ทั้งที่ VAT คิดบนฐานที่รวมค่าบริการแล้ว — เปลี่ยนเป็นฐานภาษี (`total − vat`) ทั้ง backend และโหมดสาธิต
- **#44 เปิด-ปิดกะ (ข้อ 1) × คืนเงิน (ข้อ 2)** — คืนเงินสดแล้วยอดที่คาดไว้ตอนปิดกะไม่หักออก แคชเชียร์ที่นับ
  ถูกถูกบันทึกว่าเงินขาด — ผูกรีฟันด์กับกะที่เปิดอยู่ (`refunds.shift_id`) หักออกจากยอดที่คาด และคืนเงินสด
  ต้องมีกะเปิดอยู่ (เหมือนรับเงินสด) รีฟันด์เก่าเป็น NULL ไม่คำนวณกะที่ปิดไปแล้วใหม่ย้อนหลัง
- **#45 Export รายงาน (ข้อ 12)** — ไฟล์ CSV ที่แอปดาวน์โหลดจาก backend จริงเสีย BOM (ตัวถอด UTF-8 ของ Dart
  ตัดทิ้ง) เปิดใน Excel แล้วภาษาไทยเพี้ยน ทั้งที่โหมดสาธิตดูปกติ — `ApiClient.getText` อ่านเป็นไบต์แล้วคง
  BOM ไว้เอง
- **#46 QR สั่งอาหารเอง (ข้อ 17 — ticket 17)** — ออเดอร์แรกของลูกค้าบนโต๊ะว่างค้างเป็นร่าง ครัวไม่เห็นเลย
  (เทสต์เดิมยืนยันพฤติกรรมผิดนี้ไว้ด้วย) และลิงก์ QR ที่ถูกตัดท้ายขึ้นหน้า "ไม่มีอินเทอร์เน็ต" แทน "ลิงก์ใช้
  ไม่ได้" — ส่งเข้าครัวทันที + รวมกติกา "ลิงก์เสีย" ไว้ที่ `isBrokenSelfOrderLink` ที่เดียว
- **#47 ชุด E2E** — เปิด `node src/server.js` ตัวจริงบนพอร์ตสุ่ม + ฐานข้อมูลชั่วคราวใหม่ทุกไฟล์ แล้วให้
  `ApiClient` → data source → repository ตัวจริงของแอปเดินหนึ่งวันทำงานของร้าน + ลูกค้าสแกน QR +
  สาขา/สิทธิ์ ที่ชั้น data/domain (ไม่ใช่ `integration_test` ที่ต้องมีอุปกรณ์จริง) มี job `e2e` แยกใน CI

### ข้อที่พบระหว่างตรวจ UI/UX — แก้ครบแล้วทั้ง 3 ข้อ

| # | สิ่งที่พบ | ผลการแก้ |
|---|---|---|
| 13 | ~~ชิป "ทั้งหมด" หน้าพนักงานใช้สีเทาเกือบดำตอนถูกเลือก~~ | ✅ **ตรวจแล้วพบว่าเรื่องกลับกัน** — ชิปสีเทาคือตัวเดียวที่ผ่านเกณฑ์ (4.83:1) ส่วนชิปที่เหลือทั้งแอปใช้ตัวหนังสือขาวบนสีสดซึ่งตกเกณฑ์ (ส้ม 2.84 เขียว 3.45 เหลือง 2.13) แก้โดยเปลี่ยนพื้นชิปที่ถูกเลือกเป็นเฉดเข้ม (ink) ทั้ง 6 หน้า |
| 14 | ~~ปุ่มลัดเงินสดขึ้นเลข 500 ซ้ำสองปุ่ม~~ | ✅ **แก้แล้ว** — ย้ายตรรกะขึ้นมาเป็น `roundUpShortcut` / `cashShortcuts` ใน controller เพื่อให้เทสต์ได้ แล้วกรองค่าซ้ำออก (เพิ่มเทสต์ 3 เคส) |
| 15 | ~~ยังไม่มีโหมดคอนทราสต์สูง/โหมดกลางคืน~~ | ✅ **โหมดคอนทราสต์สูงทำแล้ว** (ดู `docs/DECISIONS.md` #18) — ตัวหนังสือขยับจาก AA เป็น AAA ขอบการ์ดผ่านเกณฑ์ 3:1 ส่วนโหมดกลางคืนยังไม่ทำ ต้องตัดสินใจ palette ชุดที่สามแยกต่างหาก |

### ข้อที่พบระหว่างตรวจโค้ดจริงซ้ำ (2026-09-15)

| # | สิ่งที่พบ | สถานะ |
|---|---|---|
| 16 | PromptPay QR / payment gateway ยังเป็นของปลอม | ✅ **แก้แล้ว** — generate QR จริงตามมาตรฐาน EMV QR ดู `docs/tickets/16-promptpay-qr.md`, `docs/DECISIONS.md` #26 |
| 17 | Audit ระดับบัญชี/การเงิน (แก้ราคาเมนู/โปรโมชัน/ต้นทุนวัตถุดิบไม่ถูก log, ไม่มี export) | ✅ **แก้แล้ว** — log ครบ + export CSV (เว็บเท่านั้น) + date range picker ดู `docs/tickets/14-financial-audit-trail.md`, `docs/DECISIONS.md` #27 |

ผู้ใช้ถามว่า "ทำฟีเจอร์ครบหมดหรือยังที่จะไปแข่งได้" หลังจากรายการ 🔴🟠 เดิมทั้งหมดขึ้นสถานะ
✅ ครบแล้ว จึงตรวจโค้ดจริงซ้ำอีกรอบทีละไฟล์แทนที่จะเชื่อสถานะที่ติ๊กไว้ พบว่าทุกอย่างที่ติ๊ก ✅
ไว้ทำจริงครบ (DB + backend + UI + test) แต่เจอ gap ใหม่ 1 จุดที่ไม่เคยอยู่ในเอกสารนี้มาก่อน:

**`backend/src/modules/payments/payment.schema.js`** กำหนด
`PAYMENT_METHODS = ['cash', 'qr', 'card', 'transfer']` เป็น enum เฉยๆ — `qr` ถูกจัดการเหมือน
`card`/`transfer` ทุกประการ (มีแค่ช่อง `reference` ให้กรอกข้อความอิสระ) **ไม่มีการ generate QR
code จริงตามมาตรฐาน PromptPay (EMV QR) ไม่มี payment gateway ไม่มี callback/signature ตรวจสอบ
ว่าลูกค้าจ่ายเงินจริง** — ค้นทั้ง `app/` และ `backend/` ไม่พบ package หรือโค้ดที่เกี่ยวกับ
PromptPay/QR generation เลยแม้แต่ไฟล์เดียว

**ทำไมจัดเป็น 🔴 Critical ไม่ใช่ 🟡 nice-to-have:** POS คู่แข่งในตลาดไทยทุกเจ้า (FoodStory,
Loyverse ฯลฯ) มี PromptPay QR จริงเป็นมาตรฐานขั้นต่ำ ถ้าร้านเอาไปใช้จริงโดยเข้าใจว่า "QR" คือรับ
เงินผ่านระบบจริง ทั้งที่จริงเป็นแค่ปุ่มให้กดยืนยันเองว่า "ลูกค้าโอนแล้ว" (cashier ต้องไปเช็คสลิป/
แอปธนาคารเองนอกระบบ) มีความเสี่ยงทั้งเรื่อง trust และเงินขาด/หายที่ตรวจสอบย้อนหลังไม่ได้ — เป็น
gap เดียวที่เหลืออยู่ใน 🔴 Critical หลังจากรายการ 1-4 เดิมทำครบแล้ว

**ขอบเขตแนะนำสำหรับ ticket ถัดไป:** อย่างน้อยควร generate QR code ตาม PromptPay standard
(EMV QR) ให้ลูกค้าสแกนจ่ายได้จริง แม้จะยังไม่ต้องมี payment gateway เต็มรูป (ตรวจสอบสถานะจ่าย
อัตโนมัติ) ก็ตาม — เป็นก้าวแรกที่ทำได้เร็วและกระทบภาพลักษณ์มากที่สุด

**#17 Audit ระดับบัญชี/การเงิน** — ผู้ใช้ระบุตั้งแต่ต้นว่าต้องการ audit "ทั้ง financial และ
restaurant manager" ticket 13 (เสร็จแล้ว) ปิดฝั่งผู้จัดการร้าน ("ใครสั่ง/แก้ไขออเดอร์") แต่ตรวจโค้ด
จริงพบว่าฝั่งบัญชียังไม่ครบ: `menu.service.js` (แก้ราคาเมนู), `promotion.service.js` (สร้าง/แก้/ลบ
โปรโมชัน), `ingredient.service.js` (ปรับสต๊อก/ต้นทุนมือ) **ไม่มีการเรียก `auditLogService.log()`
เลยสักจุดเดียว** — ข้อมูลหลักที่กระทบรายได้/ต้นทุนโดยตรงจึงตรวจสอบย้อนหลังไม่ได้ ต่างจาก
order-level discount ที่ log ครบแล้ว นอกจากนี้ audit log เองก็ยังไม่มีทาง export ให้ฝ่ายบัญชี และ
ยังไม่มี date range picker บน UI (ของค้างจาก `docs/DECISIONS.md` #21) — รายละเอียดเต็มดู
`docs/tickets/14-financial-audit-trail.md`
