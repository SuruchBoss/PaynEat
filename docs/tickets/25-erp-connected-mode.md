# Ticket: โหมดเชื่อมต่อ PaynEat ERP — ลงทะเบียน, ดึง master data ตามเวอร์ชัน, หน้าจัดการเป็นอ่านอย่างเดียว

**Priority:** 🟠 High — สัปดาห์ที่ 4 ของแผน PaynEat ERP v1
**Ref:** [ERP ADR-0002](https://github.com/SuruchBoss/PaynEat-ERP/blob/main/docs/adr/0002-system-boundaries-and-pos-integration.md),
[ERP ADR-0011](https://github.com/SuruchBoss/PaynEat-ERP/blob/main/docs/adr/0011-ecosystem-and-sherwhyve.md),
[สเปก ERP #1](https://github.com/SuruchBoss/PaynEat-ERP/issues/1), `docs/DECISIONS.md` #66
**Blocked by:** [PaynEat-ERP#9](https://github.com/SuruchBoss/PaynEat-ERP/issues/9) (สัญญาเชื่อมต่อ v1 ต้อง merge แล้ว),
`24-telemetry-contract.md`

## ปัญหา
เชนร้านที่ใช้ PaynEat ERP ต้องให้รายการวัตถุดิบ สาขา (และต่อไปคือเมนู ราคา สูตร) มีเจ้าของที่เดียวคือ ERP แต่ POS ทุกวันนี้
จัดการข้อมูลพวกนี้เองทั้งหมด ถ้าปล่อยให้แก้ได้ทั้งสองที่ ข้อมูลจะไม่ตรงกันภายในสัปดาห์เดียว

## ทำไมสำคัญ
เป็นขั้นแรกที่ทำให้ POS เข้าร่วมระบบนิเวศได้ **โดยไม่ทำให้ร้านเดี่ยวที่โหลด POS ไปใช้เสียอะไรเลย**

## ขอบเขตงาน
- **ค่าเริ่มต้นคือโหมดใช้งานเดี่ยว** ทำงานเหมือนเดิมทุกประการ ไม่มีอะไรเปลี่ยนสำหรับร้านที่ไม่มี ERP
- **เข้าโหมดเชื่อมต่อ**: admin ใส่ URL ของ ERP และ machine credential ที่ได้ตอนลงทะเบียน POS instance ใน ERP
  - เก็บ credential อย่างปลอดภัย: ไม่ log, ไม่ส่งกลับไปที่แอปหลังบันทึก, และห้ามอยู่ในไฟล์ export ใดๆ
  - ออกจากโหมดเชื่อมต่อได้ ข้อมูลที่ mirror ไว้ยังอยู่และกลับมาแก้ได้ บันทึกพฤติกรรมนี้ใน DECISIONS
- **ดึง master data ตามเวอร์ชัน** ตามสัญญา v1: ดึงเป็นรอบ และมีปุ่ม "ดึงทันที" เก็บเวอร์ชันล่าสุดที่นำไปใช้แล้ว
  ถ้าเน็ตหลุดนานก็ดึงต่อจากเวอร์ชันนั้นได้โดยไม่พลาดอะไร
  - **รายการสินค้าใน ERP → วัตถุดิบใน POS** จับคู่ด้วย item code
  - **สาขาใน ERP → สาขาใน POS** จับคู่ด้วย**รหัสสถานที่กลาง** (`^[A-Z0-9][A-Z0-9-]{1,31}$`) รหัสสาขาที่ไม่ตรงรูปแบบใช้ใน
    โหมดเดี่ยวต่อได้ แต่ต้องแก้ก่อนเข้าโหมดเชื่อมต่อ โดยหน้าจอต้องบอกว่าสาขาไหนต้องแก้อะไร
- **ในโหมดเชื่อมต่อ หน้าจัดการสิ่งที่ ERP เป็นเจ้าของเป็นอ่านอย่างเดียว** และมีป้าย "จัดการใน PaynEat ERP" ส่วน API
  ปฏิเสธการแก้ด้วย 409 พร้อมเหตุผล (ตอนนี้คือวัตถุดิบและสาขา — เมนู ราคา สูตร จะตามมาเมื่อ ERP เปิดสัญญารุ่นถัดไป)
- **สต๊อกช่วงคั่น (สำคัญ)**: ในโหมดเชื่อมต่อ ยอดคงเหลือของสาขาเป็นของ ERP (ERP ADR-0003) แต่สัญญา v1 ยังไม่มี feed
  ยอดคงเหลือกลับมาที่ POS ดังนั้นระหว่างนี้:
  - POS **ต้องไม่ใช้ยอดวัตถุดิบในเครื่องเป็นความจริง**: ปิดการปิดขายอัตโนมัติจากสต๊อกในเครื่อง และใช้การปิดขายด้วยมือแทน
  - **การขายต้องไม่ถูกบล็อกเพราะสต๊อก**
- **telemetry**: log การดึง master data มี `event`, จำนวนรายการ และเวอร์ชัน ส่วน credential ห้ามโผล่
- **contract test**: ตรวจ response ของ ERP ตัวอย่างกับไฟล์สัญญา v1 ใน CI (ปักเวอร์ชันไว้)
- **Demo Mode**: แสดงสถานะ "ใช้งานเดี่ยว" เสมอ

## ขอบเขตที่ตั้งใจไม่ทำ
- ดึงเมนู ราคา สูตร จาก ERP — รอ ERP เปิดสัญญารุ่นถัดไป แล้วจะเปิด ticket ต่อยอด
- feed ยอดคงเหลือสาขาจาก ERP เพื่อขึ้นป้าย "หมด" อัตโนมัติ — รอสัญญารุ่นถัดไปเช่นกัน
- single sign-on ระหว่าง POS กับ ERP
- การส่งยอดขาย (ticket 26)

## Acceptance Criteria
- [ ] ร้านที่ไม่ตั้งค่า ERP ทำงานเหมือนเดิมทุกประการ (เทสต์เดิมทั้งหมดผ่านโดยไม่ต้องแก้)
- [ ] เข้าและออกจากโหมดเชื่อมต่อได้ credential ไม่โผล่ใน log, response หรือไฟล์ export
- [ ] ดึง master data ตามเวอร์ชันได้ ดึงซ้ำไม่ได้ข้อมูลซ้ำ ต่อจากเน็ตหลุดได้โดยไม่พลาดรายการ
- [ ] สาขาที่รหัสไม่ตรงรูปแบบถูกบอกชัดว่าต้องแก้อะไรก่อนเชื่อมต่อ
- [ ] โหมดเชื่อมต่อ: แก้วัตถุดิบ/สาขาไม่ได้ทั้งทางหน้าจอและ API (409) และไม่มีการปิดขายอัตโนมัติจากสต๊อกในเครื่อง
- [ ] contract test ผ่านกับไฟล์สัญญา v1
- [ ] README (ไทย/อังกฤษ), `docs/DECISIONS.md`, `docs/FEATURE-GAP-ANALYSIS.md` อัปเดตตาม `CLAUDE.md`

## เทสต์
backend: stub server ของ ERP ในเทสต์ (ไม่ต้องรัน ERP จริง) ครอบคลุมการเข้า/ออกโหมด การดึงตามเวอร์ชัน รหัสสาขาผิดรูปแบบ
และการปฏิเสธการแก้ในโหมดเชื่อมต่อ
แอป: controller test ของหน้าตั้งค่าการเชื่อมต่อ และของหน้าจัดการในโหมดอ่านอย่างเดียว
