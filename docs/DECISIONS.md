# บันทึกการตัดสินใจเชิงออกแบบ

เอกสารนี้เก็บ **เหตุผล** เบื้องหลังทางเลือกสำคัญในโปรเจกต์ พร้อมข้อเสียที่ยอมรับ
เขียนไว้เพื่อให้คนที่มาอ่านโค้ด (รวมถึงตัวเองในอีก 6 เดือน) เข้าใจว่าทำไมถึงเลือกทางนี้

---

## 1. เก็บจำนวนเงินเป็นสตางค์ (integer) ไม่ใช่ทศนิยม

**ปัญหา** — `0.1 + 0.2 !== 0.3` ในทุกภาษาที่ใช้ IEEE 754 พอเอามาคิดบิลหลาย ๆ ครั้ง ยอดจะเพี้ยนทีละสตางค์
ซึ่งในระบบที่เกี่ยวกับเงินถือว่ารับไม่ได้

**ที่เลือก** — ฝั่ง backend เก็บทุกจำนวนเงินเป็นจำนวนเต็มหน่วยสตางค์ (`120.50 บาท → 12050`)
แล้วแปลงกลับเป็นบาทเฉพาะตอนส่งออกทาง API

**ข้อเสียที่ยอมรับ** — ต้องมี `toSatang()` / `toBaht()` คั่นทุกจุดที่รับ-ส่งข้อมูล ถ้าลืมจะเพี้ยน 100 เท่าทันที
จึงรวมไว้ที่ `core/money.js` ที่เดียว และบังคับให้ mapper เป็นทางผ่านเดียวที่แปลงค่า

**ทางเลือกที่ไม่ได้เลือก** — ใช้ decimal library เพราะเพิ่ม dependency และช้ากว่าโดยไม่จำเป็นสำหรับสเกลนี้

---

## 2. ตรรกะคิดบิลเขียน 2 ที่ (Dart + JavaScript)

**ปัญหา** — พนักงานควรเห็นยอดทันทีขณะกดสั่งบนแท็บเล็ต แต่ยอดที่ใช้เก็บเงินจริงต้องเชื่อถือได้และมาจากที่เดียว

**ที่เลือก** — เขียนตรรกะเดียวกันไว้ 2 ที่โดยตั้งใจ:

| ที่ | หน้าที่ | ความสำคัญ |
|---|---|---|
| `backend/.../order.calculator.js` | ยอดจริงที่บันทึกและใช้เก็บเงิน | เป็นแหล่งความจริงเสมอ |
| `app/.../bill_calculator.dart` | พรีวิวในตะกร้าก่อนกดยืนยัน | แค่แสดงผล ไม่เคยถูกส่งขึ้นเซิร์ฟเวอร์ |

**ข้อเสียที่ยอมรับ** — โค้ดซ้ำ 2 ภาษา ถ้าแก้กฎต้องแก้ทั้งคู่
จึงมีเทสต์ทั้งสองฝั่งที่ยืนยันด้วย **ตัวเลขชุดเดียวกัน** (180 → 211.86) ถ้าฝั่งใดฝั่งหนึ่งเพี้ยน เทสต์จะจับได้ทันที
และแอปไม่เคยส่งยอดที่คำนวณเองขึ้นไป — ส่งแค่รายการอาหาร เซิร์ฟเวอร์คิดเงินเอง

**ทางเลือกที่ไม่ได้เลือก** — ยิง API ทุกครั้งที่กดเพิ่มของ เพราะ UX แย่ในร้านที่ WiFi ไม่ดี

---

## 3. ใช้ `Result<T>` ที่เขียนเอง แทน `dartz` / การโยน exception ข้ามชั้น

**ที่เลือก** — sealed class `Result<T>` ที่มี `fold(onSuccess:, onFailure:)`

```dart
result.fold(
  onSuccess: (order) => Get.toNamed(AppRoutes.orderDetail),
  onFailure: (failure) => AppDialogs.error(failure.message),
);
```

**เหตุผล**

- Dart 3 มี sealed class + exhaustive `switch` อยู่แล้ว ไม่จำเป็นต้องพึ่ง dartz
- compiler บังคับให้จัดการทั้งกรณีสำเร็จและล้มเหลว ลืมไม่ได้
- ชื่อ `onSuccess` / `onFailure` อ่านง่ายกว่า `Left` / `Right` สำหรับคนที่ไม่คุ้น FP
- ไม่มี exception หลุดข้ามชั้นไปโผล่กลางหน้าจอ

**ข้อเสียที่ยอมรับ** — ไม่มี `flatMap` / `traverse` แบบ dartz ถ้าโปรเจกต์ใหญ่ขึ้นและมีการต่อ operation เยอะอาจต้องเพิ่มเอง

---

## 4. รายการอาหารที่ครัวเริ่มทำแล้ว "แก้ไม่ได้"

**ปัญหาจริงในร้าน** — ถ้าพนักงานลดจำนวนหลังครัวลงมือทำไปแล้ว วัตถุดิบเสียฟรีและยอดขายหาย

**ที่เลือก** — ล็อกด้วยสถานะของรายการอาหาร

| สถานะ | แก้จำนวน | ลบ | ยกเลิก (void) |
|---|:---:|:---:|:---:|
| `pending` (ยังไม่เริ่มทำ) | ✅ | ✅ | ✅ ใครก็ได้ |
| `cooking` / `ready` | ❌ | ❌ | ✅ เฉพาะผู้จัดการ |
| `served` | ❌ | ❌ | ❌ |

บังคับทั้งฝั่ง API (คืน 409) และซ่อนปุ่มในแอป — **ฝั่ง API เป็นตัวตัดสิน** ส่วน UI แค่ช่วยไม่ให้กดผิด

---

## 5. โต๊ะหนึ่งเปิดได้ทีละบิล

**ที่เลือก** — สร้างออเดอร์บนโต๊ะที่ยังมีบิลค้างจะได้ HTTP 409 พร้อมข้อความให้ไปเพิ่มรายการในบิลเดิม

**เหตุผล** — กันเคสพนักงาน 2 คนเดินไปโต๊ะเดียวกันแล้วเปิดบิลซ้อน ทำให้ยอดแยกกันและเก็บเงินพลาด

**ข้อเสียที่ยอมรับ** — ยังไม่รองรับ "แยกบิลรายคน" ซึ่งร้านจริงเจอบ่อย บันทึกไว้ในสิ่งที่จะทำต่อแล้ว

---

## 6. SQLite แทน PostgreSQL

**ที่เลือก** — SQLite ผ่าน `better-sqlite3`

**เหตุผล**

- ร้านอาหาร 1 สาขามี transaction ระดับหลักพันต่อวัน — SQLite รับไหวสบาย
- ผู้ที่มาดูผลงาน `git clone` แล้ว `npm run dev` ได้เลย ไม่ต้องติดตั้ง DB server หรือรัน container
- `better-sqlite3` เป็น synchronous API ทำให้โค้ด repository อ่านง่ายและไม่ต้องจัดการ connection pool
- เปิด WAL mode ให้อ่าน/เขียนพร้อมกันได้จากหลายเครื่องในร้าน

**ข้อเสียที่ยอมรับ** — ขยายเป็นหลายสาขาที่เขียนพร้อมกันหนัก ๆ ไม่ได้
จึงแยกทุก SQL ไว้ในชั้น `repository` เท่านั้น ถ้าจะย้ายไป PostgreSQL แก้ชั้นเดียวจบ ชั้น service ไม่ต้องแตะ

---

## 7. GetX ทั้ง state, DI และ routing

**ที่เลือก** — GetX ตัวเดียวคุมทั้ง 3 อย่าง

**เหตุผล**

- โจทย์ระบุให้ใช้ GetX
- `Bindings` ผูกกับ route ทำให้ controller ถูกสร้างตอนเข้าหน้าและถูกเก็บกวาดตอนออก — จัดการ lifecycle ได้ชัด
- `Get.lazyPut(fenix: true)` ทำให้บัญชีครัวไม่ต้องเสียแรงสร้าง controller ของรายงานที่ไม่มีสิทธิ์เห็น

**ข้อควรระวังที่จัดการแล้ว**

- GetX ทำให้ "เรียกอะไรจากที่ไหนก็ได้" ซึ่งพังโครงสร้างได้ง่าย → บังคับให้ controller รับ dependency ผ่าน constructor เท่านั้น
  ไม่เรียก `Get.find()` กลางไฟล์ ทำให้เทสต์ยัดของปลอมเข้าไปได้ (ดู `cart_controller_test.dart`)
- `Get.snackbar` / `Get.dialog` ถูกรวมไว้ที่ `AppDialogs` ที่เดียว ไม่กระจายทั่วโค้ด

---

## 8. Demo Mode สลับที่ชั้น data source

**ปัญหา** — อยากให้คนที่มาดูผลงานกดลิงก์เดียวแล้วลองใช้ได้เลย โดยไม่ต้องจ่ายค่าเซิร์ฟเวอร์
และไม่เจอ cold start 30 วินาทีของ free tier

**ที่เลือก** — เขียน data source อีกชุดที่คุยกับ "เซิร์ฟเวอร์จำลอง" ในหน่วยความจำ (`core/demo/`)
แล้วสลับด้วย `--dart-define=DEMO_MODE=true` ที่ `InitialBinding` จุดเดียว

```dart
void _bindDataSources() {
  if (AppConfig.demoMode) {
    _bindDemoDataSources();   // ← สลับที่นี่ที่เดียว
    return;
  }
  ...
}
```

**ผลที่ได้** — deploy ขึ้น GitHub Pages ฟรีถาวร ไม่มีเซิร์ฟเวอร์ ไม่มี cold start
และเป็นตัวพิสูจน์ว่าการแยกชั้นได้ผลจริง เพราะ **หน้าจอ, controller และ use case ไม่ถูกแก้แม้แต่บรรทัดเดียว**

**ข้อเสียที่ยอมรับ** — ข้อมูลหายเมื่อรีเฟรช และไม่มี realtime (ไม่มี socket ให้ต่อ)
ยอมรับได้เพราะเป็นโหมดสำหรับลองใช้เท่านั้น และมีแบนเนอร์บอกผู้ใช้ชัดเจน

---

## 9. โหลดเมนูทั้งหมดครั้งเดียวแล้วกรองในเครื่อง

**ที่เลือก** — ดึงเมนูทั้งหมด (หลักสิบรายการ) มาเก็บไว้ แล้วกรองหมวดหมู่/ค้นหาในเครื่อง

**เหตุผล** — พนักงานกดสลับหมวดหมู่ถี่มากตอนรับออเดอร์ ถ้ายิง API ทุกครั้งจะหน่วงและกินเน็ตในร้านที่ WiFi ไม่ดี
ส่วนช่องค้นหาก็หน่วง 250ms ก่อนกรอง เพื่อไม่ให้ rebuild ทุกตัวอักษร

**ข้อเสียที่ยอมรับ** — ถ้าร้านมีเมนูหลักพันรายการต้องเปลี่ยนไปใช้ pagination
ซึ่ง API รองรับอยู่แล้ว (`?page=&limit=`) แค่ยังไม่ได้ใช้ฝั่งแอป

---

## 10. เขียนกราฟเองแทนใช้ package

**ที่เลือก** — กราฟแท่งรายชั่วโมงและแถบสัดส่วนเขียนด้วย widget พื้นฐานของ Flutter

**เหตุผล**

- ต้องการแค่กราฟแท่งเรียบ ๆ 2 แบบ การลง `fl_chart` เพิ่ม ~300KB เพื่อใช้ 5% ของความสามารถไม่คุ้ม
- คุมสีและมุมโค้งให้เข้ากับธีมได้เป๊ะโดยไม่ต้องสู้กับ API ของ package
- ลด dependency ที่ต้องตามอัปเดตในระยะยาว

**ข้อเสียที่ยอมรับ** — ถ้าต้องการกราฟซับซ้อน (เส้นหลายชุด, zoom, tooltip ขั้นสูง) ควรเปลี่ยนไปใช้ package

---

## 11. พิมพ์ใบเสร็จผ่านเครื่องพิมพ์ความร้อนจริง — เจาะจงแค่ LAN/WiFi ก่อน (ไม่ใช่ Bluetooth/USB)

> อัปเดต: เดิมข้อนี้เคยตัดสินใจไม่ต่อเครื่องพิมพ์จริงเลย (เดโมพอร์ตโฟลิโอ ไม่ใช่ระบบขายจริง)
> ภายหลังพบว่าเป็น blocker จริงสำหรับการใช้งานหน้าร้าน (ดู `docs/FEATURE-GAP-ANALYSIS.md`
> #3 และ `docs/tickets/03-receipt-printer.md`) จึงทำเพิ่ม แต่ยังเจาะจงขอบเขตตามด้านล่าง

**ที่เลือก** — พิมพ์ใบเสร็จจริงผ่าน ESC/POS โดยส่งคำสั่งเป็น raw bytes ผ่าน TCP socket ไปยัง
เครื่องพิมพ์ความร้อนบนวง LAN/WiFi เดียวกัน (พอร์ต 9100 ซึ่งเป็นค่ามาตรฐานของเครื่องพิมพ์เกือบทุก
ยี่ห้อ) — ตั้งค่า IP/พอร์ต/ขนาดกระดาษได้จากหน้าตั้งค่า ผูกกับเครื่อง cashier นั้น ๆ ไม่ผูกกับร้าน
ยังไม่รองรับ Bluetooth หรือ USB

**เหตุผล** — เครื่องพิมพ์ความร้อนที่ร้านอาหารไทยใช้ทั่วไป (Xprinter, Gprinter, HPRT ฯลฯ) รองรับ
raw ESC/POS ผ่าน WiFi/Ethernet เป็นมาตรฐานอยู่แล้ว ทำให้เชื่อมต่อได้โดยไม่ต้องพึ่ง native plugin
สำหรับจับคู่ Bluetooth (ต้องขอ permission เพิ่มทั้ง Android/iOS และมีโค้ด platform channel ที่
ทดสอบได้เฉพาะบนอุปกรณ์จริงเท่านั้น) — สภาพแวดล้อมพัฒนานี้ไม่มีฮาร์ดแวร์เครื่องพิมพ์หรืออุปกรณ์
Bluetooth จริงให้ทดสอบ จึงจำกัดขอบเขตไว้ที่ทางที่ตรวจสอบได้แน่ใจว่าถูกต้องก่อน
(ยืนยันด้วยเทสต์ของตาราง CP874/ESC-POS bytes และรันจริงในเว็บว่า fallback ทำงานถูกต้อง)

ข้อความไทยเข้ารหัสเองด้วยตาราง CP874/TIS-620 (`Cp874Codec`) เพราะไลบรารี `esc_pos_utils_plus`
ที่ใช้สร้างคำสั่ง ESC/POS รองรับแค่ `latin1`/GBK ไม่มี codec ภาษาไทยมาให้

เว็บเปิด TCP socket ตรง ๆ จาก browser ไม่ได้ (ไม่มี `dart:io`) จึงคืนข้อความแจ้งให้ใช้ใบเสร็จ
บนจอแทนทันทีเมื่อกดพิมพ์ ไม่กระทบ demo mode หรือแพลตฟอร์มที่ยังไม่รองรับ

**ข้อเสียที่ยอมรับ** — เครื่องพิมพ์ที่ต่อผ่าน Bluetooth/USB เท่านั้น (ไม่มี WiFi/Ethernet)
ยังใช้กับฟีเจอร์นี้ไม่ได้ ถ้าจะเพิ่มต้องต่อ native plugin ใหม่ (เช่น `esc_pos_bluetooth`) และ
ต้องมีอุปกรณ์จริงมาเทสต์ — สถาปัตยกรรมรองรับอยู่แล้วเพราะเทมเพลตใบเสร็จ (`ReceiptTicketBuilder`)
แยกจากชั้นส่งข้อมูล (`printer_transport_io.dart`) ไว้ตั้งแต่แรก เปลี่ยน/เพิ่ม transport ได้โดยไม่
ต้องแตะ logic การสร้างใบเสร็จ

---

## 12. ใช้ SQLite ต่อไป ไม่ย้ายไป PostgreSQL แม้รองรับหลายสาขาได้ดีกว่า

**ที่เลือก** — คงสถาปัตยกรรมสาขาเดียว (single-tenant, single-database) ไว้ตามสโคปเดิมของโปรเจกต์

**เหตุผล** — เป้าหมายของโปรเจกต์คือร้านอาหารสาขาเดียว การรองรับหลายสาขาต้องออกแบบเพิ่ม
(tenant isolation, sync ข้าม branch, aggregate reporting ข้ามสาขา) ซึ่งเป็นงานออกแบบระดับใหญ่
ที่ไม่ได้อยู่ในสโคปที่ต้องการนำเสนอตอนนี้ — ทำไปก็จะเพิ่มความซับซ้อนโดยไม่ได้ใช้จริง

**ข้อเสียที่ยอมรับ** — ถ้าลูกค้าต้องการหลายสาขาในอนาคต ต้องออกแบบ multi-tenancy เพิ่มเติม
แต่ชั้น repository ที่แยกไว้แล้ว (ดูข้อ 6) ทำให้เปลี่ยนฐานข้อมูลจาก SQLite ไป PostgreSQL
ได้โดยไม่ต้องแตะ service/controller layer

---

## 13. Offline mode — เจาะจงแค่ "สั่งเพิ่มเข้าออเดอร์เดิม" เท่านั้น

**ที่เลือก** — action เดียวที่ทำงานแบบ offline-first (queue ในเครื่องแล้ว sync อัตโนมัติเมื่อ
เน็ตกลับมา) คือ **สั่งเพิ่มรายการอาหารเข้าออเดอร์ที่เปิดอยู่แล้ว** (`CartController.submit()`
เมื่อ `isAddingToExistingOrder == true`) เท่านั้น เก็บคิวด้วย `get_storage` ผ่าน
`OfflineOrderQueueService` แสดงจำนวนรายการค้าง sync เป็น badge บน AppBar ของทุกหน้า (ต่อยอด
จากไฟบอกสถานะ socket ที่มีอยู่แล้ว) ตรวจจับ "ออฟไลน์" จาก `NetworkFailure` ที่โยนมาจาก
`ApiClient` เมื่อ HTTP call ล้มเหลวจริง (connection error/timeout) ไม่ใช้ปลั๊กอินตรวจสถานะ
เครือข่ายของ OS (เช่น `connectivity_plus`)

**Action ที่ยังไม่รองรับ offline โดยตั้งใจ (ต้องออนไลน์เสมอ):**
- เปิดออเดอร์ใหม่ (`CreateOrderUseCase`) — เสี่ยง conflict สูงกว่า (จองโต๊ะซ้ำ, เลขที่ออเดอร์
  ต้องมาจากเซิร์ฟเวอร์)
- ชำระเงิน/คืนเงิน (`PayOrderUseCase`, refund) และ เปิด/ปิดกะ — ต้องถูกต้องแม่นยำเรื่องเงินเสมอ
  ห้ามมีโอกาส apply ซ้ำหรือ double-count จากการ retry
- ย้ายโต๊ะ/รวมบิล/แยกบิล/ยกเลิกออเดอร์/ส่วนลด — แก้ไข state ของออเดอร์ที่อาจถูกฝั่งอื่นแก้พร้อมกัน
  ระหว่างออฟไลน์ ความเสี่ยง conflict สูง
- จัดการเมนู/พนักงาน/ตั้งค่าร้าน — ไม่ใช่ operation เร่งด่วนระดับหน้าร้าน รอเน็ตกลับมาได้

**เหตุผล** — ตามคำแนะนำใน `docs/tickets/04-offline-mode.md`: เริ่มจาก action ที่ความเสี่ยง
conflict ต่ำสุดก่อน สั่งเพิ่มเข้าออเดอร์ที่เปิดอยู่แล้วแทบไม่มีทางชนกับ state อื่นเพราะแค่ "เพิ่ม
รายการต่อท้าย" ไม่ได้แก้ไขข้อมูลเดิม ต่างจากเปิดออเดอร์ใหม่/ชำระเงินที่ผลลัพธ์ผูกกับสถานะ ณ
ขณะนั้นบนเซิร์ฟเวอร์โดยตรง หากรายการที่ queue ไว้ sync ไม่สำเร็จเพราะเหตุผลอื่นที่ไม่ใช่เน็ตหลุด
(เช่นออเดอร์ถูกปิด/ลบไปแล้วระหว่างที่ออฟไลน์) ระบบจะตัดรายการนั้นออกจากคิวทันทีแทนที่จะ retry
วนไม่รู้จบ แล้วบันทึกข้อความไว้แจ้งผู้ใช้ (`lastFailureMessage`)

**ข้อเสียที่ยอมรับ** — พนักงานยังเปิดออเดอร์ใหม่/รับชำระเงินไม่ได้ตอนเน็ตหลุด ถ้าจะขยายขอบเขต
ต้องออกแบบเพิ่ม (เช่นเลขที่ออเดอร์ชั่วคราวที่เครื่อง + เปลี่ยนเป็นเลขจริงตอน sync, ล็อกการรับชำระ
ซ้ำระหว่าง sync) ซึ่งซับซ้อนกว่ามากและมีความเสี่ยงเรื่องเงินโดยตรง — การตรวจจับออฟไลน์ด้วย
`NetworkFailure` (reactive, หลัง API call ล้มเหลวจริง) ก็ตรวจจับได้ช้ากว่าปลั๊กอินตรวจสถานะ
เครือข่ายของ OS (proactive) เล็กน้อย แต่ไม่ต้องพึ่ง native permission และทดสอบได้เต็มรูปแบบด้วย
unit test ล้วน ๆ

---

## 14. Promotion engine — ไม่ซ้อนโปรโมชันหลายใบ, รวมกับส่วนลดมือได้แต่ไม่เกิน subtotal, auto-match ประเมินใหม่ทุกครั้ง

**ปัญหา** — ต้องเพิ่มส่วนลดแบบตั้งเงื่อนไขล่วงหน้า (happy hour, โค้ดส่วนลด, ซื้อ 1 แถม 1) โดยห้าม
กระทบลำดับการคำนวณเดิม (`subtotal → discount → service charge → VAT`, ดูข้อ 2) และห้ามทำให้
`order.calculator.js`/`bill_calculator.dart` ซับซ้อนเกินจำเป็น

**ที่เลือก**

- **สูงสุด 1 โปรโมชันต่อบิลเสมอ** ไม่ว่าจะ auto-match หรือกรอกโค้ด — เลี่ยงความซับซ้อนของการจัดลำดับ/
  รวมหลายโปรโมชันพร้อมกันในเวอร์ชันแรก ตรรกะจับคู่ทั้งหมดแยกไว้ที่ `promotion.engine.js`
  (ฟังก์ชันบริสุทธิ์ทั้งหมด) ไม่ปนกับ `calculateBill` ที่รับแค่ `promotionDiscountAmount` ไปรวม
- **ส่วนลดโปรโมชัน "รวมกับ" ส่วนลดมือได้** (ไม่ใช่เลือกอย่างใดอย่างหนึ่ง) แต่ผลรวมทั้งคู่ต้องไม่เกิน
  subtotal — สะท้อนพฤติกรรมจริงของร้าน (แคชเชียร์อาจลดให้ลูกค้าประจำเพิ่มเติมนอกเหนือจากโปรโมชันที่มีอยู่แล้ว)
- **โปรโมชันแบบ auto (ไม่มีโค้ด) ถูกประเมินใหม่ทุกครั้งที่คำนวณบิล** (`recalculate()`) เลือกตัวที่ให้
  ส่วนลดมากที่สุดในบรรดาที่เข้าเงื่อนไข ณ ขณะนั้น — ถ้าลูกค้ากรอกโค้ดไว้ (`promotion_code_snapshot`
  ไม่เป็น null) จะ "ยึด" โค้ดนั้นไว้เป็นหลักจนกว่าจะไม่เข้าเงื่อนไขอีกต่อไปหรือถูกลบออกเอง แม้ auto
  promotion อื่นจะให้ส่วนลดมากกว่าก็ตาม — ป้องกันไม่ให้ระบบ "แย่ง" เปลี่ยนโปรโมชันที่ลูกค้าตั้งใจใช้เอง
- **ลบโปรโมชันออกจากออเดอร์ (`DELETE /orders/:id/promotion`) ไม่ได้แปลว่า "ห้ามใช้โปรโมชันอีก"** —
  เป็นแค่การเคลียร์โค้ดที่ผูกไว้ ถ้ายังมี auto promotion ที่เข้าเงื่อนไขอยู่ ระบบจะใส่กลับให้ทันทีตอน
  คำนวณครั้งถัดไป (เหมือน VAT/Service Charge ที่คำนวณจาก settings ปัจจุบันเสมอ ไม่ใช่ค่าที่ "ล็อก" ไว้)
- **BOGO** คำนวณด้วยการกระจายราคาเป็น "ราคาต่อชิ้น" ของทุกรายการที่เข้าเงื่อนไข (ข้ามเมนูได้) เรียง
  ถูกไปแพง แล้วให้ครึ่งที่ถูกกว่าฟรี (`floor(count/2)` ชิ้น) — ง่ายกว่าการจับคู่ "ซื้อเมนู A แถมเมนู A"
  ตรง ๆ และให้ผลลัพธ์ที่ยุติธรรมกับลูกค้าเมื่อซื้อเมนูราคาต่างกันในโปรโมชันเดียว
- **โปรโมชันที่ใช้ไปแล้วถูก snapshot ไว้ที่ออเดอร์** (`promotion_name_snapshot`,
  `promotion_code_snapshot`) แยกจาก `promotion_id` (FK แบบ `ON DELETE SET NULL`) — ลบโปรโมชันทิ้งจาก
  ระบบภายหลังจะไม่ทำให้ออเดอร์เก่าที่เคยใช้โปรโมชันนั้นแสดงข้อมูลหายหรือพัง

**ข้อเสียที่ยอมรับ** — ร้านที่อยากให้ลูกค้าใช้โปรโมชันซ้อนกันหลายใบพร้อมกัน (เช่น โค้ดส่วนลด + happy
hour พร้อมกัน) ทำไม่ได้ในเวอร์ชันนี้ ต้องเลือกใช้ได้ทีละ 1 ใบ — ถ้าจะรองรับต้องออกแบบลำดับความสำคัญ/
กติกาการรวมโปรโมชันหลายใบเพิ่ม ซึ่งซับซ้อนขึ้นมากและเสี่ยงสร้างส่วนลดที่ร้านไม่ได้ตั้งใจให้

---

## 15. Inventory/Stock — หน่วยอิสระไม่แปลงข้ามหน่วย, ตัดสต๊อกตอนส่งครัว, ผูกวัตถุดิบฝังในเมนู

**ปัญหา** — ต้องเพิ่มระบบตัดสต๊อกวัตถุดิบอัตโนมัติเมื่อขาย (ดู `docs/tickets/06-inventory-stock.md`)
โดยห้ามกระทบ flow การสั่งอาหาร/ครัวเดิม และต้องตัดสินใจหลายจุดที่ ticket ไม่ได้ระบุไว้ชัดเจน
(หน่วยนับ, จังหวะตัดสต๊อกที่แน่นอน, การคืนสต๊อกเมื่อยกเลิก, รายการที่สั่งเพิ่มหลังส่งครัวไปแล้ว)

**ที่เลือก**

- **หน่วย (`unit`) เป็น string อิสระที่ร้านตั้งเอง** เช่น "กก.", "กรัม", "ฟอง", "ตัว" — ไม่มี unit
  conversion ข้ามหน่วย (เช่น กก. ↔ กรัม) ในเวอร์ชันแรก ร้านต้องตั้งหน่วยของวัตถุดิบกับ `qtyPerUnit`
  ของเมนูให้เป็นหน่วยเดียวกันเอง — เลี่ยงความซับซ้อนของตาราง conversion/หน่วยมาตรฐานที่ไม่จำเป็นสำหรับ
  ร้านอาหารขนาดเล็ก-กลางที่เป็นกลุ่มเป้าหมาย
- **ตัดสต๊อกตอน "ส่งครัว" (`sendToKitchen`) ไม่ใช่ตอนสั่ง/ตอนจ่ายเงิน** — ตรงกับคำว่า "ยืนยัน/ส่งครัว"
  ใน ticket มากที่สุด และเป็นจุดที่ครัวเริ่มลงมือทำจริง (ก่อนหน้านั้นลูกค้ายังแก้ไข/ยกเลิกได้อิสระโดยไม่
  กระทบสต๊อกจริง) รายการที่เพิ่มเข้าออเดอร์ที่ถูกส่งครัวไปแล้วจะถูกตัดสต๊อกทันทีที่เพิ่ม (ไม่ต้องกด
  "ส่งครัว" ซ้ำ) ส่วน `sendToKitchen` เองออกแบบให้ idempotent — เรียกซ้ำได้โดยตัดสต๊อกเฉพาะรายการที่ยัง
  ไม่เคยถูกตัด (`order_items.stock_deducted`)
- **ยกเลิกรายการ/ทั้งบิลคืนสต๊อกอัตโนมัติเสมอ** ถ้ารายการนั้นเคยถูกตัดสต๊อกไปแล้ว (ทั้งยกเลิกรายการเดี่ยว,
  ลบรายการ, แก้จำนวน (ปรับตามส่วนต่างเท่านั้น ไม่ตัดใหม่ทั้งหมด), และยกเลิกทั้งบิล) — สะท้อนว่าวัตถุดิบ
  ยังไม่ถูกใช้จริงถ้าไม่ได้เสิร์ฟลูกค้า
- **`order_items.stock_deducted`** (boolean) ทำให้การตัด/คืนสต๊อกเป็น idempotent และแยกแยะได้ว่ารายการ
  ไหน "ตัดไปแล้วต้องคืนตอนยกเลิก" กับรายการที่ยังไม่เคยตัด (เช่นถูกยกเลิกตั้งแต่ยังเป็น pending ก่อนส่งครัว)
- **`menu_items.auto_disabled_by_stock`** (boolean) แยก "ระบบปิดขายเองเพราะสต๊อกหมด" ออกจาก "พนักงานปิด
  ขายเอง" (สินค้าหมดจริงที่ไม่เกี่ยวกับสต๊อกในระบบ, หรือของไม่อร่อยวันนี้เลยงดขาย) — เติมสต๊อกกลับจะเปิด
  ขายอัตโนมัติให้เฉพาะเมนูที่ระบบเป็นคนปิดเท่านั้น การแก้ไขเมนูที่แตะ `isAvailable` (ฟอร์มแก้เมนู หรือปุ่ม
  เปิด/ปิดขายโดยตรง) ถือเป็นการ override เจตนาของพนักงานเสมอ ระบบจะไม่ auto-เปิดขายทับให้ภายหลัง
- **ผูกวัตถุดิบฝังอยู่ในฟอร์ม/payload สร้าง-แก้ไขเมนู** (`ingredients: [{ingredientId, qtyPerUnit}]`)
  แทนที่จะแยก endpoint/หน้าจอผูกต่างหาก — mirror ทุกจุดกับวิธีที่ `optionGroups` ถูกจัดการอยู่แล้ว (แทนที่
  ทั้งชุดทุกครั้งที่บันทึกเมนู) ลด API surface และนำ pattern ที่พิสูจน์แล้วมาใช้ซ้ำ
- **ค่าที่เกี่ยวกับสต๊อก (`current_stock`, `low_stock_threshold`, `qty_per_unit`) เก็บเป็น `REAL` ตรง ๆ
  ไม่ผ่าน `toSatang`/`toBaht`** (ดูข้อ 1) เพราะไม่ใช่หน่วยเงิน — ความคลาดเคลื่อนทศนิยมเล็กน้อยของสต๊อก
  ยอมรับได้ ต่างจากเงินที่ต้องเป๊ะเสมอ

**ข้อเสียที่ยอมรับ** — ร้านที่ต้องการแปลงหน่วยข้ามกัน (เช่น ซื้อวัตถุดิบเป็น กก. แต่ใช้ต่อเมนูเป็นกรัม)
ต้องตั้งหน่วยให้ตรงกันเองตั้งแต่แรก (ป้อนวัตถุดิบเป็น "กรัม" แทน "กก." ถ้าเมนูใช้หน่วยกรัม) และยังไม่มี
หน้ารายงานการใช้วัตถุดิบ (ingredient usage report) ตามที่ระบุไว้ในขอบเขตงานของ ticket — เพราะไม่ได้อยู่ใน
Acceptance Criteria ทั้ง 4 ข้อ จึงเลื่อนไปทำเป็น ticket แยกในอนาคตถ้าร้านต้องการจริง

---

## 16. ใบเสร็จแสดง "เงินที่ลูกค้ายื่นมา" ไม่ใช่ยอดที่ตัดเข้าบิล

**ปัญหา** — `payments` เก็บทั้ง `amount` (ยอดที่ตัดเข้าบิล) และ `received` (เงินที่ลูกค้ายื่นมา)
แต่ใบเสร็จเดิมแสดง `amount` คู่กับบรรทัดเงินทอน ทำให้ตัวเลขบนใบเสร็จกระทบยอดกันเองไม่ได้ —
ลูกค้าจ่ายเงินสด 227.20 แต่ใบเสร็จเขียน "เงินสด 207.20" คู่กับ "เงินทอน 20.00" ซึ่งไม่ตรงกับ
เงินที่ยื่นให้จริง

**ที่เลือก** — เพิ่ม getter `Payment.tendered` ไว้ที่ entity **ที่เดียว** แล้วให้ทั้งใบเสร็จบนจอ
(`receipt_page.dart`) และใบเสร็จที่พิมพ์ออกเครื่องพิมพ์ความร้อน (`receipt_ticket_builder.dart`)
เรียกใช้ร่วมกัน ใบเสร็จจึงกระทบยอดได้ตามธรรมเนียมใบเสร็จไทย: **ยื่นมา − ทอน = ยอดที่ตัดเข้าบิล**

```dart
double get tendered => received > amount ? received : amount;
```

เงื่อนไข `>` เผื่อข้อมูลเก่าที่ยังไม่มีช่อง `received` (ได้ 0) ให้ถอยไปใช้ `amount` ตามเดิม
ส่วน QR/บัตร/โอน ไม่มีการทอน ทั้ง backend และ demo store เซ็ต `received = amount` อยู่แล้ว
จึงไม่มีอะไรเปลี่ยน

**จุดที่จงใจไม่เปลี่ยน** — ยังใช้ `amount` ตามเดิมสองที่:

- `ReceiptController.refundableAmount` — คืนเงินได้แค่ยอดที่ตัดเข้าบิลจริง ถ้าใช้ `tendered`
  จะคืนเกินเป็นจำนวนเท่ากับเงินทอน
- รายการ "จ่ายแล้ว" ในหน้าเก็บเงิน — เป็นมุมมองยอดคงเหลือ ผลรวมต้องเท่ายอดบิลพอดี

**เหตุผลที่รวมไว้ที่ entity ไม่ใช่แก้ทีละหน้า** — ใบเสร็จบนจอกับใบเสร็จที่พิมพ์เป็นคนละไฟล์และ
เคยเขียนแยกกัน ถ้าแก้ทีละจุดจะเพี้ยนออกจากกันภายหลังโดยไม่มีอะไรฟ้อง เพราะไม่มีใครเปิดเทียบสองที่พร้อมกัน

**ข้อเสียที่ยอมรับ** — ผลรวมของบรรทัดวิธีชำระเงินบนใบเสร็จจะมากกว่ายอดบิลเท่ากับเงินทอน
ซึ่งถูกต้องตามธรรมเนียมใบเสร็จ แต่ต่างจากมุมมองทางบัญชีที่ผลรวมต้องเท่ายอดบิลพอดี —
ระบบจึงแยกสองมุมมองนี้ออกจากกันชัดเจนตามรายการด้านบน

---

## 17. AppClock — รวมการอ่านเวลาปัจจุบันไว้ที่เดียว เพื่อให้ภาพหน้าจอถ่ายซ้ำได้ผลเหมือนเดิม

**ปัญหา** — ภาพหน้าจอในเอกสารถูกถ่ายด้วย golden test แล้วคอมมิตลง git แต่โค้ดที่ผลลัพธ์บนจอ
ขึ้นกับเวลาปัจจุบันเรียก `DateTime.now()` ตรง ๆ ภาพ 4–6 ภาพจึงได้ไฟล์ไม่เหมือนเดิมทุกครั้งที่ถ่าย
(ต่างกันแค่ `· 07:45` เป็น `· 07:54`) เวลาดูประวัติใน git จึงแยกไม่ออกว่าภาพไหนเปลี่ยนเพราะ UI
เปลี่ยนจริง ภาพไหนแค่นาฬิกาเดิน

**ที่เลือก** — เพิ่ม `AppClock` เป็นทางผ่านเดียวของเวลาปัจจุบัน ค่าเริ่มต้นคือนาฬิกาจริง
แต่ `freeze()` ได้ในเทสต์และสคริปต์ถ่ายภาพ แล้วไล่เปลี่ยนจุดที่ผลลัพธ์บนจอขึ้นกับเวลาให้เรียกผ่าน
`AppClock` (demo store, seed ยอดขายย้อนหลัง, `Formatters.elapsed`, ตัวกรองออเดอร์วันนี้,
คำทักทายตามช่วงเวลา, ช่วงเวลาของรายงาน, promotion engine, เวลาเข้าคิวออเดอร์ออฟไลน์, date picker)

สคริปต์ถ่ายภาพตรึงเวลาไว้ที่ **11 ก.ย. 2026 19:42** ก่อน seed ข้อมูลสาธิต และคืนนาฬิกาจริงใน
`tearDownAll`

**ทำไมตรึงเป็นเวลา "ตามเครื่อง" ไม่ใช่ UTC** — `Formatters.parse` แปลง UTC กลับเป็นเวลาเครื่อง
ก่อนแสดงผลอยู่แล้ว พอตรึงเป็นเวลาเครื่อง ค่าที่ round-trip ออกมาจึงเท่ากันทุกเครื่องไม่ว่า time zone
จะตั้งไว้เป็นอะไร ถ้าตรึงเป็น UTC คนที่รันในไทย (UTC+7) จะได้ภาพเลื่อนไป 7 ชั่วโมง

**จุดที่จงใจไม่ตรึง** — id ของคิวออเดอร์ออฟไลน์ยังใช้ `DateTime.now()` เพราะต้องไม่ซ้ำ
ถ้าใช้เวลาที่ตรึงไว้ ทุกรายการที่เข้าคิวจะได้ id เดียวกันหมด (ปิดกฎ lint เป็นบรรทัดพร้อมเหตุผลกำกับ)

**บั๊กที่เจอระหว่างทาง** — ตรึงในแอปจนหมดแล้วจอครัวยังดิ้นอยู่ 2 ภาพ ไล่ดูพบว่าตัวสคริปต์ถ่ายภาพเอง
ยังย้อนเวลาตั๋วด้วยนาฬิกาจริงขณะที่ "ตอนนี้" ถูกตรึงไว้แล้ว ตั๋วทุกใบจึงขึ้น "11 ชม. 56 นาที"
เหมือนกันหมดและติดแถบเตือนของช้าสีแดงหมดทุกใบ — แก้แล้วขึ้น 18 / 4 / 2 นาทีตามที่ตั้งใจไว้

**ผลที่ได้นอกจากความเรียบร้อยของ git** — เมื่อภาพนิ่งแล้ว ภาพที่ขยับใน diff ย่อมแปลว่า UI
เปลี่ยนจริงเสมอ การถ่ายภาพจึงกลายเป็นเครื่องมือตรวจจับการเปลี่ยนแปลงที่ไม่ตั้งใจไปด้วย

**บังคับด้วย lint** — กฎ `use_app_clock_not_date_time_now` ดัก `DateTime.now()` /
`DateTime.timestamp()` ที่เรียกตรง ๆ ครอบคลุมทั้ง `lib/**` และ `tool/screenshots/**`
(ดู `app/tool/lints/README.md`) เพราะบั๊กที่เจ็บที่สุดอยู่ในตัวสคริปต์ถ่ายภาพเอง ไม่ใช่ในโค้ดแอป

---

## 18. โหมดคอนทราสต์สูง — พาเลตต์เดิมที่เข้มขึ้น ไม่ใช่ธีมที่สอง

**ปัญหา** — R-06 จากผลตรวจ UI/UX ค้างมาตั้งแต่รอบที่ 2 แอปล็อกโหมดสว่างอย่างเดียว แต่หน้างานจริง
ของร้านอาหารกินคอนทราสต์ไปอีกชั้นหนึ่งจากที่วัดได้บนจอสะอาดในร่ม: จอครัวโดนไอน้ำ โต๊ะริมหน้าต่าง
โดนแดดตอนกลางวัน และแท็บเล็ตรับออเดอร์มีรอยนิ้วมือตลอดเวลา

**ที่เลือก — คอนทราสต์สูง ไม่ใช่ dark mode** ร้านอาหารได้ประโยชน์จากการอ่านออกกลางแดดมากกว่า
การถนอมสายตาตอนกลางคืน และ dark mode ต้องนิยาม token ใหม่ทั้งหมดพร้อมตรวจคอนทราสต์ใหม่ทุกคู่สี
ขณะที่คอนทราสต์สูงต่อยอดจากพาเลตต์เดิมได้ตรง ๆ

**โครงสีไม่เปลี่ยน เปลี่ยนแค่ความเข้ม** — ส้มยังเป็นแบรนด์ เหลืองยังแปลว่ามีลูกค้า แดงยังแปลว่า
อันตราย พนักงานที่ชินกับสีอยู่แล้วจึงไม่ต้องเรียนรู้ใหม่ และไม่ต้องดูแลภาษาสีสองชุดคู่ขนาน

ตัวเลขด้านล่างเป็น **ค่าต่ำสุดเมื่อเทียบกับทุกพื้นที่ถูกใช้จริง** (surface, background,
surfaceAlt, primarySoft, secondarySoft) ไม่ใช่ค่าบนพื้นขาวอย่างเดียว — ดูหัวข้อ "บั๊กที่เจอ
ระหว่างทำ" ด้านล่างว่าทำไมถึงต้องวัดแบบนี้

| token | ปกติ | คอนทราสต์สูง |
|---|---|---|
| textPrimary | 15.08:1 | 21.00:1 |
| textSecondary | 4.67:1 | 11.48:1 |
| textDisabled | 2.26:1 | 6.92:1 |
| brandInk / warningInk / successInk | 4.62 / 4.95 / 5.11 | 7.45 / 7.75 / 8.42 |
| secondaryInk / dangerInk / purpleInk / infoInk | 4.75 / 4.87 / 6.55 / 5.42 | 7.90 / 7.66 / 9.96 / 8.02 |
| border | 1.24:1 | 4.10:1 |

`textDisabled` จงใจไม่ดันถึง AAA เพราะหน้าที่ของมันคือ "ดูไม่พร้อมใช้งาน" ถ้าเข้มเท่าข้อความ
ปกติจะแยกไม่ออก — WCAG ก็ยกเว้นองค์ประกอบที่ปิดใช้งานไว้อยู่แล้ว

ตัวหนังสือทุกตัวขยับจากเกณฑ์ AA (4.5:1) เป็น AAA (7:1) ส่วนขอบการ์ดผ่านเกณฑ์องค์ประกอบที่ไม่ใช่
ตัวหนังสือ (3:1) ซึ่งโหมดปกติไม่ผ่าน — โหมดปกติตั้งใจให้ขอบจางเพื่อให้การ์ดดูลอย แต่กลางแดดจ้า
ขอบจาง ๆ หายไปเลยจนแยกไม่ออกว่าการ์ดไหนจบตรงไหน

**ที่เก็บสถานะ — เป็น static บน `AppColors` ไม่ได้ส่งผ่าน BuildContext** ทั้งแอปเรียก
`AppColors.x` ตรง ๆ 396 จุดใน 43 ไฟล์ การเปลี่ยนทุกจุดให้อ่านผ่าน `Theme.of(context)` เป็นการรื้อ
ที่ไม่คุ้มกับสิ่งที่ได้ และเสี่ยงพลาดเงียบ ๆ ตรงจุดที่ลืมแก้ วิธีนี้แลกมาด้วยการที่ token ที่เปลี่ยนตามโหมด
ต้องเป็น getter ไม่ใช่ `const` จึงต้องถอด `const` ออกจากจุดที่ใช้ token พวกนี้ 104 จุด — คอมไพเลอร์
ชี้ให้ทุกจุดเอง ไม่มีอะไรพลาดเงียบได้

**กับดักที่เจอตอนทำ** — `AppTheme._textTheme` เป็น `static const` อยู่เดิม พอถอด `const` ออกมันกลาย
เป็น static field ที่คำนวณครั้งเดียวตอนคลาสถูกโหลด สีตัวหนังสือของธีมจึงค้างเป็นของโหมดแรกที่เปิด
แก้โดยเปลี่ยนเป็น getter การสลับโหมดจึงต้องทำสองอย่างคู่กัน: `Get.changeTheme()` สำหรับสีที่ไหล
ผ่านธีม และ `Get.forceAppUpdate()` สำหรับสีที่อ่านจาก `AppColors` ตรง ๆ ซึ่งเป็นส่วนใหญ่ของแอป

**ที่วางปุ่ม — หน้า "บัญชี" ไม่ใช่หน้า "ตั้งค่า"** ด้วยเหตุผลเดียวกับตัวสลับภาษาใน F-07:
คนที่ต้องใช้จริงคือพนักงานครัวกับพนักงานเสิร์ฟ ซึ่งเข้าหน้าตั้งค่าของแอดมินไม่ได้เลย

**จำค่าไว้กับเครื่อง ไม่ใช่กับบัญชีผู้ใช้** เหมือนภาษาและเครื่องพิมพ์ — แท็บเล็ตหน้าร้านที่โดนแดด
ควรตั้งคอนทราสต์สูงค้างไว้ ไม่ว่าใครจะมาล็อกอิน

**บั๊กที่เจอระหว่างทำ — วัดคอนทราสต์บนพื้นขาวอย่างเดียวทำให้รายงานเกินจริง**

เทสต์ชุดแรกเทียบ ink กับ `AppColors.surface` (ขาว) อย่างเดียวจึงผ่านหมด ทั้งที่ของจริงหลายที่
วางบนพื้นเทาอ่อนหรือพื้น soft ของสีตัวเอง ซึ่งกินคอนทราสต์ไปอีก 0.5–1.5 จุด พอแก้เทสต์ให้ไล่
เทียบทุกพื้นก็พบว่า **พาเลตต์เดิมที่เคยประกาศว่า "ผ่าน AA ครบ" จริง ๆ แล้วตก 3 ตัว**:
`textSecondary` (4.31) `warningInk` (4.48) และ `infoInk` (4.48) เมื่อไปวางบน `surfaceAlt`
— ปรับค่าทั้งสามแล้ว และคอมเมนต์เดิมใน `app_colors.dart` ที่อ้างว่าตรวจครบแล้วก็แก้ให้ตรงความจริง

ยังเจออีกชั้นว่า **สีสดที่เอาไปเป็นพื้นปุ่มก็ตกเกณฑ์เหมือนกัน** — ปุ่มเดินสถานะในจอครัวใช้ป้าย
สีขาวบนสีเหลือง "รอทำ" ได้แค่ 2.13:1 และปุ่ม "รับชำระ" สีเขียวได้ 3.45:1 ทั้งที่เป็นปุ่มรับเงิน
แก้โดยใช้เฉด ink เป็นพื้นปุ่มเหมือนที่ทำกับชิป และเพิ่มเทสต์บังคับว่าสีที่เอาไปเป็นพื้นปุ่ม/ชิป
ต้องรองป้ายสีขาวได้ 4.5:1 เสมอ

**ชั้นที่สาม — สีสดถูกใช้เป็นตัวหนังสือ/ไอคอนอยู่ 39 จุดทั่วแอป**

ไล่ตรวจซ้ำอีกรอบด้วยการแยกประเภทการใช้งานทุกจุดที่อ้างถึงสีสด (ตัวหนังสือ / ไอคอน /
ป้ายปุ่ม / พื้น-จุด-ขอบ) แล้ววัดกับเกณฑ์ของแต่ละประเภท (ตัวหนังสือ 4.5:1, ไอคอน 3:1)
พบว่าเมื่อเทียบกับพื้นที่เข้มที่สุดที่ใช้จริง **สีสดไม่มีตัวไหนผ่านเกณฑ์ตัวหนังสือเลย
ยกเว้นม่วง** และสามสี (ส้ม 2.53 / เหลือง 1.90 / เขียวมิ้นต์ 2.28) ไม่ผ่านแม้แต่เกณฑ์ไอคอน

จุดที่เจ็บที่สุดคือหัวคอลัมน์จอครัว ซึ่งวางตัวหนังสือสีสดบนพื้น tint ของสีตัวเอง —
"รอทำ" ได้แค่ **1.94:1** ทั้งที่เป็นป้ายบอกสถานะหลักของจอที่ครัวต้องอ่านตลอดกะ
แปลงทั้ง 39 จุดเป็นเฉด ink แล้ว ส่วนพื้น/จุด/ขอบยังใช้สีสดตามเดิมเพราะถูกต้องอยู่แล้ว

ผลพลอยได้: เฉด ink เป็น getter ที่เข้มขึ้นตามโหมดคอนทราสต์สูง ขณะที่สีสดเป็น `const`
จุดเหล่านี้จึงเพิ่งจะตอบสนองต่อโหมดคอนทราสต์สูงจริง ๆ เป็นครั้งแรก

**ทางเลือกสุดท้าย — พื้นสีสด + ป้ายสีเข้ม แทนพื้นเฉด ink + ป้ายสีขาว**

ตอนแรกแก้ปุ่ม/ชิปด้วยการเปลี่ยน "พื้น" เป็นเฉด ink แล้วคงป้ายสีขาวไว้ ซึ่งผ่านเกณฑ์จริง
แต่ทำให้จอครัวทึบลงจากเหลืองอำพันสดเป็นน้ำตาล ซึ่งเสียประโยชน์หลักของ KDS ไป —
ครัวอาศัย "ความสดของสี" ในการกวาดสายตาหาตั๋วที่ต้องทำจากระยะไกล ไม่ได้อ่านทีละใบ

จึงเปลี่ยนมาใช้คู่ที่ได้ทั้งสองอย่าง: **คงพื้นสีสดไว้ แล้วเลือกสีป้ายตามพื้น**

| พื้น | ป้ายขาว | ป้ายเข้ม | เลือก | ผล |
|---|---|---|---|---|
| เหลือง (รอทำ) | 2.13 | **7.93** | เข้ม | AAA |
| ส้ม (กำลังทำ) | 2.84 | **5.95** | เข้ม | AA |
| เขียว (พร้อมเสิร์ฟ) | 3.45 | **4.91** | เข้ม | AA |
| เขียวมิ้นต์ | 2.55 | **6.63** | เข้ม | AA |
| แดง | **4.51** | 3.75 | ขาว | AA |
| น้ำเงิน | **5.02** | 3.37 | ขาว | AA |
| ม่วง | **5.55** | 3.05 | ขาว | AA |

ทำเป็น helper สองตัวแทนการฮาร์ดโค้ดทีละจุด:

- `AppColors.fillOf(base)` — คืนสีสดในโหมดปกติ และเฉด ink ในโหมดคอนทราสต์สูง
  (โหมดคอนทราสต์สูงต้องการ AAA ซึ่งสีสดทำไม่ได้ทุกตัว — เขียวได้แค่ 4.91)
- `AppColors.onColor(background)` — เลือกป้าย "เข้ม" หรือ "ขาว" จากสีพื้นจริง
  โดยคำนวณคอนทราสต์ทั้งสองแบบแล้วเลือกตัวที่ดีกว่า ไม่ใช่จำว่าสีไหนใช้อะไร

ข้อดีของการคำนวณแทนการจำ: เพิ่มสีใหม่เข้าพาเลตต์แล้วป้ายถูกต้องเองทันที
และเทสต์บังคับได้ว่าทุกสีที่เอาไปเป็นพื้นต้องมีป้ายที่อ่านออก

**บั๊กที่เทสต์ชุดนี้จับได้ทันที** — `inkOf()` ไม่เคยแมป `info` ไว้เลย เพราะตอนเขียน
`infoInk` มีค่าเท่ากับ `info` เป๊ะ ๆ (เขียนไปก็ไม่มีผล) พอ `infoInk` แยกเป็นเฉดของตัวเอง
การลืมบรรทัดนั้นทำให้ `inkOf(info)` คืนสีเดิมกลับไปเงียบ ๆ ชิปและปุ่มสีน้ำเงินจึงไม่เข้มขึ้น
ตามโหมดคอนทราสต์สูงเลย — เป็นบั๊กที่มองด้วยตาไม่มีทางเห็น

**บังคับด้วย lint** — กฎ `use_ink_color_for_foreground` ดักการเอาสีสดไปใส่
`TextStyle(color:)` / `Icon(color:)` / `styleFrom(foregroundColor:)` บั๊กชุดนี้เกิดซ้ำ
สามรอบและทุกรอบหลุดสายตาไปได้ เพราะ "ดูแล้วก็อ่านออกอยู่" บนจอสะอาดในร่ม
รอบที่สามเจอเพราะถูกถามย้ำว่า "แก้หมดแล้วหรือยัง" ไม่ใช่เพราะมีอะไรฟ้อง

**ข้อเสียที่ยอมรับ** — โหมดคอนทราสต์สูงหน้าตาแข็งกว่าและ "สวย" น้อยกว่าโหมดปกติอย่างเห็นได้ชัด
เพราะขอบเข้มทำให้การ์ดดูเป็นกล่องมากกว่าลอย และปุ่ม/ชิปเปลี่ยนเป็นเฉด ink คู่กับป้ายสีขาว
(ต่างจากโหมดปกติที่คงสีสดไว้) ซึ่งเป็นการแลกที่ตั้งใจ — โหมดนี้เลือกอ่านออกกลางแดดก่อนความสด และยังไม่มีโหมดกลางคืนจริง ๆ
สำหรับร้านที่เปิดดึก ถ้าต้องการควรทำเป็น ticket แยกที่ตัดสินใจ palette ชุดที่สามโดยเฉพาะ

---

## 19. ใบกำกับภาษี — เลขที่รันรีเซ็ตทุกปี พ.ศ., ออกได้ 1 ใบต่อออเดอร์ที่จ่ายแล้ว, รองรับยกเลิก/ออกใหม่, snapshot ข้อมูลทั้งหมด

**ปัญหา** — ต้องออกใบกำกับภาษีตามข้อกำหนดกรมสรรพากร (มาตรา 86/4 เต็มรูป, มาตรา 86/6 อย่างย่อ)
โดยกฎหมายบังคับเรื่อง running number ต้องต่อเนื่องไม่ซ้ำ/ไม่ข้าม แต่ ticket ไม่ได้ระบุรายละเอียด
implementation ไว้ ต้องตัดสินใจเพิ่มหลายจุด (นโยบาย reset เลขที่, จุดที่ออกได้, การแก้ไข/ยกเลิกใบที่
ออกผิด, ความบังคับของเลขผู้เสียภาษีลูกค้า) — ตัดสินใจร่วมกับผู้ใช้ผ่านคำถามเลือกทางเลือกก่อนเริ่มเขียนโค้ด

**ที่เลือก**

- **รูปแบบเลขที่รัน `INV<ปี พ.ศ. 2 หลัก>-<เลขรัน 6 หลัก>` รีเซ็ตทุกปี พ.ศ.** (เช่น `INV69-000001`)
  คำนวณจาก `COUNT(*) WHERE running_number LIKE 'INV{yy}-%'` (นับรวมใบที่ถูกยกเลิกด้วย เพื่อไม่ให้เลข
  ถูกใช้ซ้ำ) — กฎหมายไทยอนุญาตให้แบ่งเลขที่รันเป็นหมวดตามช่วงเวลาได้ ตราบใดที่แต่ละหมวดยังต่อเนื่อง
  ไม่ซ้ำ/ไม่ข้าม จึงออกแบบให้ต่อเนื่องเฉพาะภายในปี พ.ศ. เดียวกัน (ไม่ต้องมีเลขยาวขึ้นเรื่อย ๆ ไม่มีที่สิ้นสุด)
- **ออกได้ 1 ใบต่อออเดอร์ เมื่อจ่ายครบแล้วเท่านั้น** (`order.status === 'paid'`) — บังคับด้วย partial
  unique index `CREATE UNIQUE INDEX ... ON tax_invoices(order_id) WHERE voided_at IS NULL` ทำให้มีใบ
  ที่ "ยังใช้งานอยู่" ได้สูงสุด 1 ใบต่อออเดอร์ในเวลาเดียวกัน โดยไม่ต้อง lock ระดับแอปพลิเคชันเพิ่ม เพราะ
  `better-sqlite3` เป็น synchronous และ Node.js เป็น single-threaded event loop จึงไม่มีการแทรกคิวขณะที่
  request handler เดียวกันยังทำงานไม่จบ
- **รองรับยกเลิก (void) ใบที่ออกผิดแล้วออกใหม่ได้** — เลือกทางนี้แทนทางที่แนะนำ (ออกครั้งเดียวแก้ไข/
  ยกเลิกไม่ได้เลย) เพราะร้านจริงพิมพ์ผิดประเภท/ข้อมูลลูกค้าผิดได้บ่อย การไม่มีทางแก้เลยจะสร้างภาระ
  บันทึกนอกระบบแทน — ยกเลิกเป็นการ "เติม" ไม่ใช่ "ลบ" (ตั้ง `voided_at`/`void_reason`/`voided_by` ไว้
  ถาวร ไม่ลบแถวทิ้งและไม่ใช้เลขที่เดิมซ้ำ) เก็บประวัติใบที่ถูกยกเลิกไว้ครบเพื่อการตรวจสอบ จำกัดสิทธิ์
  ยกเลิกไว้ที่ผู้จัดการขึ้นไปเท่านั้น (RBAC ระดับเดียวกับ refund)
- **เลขผู้เสียภาษีลูกค้า (`customerTaxId`) ไม่บังคับแม้ใบกำกับภาษีเต็มรูป** — บังคับแค่ชื่อและที่อยู่
  ลูกค้า สอดคล้องกับมาตรา 86/4 ที่ไม่ได้บังคับต้องมีเลขผู้เสียภาษีของผู้ซื้อ (ต่างจากอย่างย่อที่ไม่ต้อง
  มีข้อมูลลูกค้าเลย)
- **Snapshot ข้อมูลร้าน + ยอดเงินทั้งหมดไว้ที่ตัวเอกสารตอนออก** (`store_name`, `store_tax_id`,
  `store_address`, `store_branch`, `subtotal`, `vat`, `total`) แทนที่จะ join กับ `settings`/`orders`
  ตอนแสดงผล — ใช้แนวทางเดียวกับ snapshot โปรโมชันในข้อ 14 เพื่อให้เอกสารที่ออกไปแล้วไม่เปลี่ยนแปลง
  แม้ตั้งค่าร้านหรือ (ในทางทฤษฎี) ข้อมูลออเดอร์จะถูกแก้ภายหลัง
- **ฟิลด์ตั้งค่าร้าน (`storeTaxId`/`storeAddress`/`storeBranch`) ไม่บังคับกรอก** — ร้านที่ไม่ได้จด
  ทะเบียน VAT ไม่จำเป็นต้องมีเลขผู้เสียภาษี ระบบจะปฏิเสธการออกใบกำกับภาษี (HTTP 400) ก็ต่อเมื่อร้าน
  ยังไม่ได้ตั้งค่าเหล่านี้ไว้ แทนที่จะบังคับกรอกตั้งแต่ตอนติดตั้งระบบ

**ข้อเสียที่ยอมรับ** — เลขที่รันรีเซ็ตทุกปีทำให้เทียบเลขที่ข้ามปีไม่ได้โดยตรง (ต้องดูปี พ.ศ. ในเลขที่
ประกอบด้วย) และยังไม่รองรับ e-Tax invoice (ยื่นอิเล็กทรอนิกส์ต่อกรมสรรพากรโดยตรง) ตามที่ระบุไว้ใน
ticket ว่าจะเลื่อนไปเฟสถัดไป — เฟสนี้ออกได้แค่เอกสารที่ข้อมูลครบตามกฎหมาย ยังไม่เชื่อมต่อระบบยื่นจริง

---

## 20. Security hardening batch — fail-closed แทนการเตือนเฉยๆ ในทุกจุดที่เคยเจาะช่องโหว่

**ปัญหา** — ทำ security review เต็มรูปแบบ (OWASP Top 10) พบช่องโหว่ 7 จุด ตั้งแต่ High ถึง Low
ครอบคลุม broken access control, hardcoded secret, broken authentication และ security
misconfiguration — ต้องตัดสินใจว่าแต่ละจุดจะแก้แบบไหนโดยไม่กระทบ flow การใช้งานปกติของ
เดโม/ร้านที่ยังไม่ deploy จริง

**ที่เลือก** — ยึดหลักเดียวกันทุกจุด: **fail-closed แทนเตือนเฉยๆ**, ค่า default ที่ปลอดภัยสำหรับ
เดโม/dev ต้องไม่เปลี่ยนพฤติกรรม ส่วน production ต้องบังคับให้ตั้งค่าเองก่อนเสมอ

- **`JWT_SECRET` ไม่มี fallback ในโค้ดอีกต่อไป** (`config/env.js`) — เดิม fallback ไปใช้ค่า
  hardcode ที่เห็นได้จากซอร์สสาธารณะ ถ้าใครลืมตั้ง env ตัวนี้เท่ากับเปิดช่องให้ปลอม JWT สิทธิ์ admin
  ได้ทันที เปลี่ยนเป็น throw ตอน startup ถ้าไม่มีค่า
- **RBAC ของโมดูล users เพิ่มการเช็คลำดับสิทธิ์** (`user.service.js`) — เดิมมีแค่
  `authorize('admin','manager')` ระดับ route ทำให้ manager ตั้งรหัสผ่านใหม่ให้บัญชี admin หรือ
  เลื่อน role ตัวเองเป็น admin ได้ตรงๆ เพิ่ม `assertAdminBoundary()` บังคับว่าเฉพาะ admin เท่านั้น
  ที่แตะบัญชีระดับ admin ได้ (สร้าง/แก้ไข/รีเซ็ตรหัสผ่าน)
- **`authenticate()` เช็คสถานะจาก DB ทุก request แทนการเชื่อ JWT payload เก่า** (`middlewares/
  auth.js`) — เดิม role/is_active ที่ฝังใน token มีผลจนกว่า token จะหมดอายุเอง (สูงสุด 12 ชม.)
  แม้บัญชีจะถูกปิดใช้งาน/เปลี่ยน role ไปแล้ว เปลี่ยนเป็น query `userRepository.findById()` ทุก
  request แลกกับ DB read เพิ่ม 1 ครั้งต่อ request ซึ่งรับได้สำหรับสเกลร้านอาหารสาขาเดียว (ดูข้อ 6)
- **`DemoAccountPicker` (Flutter) gate ด้วย `AppConfig.demoMode`** (`login_page.dart`) — เดิม
  render ปุ่มกรอกบัญชีเดโมอัตโนมัติ (username/password ตรงกับ seed จริง) ในทุก build ไม่ใช่แค่
  demo build ย้ายเข้าไปอยู่ใน gate เดียวกับ banner
- **`CORS_ORIGIN` default เป็น string `'*'` ตรงๆ ไม่ผ่าน `.split(',')`** (`config/env.js`) — เดิม
  กลายเป็น array `['*']` ซึ่ง `cors` package เทียบแบบ exact-string จึงไม่ match origin จริงเลย
  (ปิดกั้น cross-origin ทั้งหมดโดยไม่ตั้งใจ) และปิด `credentials: true` เมื่อใช้ wildcard เพราะสอง
  อย่างนี้ใช้คู่กันไม่ได้ตามสเปก CORS/Fetch อยู่แล้ว
- **CSP เปิดไว้ทุกที่ยกเว้น `/docs`** (`app.js`) — เดิมปิด CSP ทั้งแอปเพราะ Swagger UI ต้องใช้
  inline script/style ทั้งที่ endpoint อื่นเป็น JSON ล้วน เปลี่ยนเป็นเช็ค path ต่อ request แทน
- **Seed บัญชีเดโมด้วยรหัสผ่านที่รู้อยู่แล้วไม่ได้อีกต่อไปถ้า `NODE_ENV=production`**
  (`db/seed.js`) — รหัสผ่านเดโม (`admin123` ฯลฯ) เผยแพร่อยู่ใน README/ซอร์สโค้ดสาธารณะ เดิม seed
  ทุกครั้งไม่ว่าโหมดไหน ทำให้ระบบที่ deploy จริงโดยไม่ได้อ่าน `SECURITY.md` ยังมีบัญชี admin ที่
  เดารหัสผ่านได้จากซอร์สโค้ดตรงๆ เปลี่ยนเป็นบังคับตั้งค่า `SEED_ADMIN_PASSWORD` (และอีก 5 ตัวต่อ
  บัญชี) ผ่าน env ก่อนเสมอเมื่อ production ไม่งั้น throw — ปิด `AUTO_SEED` แล้วสร้างบัญชีจริงเองก็
  ยังทำได้เหมือนเดิม (ตัวเลือกที่สองตามที่ `SECURITY.md` เคยแนะนำไว้)

**เหตุผลที่เลือก fail-closed แทนแค่ document/เตือน** — checklist ใน `SECURITY.md` เตือนเรื่องนี้
มาตั้งแต่แรกอยู่แล้ว แต่เป็นแค่คำแนะนำที่ต้องมีคนอ่านและทำตามเอง ไม่มีอะไรบังคับจริง — ถ้าจะให้
มั่นใจว่า deploy จริงจะไม่หลุดช่องโหว่ที่รู้อยู่แล้วว่าเป็นปัญหา ต้องทำให้ระบบปฏิเสธการรันแบบไม่
ปลอดภัยเอง ไม่ใช่หวังว่าจะมีคนอ่านเอกสารก่อน deploy ทุกครั้ง

**ข้อเสียที่ยอมรับ** — `authenticate()` มี DB read เพิ่มทุก request (ยอมรับได้ที่สเกลนี้ ดูข้อ 6)
และผู้ที่จะ deploy จริงต้องตั้งค่า env เพิ่มอีก 6 ตัวก่อน (หรือปิด `AUTO_SEED`) จะรันเปล่าๆ ด้วย
`NODE_ENV=production` โดยไม่ตั้งค่าอะไรเลยไม่ได้อีกต่อไป — เป็นการแลกที่ตั้งใจ เพราะ default เดิม
(รันได้เงียบๆ ด้วยรหัสผ่านที่รู้อยู่แล้ว) อันตรายกว่า

## 21. Audit log — snapshot ชื่อผู้ทำ, log อยู่ในทรานแซกชันเดียวกับการเปลี่ยนแปลงจริง, ขยาย scope เกินรายการขั้นต่ำในทิกเก็ต

**ปัญหา** — ไม่มี log ว่าใครยกเลิกออเดอร์/รายการ ให้ส่วนลดพิเศษ ปิดใช้งาน/ลบพนักงาน แก้ค่า VAT
หรือคืนเงินเมื่อไหร่และทำไม เปิดช่องให้พนักงานทุจริตหน้าร้าน (เช่น cashier ยกเลิกบิลหลังรับเงินสด
แล้วเก็บเงินเอง) โดยไม่มีทางตรวจสอบย้อนหลัง (ดู `docs/tickets/08-audit-log.md`)

**ที่เลือก**

- **ตาราง `audit_logs` แบบ append-only** — ไม่มี endpoint แก้ไข/ลบเปิดให้ใช้เลยทั้งระบบ (route
  เดียวคือ `GET /audit-logs` สำหรับ admin) `actor_user_id` เป็น `ON DELETE SET NULL` (ไม่ใช่
  `RESTRICT`) พร้อม snapshot `actor_name` ตรงๆ ในแถว — mirror ของรูปแบบเดียวกับ
  `promotion_name_snapshot`/`tax_invoices` (ดูข้อ 14/19) เพื่อให้ log ยังอ่านได้ครบแม้ผู้ใช้คนนั้น
  ถูกลบภายหลัง และลบ user ไม่ถูกบล็อกเพราะเคยมี log อ้างถึง
- **`auditLogService.log()` ถูกเรียกจากภายในทรานแซกชันเดียวกับการเขียนข้อมูลจริงเสมอ** (ไม่ใช่
  เรียกแยกหลัง commit) — ถ้าบันทึก log ไม่สำเร็จ การกระทำนั้นต้องล้มเหลวไปด้วยทั้งคู่ (rollback
  พร้อมกัน) แทนที่จะปล่อยให้การกระทำสำเร็จแต่ log หาย ใช้ `getDb().transaction()` แบบเดียวกับที่ใช้
  อยู่แล้วในทุก service (`order.service.js`, `user.service.js`, `settings.service.js`,
  `payment.service.js`, `tax-invoice.service.js`) จึงไม่ต้องเพิ่ม pattern ใหม่
- **log เฉพาะการกระทำที่ "เสี่ยงจริง" ไม่ log ทุกการแก้ไข** — ตัวอย่างที่ตั้งใจไม่ log: ยกเลิกรายการ
  อาหารตอนยัง `pending` (ยังไม่ส่งครัว ยกเลิกเองได้ปกติ ไม่ต้องขอสิทธิ์พิเศษ), แก้ชื่อพนักงานเฉยๆ,
  แก้ค่าตั้งค่าร้านที่ไม่ใช่ VAT/service charge (เช่น ชื่อร้าน/ที่อยู่) — ถ้า log ทุกอย่างจะกลืน
  สัญญาณของการกระทำที่เสี่ยงจริงจนหาไม่เจอ
- **ขยาย scope เกินรายการขั้นต่ำในทิกเก็ตอีก 3 action** เพราะเป็นการกระทำที่เสี่ยงต่อการทุจริต/
  privilege escalation แบบเดียวกัน และเพิ่งแก้ช่องโหว่ที่เกี่ยวข้องไปในข้อ 20:
  `user.role_change` (เปลี่ยนสิทธิ์พนักงาน), `user.password_reset` (ตั้งรหัสผ่านใหม่ให้พนักงาน),
  และ `tax_invoice.void` (ยกเลิกใบกำกับภาษี — เอกสารทางกฎหมายที่ยกเลิกแล้วกระทบยอดขาย)
- **Demo Mode mirror เต็มรูปแบบ** (`demo_store_audit_logs.dart` + `demo_store_settings.dart`
  ใหม่, wiring เข้า orders/auth/refunds/tax_invoices) — action/entityType/summary ตรงกับ backend
  ทุกจุด เพื่อให้หน้าจอ admin ในโหมดเดโมเห็นพฤติกรรมเดียวกับของจริง

**Frontend** — หน้า **ประวัติการทำรายการ** เห็นเฉพาะ `admin` เท่านั้น (ไม่รวม `manager` —
mirror ของ `authorize('admin')` ที่ route) filter ตาม action type ได้ด้วยชิปเลือก อ่านอย่างเดียว
ไม่มีปุ่มแก้ไข/ลบใดๆ ตาม acceptance criteria "append-only"

**ข้อเสียที่ยอมรับ** — `metadata_json`/`metadata` เป็น JSON แบบอิสระไม่มี schema บังคับต่อ action
(เทรดออฟความเร็วในการเพิ่ม action ใหม่ กับความปลอดภัยของ type) และหน้าจอ admin ยังไม่มี filter
ตามช่วงเวลาแบบ date picker (query parameter `dateFrom`/`dateTo` มีพร้อมใช้ที่ backend/repository
แล้ว แค่ยังไม่ต่อ UI) — เพิ่มทีหลังได้โดยไม่กระทบโครงสร้างเดิม

---

## 22. ลูกค้า/แต้มสะสม — แยก "ยอดที่นับเข้าออเดอร์" กับ "ยอดที่เก็บเงินจริง" เพื่อไม่ให้การใช้แต้มกระทบสถานะจ่ายครบ, ปฏิเสธแทนการปัดเศษ, reuse endpoint รายการออเดอร์เดิมสำหรับหน้าประวัติ

**ปัญหา** — ไม่มี table ลูกค้าในระบบ ผูกประวัติการซื้อกับลูกค้ารายบุคคลหรือทำระบบสมาชิก/แต้มสะสม
ไม่ได้ ซึ่งเป็นเครื่องมือการตลาดที่ช่วย retention (ดู `docs/tickets/09-customer-loyalty.md`)

**ที่เลือก**

- **`customers.phone` เป็น unique key หลักในการค้นหา/ผูกลูกค้า** — เบอร์โทรเป็นสิ่งที่ลูกค้าจำได้
  และพนักงานถามได้เร็วที่สุดตอนหน้าร้าน ไม่ใช้อีเมล (มักไม่มีตอนสั่งอาหาร) หรือเลขบัตรประชาชน
  (ละเอียดอ่อนเกินความจำเป็นของ POS ร้านอาหาร) `POST /customers` ปฏิเสธเบอร์ซ้ำด้วย 409
  ส่วน `orders.customer_id` เป็น nullable FK (`ON DELETE SET NULL`) — ลูกค้าทั่วไปไม่ต้องผูกก็สั่ง
  อาหารได้ปกติ ตรงตาม acceptance criteria "แบบ optional"
- **แยก `amount` (ยอดที่นับเข้าบัญชีจ่ายของออเดอร์) ออกจาก `chargedAmount` (ยอดที่ต้องเก็บเงินจริง
  ผ่านช่องทางที่เลือก) เวลาใช้แต้มแลกส่วนลด** — เก็บแค่ `pointsRedeemedValue` แล้วลบออกจาก
  `chargedAmount` เท่านั้น ส่วน `amount`/`alreadyPaid`/`isFullyPaid` ของออเดอร์ไม่ถูกแตะเลย
  เหตุผล: ถ้าลด `amount` ตรงๆ ตามมูลค่าแต้ม ออเดอร์ที่ลูกค้าใช้แต้มจนเกือบครอบคลุมทั้งบิลจะไม่มีวัน
  ขึ้นสถานะ "จ่ายครบ" เพราะยอดจ่ายสะสมนับได้น้อยกว่า `order.total` เสมอ ทั้งที่ร้านได้รับเงิน+แต้มที่
  หักไปครบตามมูลค่าจริงแล้ว การแยกสองยอดนี้ทำให้บัญชีของออเดอร์ถูกต้องเสมอไม่ว่าจะจ่ายด้วยเงินสด/
  QR ผสมแต้มกี่ครั้งก็ตาม (`payment.service.js#pay`, mirror ที่ `demo_store_payments.dart`)
- **แต้มที่ใช้แลกมีมูลค่าเกินยอดที่ต้องชำระรอบนี้ → ปฏิเสธ (400) ไม่ใช่ปัดลดให้อัตโนมัติ** — เลือก
  แบบเดียวกับแนวทาง error-first ที่ใช้ทั้งโปรเจกต์ (เช่นข้อ 20) เพราะการปัดลดเงียบๆ ทำให้แคชเชียร์
  ไม่รู้ตัวว่าจำนวนแต้มที่กดจริงกับที่ถูกหักไม่ตรงกัน ตรวจสอบและเขียนเทสต์ยากกว่าการปฏิเสธตรงๆ
- **สะสมแต้มคำนวณจาก `order.total` (ยอดเต็มของบิล) ไม่ใช่ยอดหลังหักแต้มที่แลกในรอบเดียวกัน** และ
  ให้ **ครั้งเดียวตอนออเดอร์เปลี่ยนเป็นจ่ายครบเท่านั้น** (ไม่ใช่ทุกครั้งที่มี payment record ใหม่) —
  กันแต้มพอกซ้ำตอนแยกจ่ายหลายรอบ (split payment) ต่อออเดอร์เดียว เก็บผลไว้ที่ `orders.points_earned`
  ครั้งเดียวเพื่อให้หน้าประวัติ/ใบเสร็จ query ย้อนกลับได้โดยไม่ต้องคำนวณซ้ำ
- **อัตราแลกแต้มตั้งค่าได้ที่ `settings`** (`pointsEarnRateBaht` จ่ายกี่บาทได้ 1 แต้ม,
  `pointsRedeemValueBaht` มูลค่า 1 แต้มตอนแลก) mirror รูปแบบเดียวกับ VAT/service charge ที่มีอยู่
  แล้ว — ไม่ log เข้า audit log ตอนเปลี่ยนอัตรา เพราะสอดคล้องกับขอบเขตเดิมในข้อ 21 ที่ log เฉพาะ
  VAT/service charge เท่านั้น ไม่ใช่ทุกฟิลด์ใน settings
- **หน้า "ประวัติการซื้อของลูกค้า" (admin/manager) reuse `GET /orders?customerId=` เดิม** แทนการทำ
  endpoint แยก (เช่น `/customers/:id/orders`) — ลดโค้ดซ้ำและได้ pagination/filter ที่ทดสอบแล้วมาฟรี
  โดยไม่ต้องเขียน endpoint ใหม่สำหรับ use case ที่เหมือนกันแค่กรองคนละมุม
- **ผูกลูกค้ากับออเดอร์ได้เฉพาะตอนเปิดออเดอร์ใหม่เท่านั้น** (ไม่รองรับตอนสั่งเพิ่ม/`POST
  /orders/:id/items`) — backend/ก็ไม่รับ `customerId` ในเส้นทางนั้นอยู่แล้ว เพราะการผูกลูกค้าย้อนหลัง
  หลังออเดอร์เปิดไปแล้วไม่ใช่ use case ที่ทิกเก็ตต้องการ (ค้นหา/ผูกตอนเปิดออเดอร์หรือตอนชำระเงิน)
  และเลี่ยงความกำกวมว่าแต้มสะสม/ประวัติของออเดอร์ที่เพิ่งผูกลูกค้าไปครึ่งทางควรนับยังไง

**Frontend** — `CustomerPickerDialog` (ค้นหา/สร้างลูกค้าใหม่) เรียก usecase ตรงผ่าน `Get.find`
แทนที่จะมี controller+binding แยก เพราะเป็นการดึงข้อมูลสนับสนุนแบบอ่านอย่างเดียว (mirror รูปแบบ
เดียวกับ `menu_form_page.dart`/`order_detail_page.dart`) เปิดจากแถบในตะกร้าตอนรับออเดอร์ ส่วน
หน้าชำระเงินโหลดข้อมูลลูกค้าเต็ม (รวมแต้มคงเหลือ) แยกจาก order object เพราะ `Order` เก็บแค่
`customerId`/`customerName`/`customerPhone` ไม่มี `pointsBalance` — ปุ่ม "ใช้สูงสุด" และตัวเลข
แต้มถูกจำกัดด้วย `maxRedeemablePoints` (min ของแต้มคงเหลือกับมูลค่าไม่เกินยอดจ่ายรอบนี้) ทุกครั้งที่
ยอดจ่ายเปลี่ยน กันผู้ใช้กดเกินโดยไม่ต้องรอ error จาก backend

**ข้อเสียที่ยอมรับ** — หน้าแยกบิลรายการอาหาร (split bill) ยังไม่มี UI ให้ใช้แต้มแลกส่วนลด (โครง
`pointsToRedeem` มีพร้อมที่ usecase/repository แล้ว แค่ยังไม่ต่อ UI) เพราะไม่ได้อยู่ใน acceptance
criteria ของทิกเก็ตนี้ — เพิ่มทีหลังได้โดยไม่กระทบโครงสร้างเดิม

---

## 23. Takeaway/Delivery — เลขคิวเฉพาะ takeaway ไม่รวม delivery, เพิ่มปุ่มเปิดออเดอร์ตรงแยกจากผังโต๊ะ, เชื่อม platform เดลิเวอรีตั้งใจไม่ทำ

**ปัญหา** — schema มี `orders.type: dine_in|takeaway|delivery` อยู่แล้ว แต่ UI/business logic
ทั้งหมดออกแบบมาสำหรับ dine-in เป็นหลัก (ผูกกับผังโต๊ะ) ไม่มีทางเปิดออเดอร์ takeaway/delivery ได้
เลยนอกจากยิง API ตรง ๆ และไม่มีเลขคิวรับอาหารให้เรียกลูกค้า (ดู
`docs/tickets/10-takeaway-delivery-flow.md`)

**ที่เลือก**

- **เลขคิวรับอาหาร (`orders.queue_number`) มีเฉพาะ `type = 'takeaway'` เท่านั้น ไม่รวม
  `delivery`** — คิวเลขเรียกมีไว้ให้ "ลูกค้ายืนรอหน้าร้าน" มารับอาหารเอง ส่วนออเดอร์เดลิเวอรีไม่มี
  ลูกค้ามายืนรอ ไรเดอร์จะอ้างอิงออเดอร์จาก `code` (เช่นที่ระบบขนส่งเดลิเวอรีจริงทำกัน) แยกแนวคิด
  ทั้งสองออกจากกันชัดเจนแทนที่จะให้เลขคิวความหมายกำกวมสำหรับทั้งสองประเภท
- **เลขคิวรันต่อวันแยกเป็นตัวนับของตัวเอง ไม่ใช้ตัวเดียวกับ `code`** — `code` เป็นเลขอ้างอิงของ
  ระบบ (ไม่มีใครอยากพูดว่า "ขอรับออเดอร์ ORD-20260915-0007") ส่วนเลขคิวต้องเป็นเลขสั้น ๆ ที่พนักงาน
  เรียกออกเสียงได้ง่าย (`nextQueueNumber()` นับเฉพาะออเดอร์ takeaway ของวันนั้น แยกจาก
  `nextCode()` โดยสิ้นเชิง — ดู `order.repository.js`)
- **เพิ่มปุ่ม "สั่งกลับบ้าน/เดลิเวอรี่" ที่หน้าผังโต๊ะ แยกจากการแตะโต๊ะ** — ก่อนหน้านี้ทาง UI ไม่มี
  ทางเปิดออเดอร์ที่ไม่ผูกโต๊ะได้เลย ทั้งที่ backend/`CartController` รองรับอยู่แล้วบางส่วน (ตอนแตะ
  โต๊ะไม่ได้ ระบบ default `orderType` เป็น takeaway ถ้าไม่มี tableId/existingOrderId — แต่บั๊กเดิม
  คือเช็คเงื่อนไขนี้อยู่ใน `if (args is Map)` เท่านั้น ถ้านำทางมาโดยไม่มี arguments เลยจะไม่ตกเป็น
  ค่าเริ่มต้นนี้ ยังคงเป็น `dine_in` แล้วโดน backend ปฏิเสธ 422 เพราะไม่มี tableId — แก้โดยย้าย
  เงื่อนไขออกมานอก `if` ให้เช็คเสมอไม่ว่าจะมี arguments หรือไม่)
- **ครัว (KDS) แยกป้าย/ไอคอนทั้ง 3 ประเภทชัดเจน** (โต๊ะ+ชื่อโต๊ะ / ถุงกลับบ้าน+"กลับบ้าน" /
  มอเตอร์ไซค์+"เดลิเวอรี") แทนที่ตรรกะเดิมที่เดาจาก `tableName != null` อย่างเดียว (ทำให้ delivery
  ถูกจัดเป็น takeaway ผิดๆ เพราะทั้งคู่ไม่มีโต๊ะเหมือนกัน) เปลี่ยนไปอ่านค่า `item.orderType` ตรง ๆ
  ซึ่ง backend ส่งมาให้อยู่แล้วในคิวครัว (`findItemsByStatuses` join `o.type` มาด้วยตั้งแต่ก่อน
  ทิกเก็ตนี้) — ไม่ได้แสดงเลขคิวบนตั๋วครัว เพราะพนักงานครัวไม่ต้องรู้เลขคิว (เอาไว้เรียกลูกค้าหน้าร้าน
  เป็นหน้าที่ของพนักงานเสิร์ฟ/แคชเชียร์)
- **ไม่เพิ่ม endpoint/logic ใหม่ให้ cashier เช็คบิล takeaway** — ตรวจสอบแล้วว่าหน้าเก็บเงิน/ใบเสร็จ
  ทั้งหมด (`checkout_page.dart`, `checkout_controller.dart`, `receipt_page.dart`) ไม่มีจุดใดอ้างอิง
  โต๊ะเลยตั้งแต่ก่อนทิกเก็ตนี้ (`order.displayTarget` แสดงชื่อประเภทออเดอร์แทนชื่อโต๊ะอยู่แล้วเมื่อไม่มี
  โต๊ะ) acceptance criteria ข้อนี้จึงผ่านอยู่แล้วโดยธรรมชาติของสถาปัตยกรรมเดิม แค่ต้องตรวจสอบซ้ำว่า
  ไม่มี regression ไม่ต้องเพิ่มโค้ดใหม่
- **ตั้งใจไม่เชื่อมต่อแพลตฟอร์มเดลิเวอรี (Grab/LINE MAN)** ตามที่ทิกเก็ตแนะนำเองว่าควรแยกเป็น
  ทิกเก็ตย่อยต่างหาก เพราะ scope ใหญ่และขึ้นกับ external API ของแต่ละเจ้าที่ยังไม่รู้ว่าจะเชื่อมกับ
  เจ้าไหนจริง (รูปแบบเดียวกับที่ตั้งใจ scope สาขาเดียวไว้ก่อนในข้อ 11) — ออเดอร์ที่รับจากแพลตฟอร์ม
  เดลิเวอรีตอนนี้ยังต้องคีย์เข้าระบบเองผ่าน type=delivery เหมือนเดิม

**พบระหว่างทำ (ไม่เกี่ยวกับทิกเก็ตนี้โดยตรง แต่แก้ไปด้วยเพราะแตะไฟล์เดียวกัน)** — `migrate.js`
ขาดรายการ `addColumnIfMissing` ของคอลัมน์ที่ทิกเก็ต 09 เพิ่มเข้า `orders`/`payments`
(`customer_id`, `points_earned`, `points_redeemed`, `points_redeemed_value`) ทำให้ฐานข้อมูลที่มี
อยู่แล้วตั้งแต่ก่อนทิกเก็ต 09 จะพังทันทีตอนอัปเกรด (query ที่อ้างถึงคอลัมน์เหล่านี้จะ error
"no such column") เพราะ `CREATE TABLE IF NOT EXISTS` ไม่แก้ตารางเดิมที่มีอยู่แล้วให้อัตโนมัติ —
เติมรายการที่ขาดให้ครบพร้อมกับเพิ่ม `queue_number` ของทิกเก็ตนี้เอง (ไม่ได้แก้ปัญหา CREATE INDEX
ที่อ้างถึงคอลัมน์ใหม่บนฐานข้อมูลเก่ามากไปกว่านี้ เพราะนอกขอบเขตทิกเก็ตนี้และไม่มีเทสต์คลุมสถานการณ์
อัปเกรดฐานข้อมูลเก่าอยู่แล้ว)

---

## 24. บั๊กในเครื่องมือถ่ายภาพหน้าจอ — ภาพ "โหมดคอนทราสต์สูง" ไม่เคยเป็นโหมดคอนทราสต์สูงจริงมาตลอด

**พบระหว่างไหน** — ระหว่างตรวจสอบหน้า Landing Page ตามที่ผู้ใช้ขอ (เห็นว่าภาพเปรียบเทียบ
โหมดปกติ/คอนทราสต์สูงในหน้านั้นดูแทบไม่ต่างกันเลยที่ขนาดเล็ก) ไล่ตรวจจนถึง `md5sum` ของไฟล์ต้นทาง
`docs/screenshots/phone-27-tables-high-contrast.png` และ `tablet-26-kitchen-high-contrast.png`
พบว่า**เหมือนกับคู่โหมดปกติทุกไบต์** — ไม่ใช่แค่คล้ายกัน แต่เป็นไฟล์เดียวกันเป๊ะ ตั้งแต่ภาพชุดนี้
ถูกสร้างขึ้นครั้งแรก (ก่อนหน้านี้ในเซสชันนี้เองที่เพิ่งรัน regenerate ทั้งชุดไปสำหรับทิกเก็ต 10
ก็ยังไม่ทันสังเกตเห็นจุดนี้ เพราะดูแค่ว่า "มีการเปลี่ยนแปลงจริง" ของแต่ละไฟล์ ไม่ได้ไล่เทียบสองไฟล์
คู่กันเอง)

**สาเหตุที่แท้จริง** — `tool/screenshots/capture_test.dart` เดิมตั้งโหมดด้วย
`setUp(() => AppColors.contrast = AppContrast.high)` ก่อนเรียก `ScreenshotHarness.launchApp()`
แต่ `PaynEatApp.build()` (ดู `lib/app/app.dart`) เรียก `ContrastService.restore()` **ทุกครั้งที่
build** ("ต้องคืนค่าคอนทราสต์ก่อนสร้างธีม ไม่งั้นเฟรมแรกจะวาดด้วยโหมดปกติแล้วค่อยกระพริบ") ซึ่งอ่านค่า
จาก `StorageService` แล้วตั้งทับ `AppColors.contrast` — และ `launchApp()` สร้าง
`StorageService.memory()` ใหม่ว่างเปล่าทุกครั้ง ค่าที่ทดสอบตั้งไว้ตรง ๆ ก่อนหน้านั้นเลยถูกรีเซ็ตกลับเป็น
`AppContrast.standard` ทันทีที่ `pumpWidget(const PaynEatApp())` ทำงาน ก่อนที่ widget ใด ๆ จะได้วาด
ด้วยซ้ำ — เท่ากับว่าทุกภาพที่ติดคำว่า "high-contrast" ในชื่อไฟล์ ถูกถ่ายด้วยโหมดปกติมาตลอด

**ทำไมไม่มีเทสต์ไหนจับได้มาก่อน** — `contrast_service_test.dart`/`app_colors_contrast_test.dart`
ทดสอบเส้นทางสลับโหมดจริง (ผ่านปุ่มในแอป) และค่าคอนทราสต์ที่คำนวณจาก token โดยตรง ซึ่งทั้งคู่ถูกต้อง
อยู่แล้ว — บั๊กอยู่เฉพาะในตัวเครื่องมือถ่ายภาพหน้าจอ (`tool/screenshots/`) ที่ไม่ได้อยู่ใน `test/`
และไม่มีอะไรเทียบภาพ 2 ไฟล์กันเองว่าต้องต่างกัน (golden test เทียบแค่ "ไฟล์นี้ตรงกับที่คอมมิตไว้
ไหม" ไม่เคยเทียบ "ไฟล์ A ต้องไม่เหมือนไฟล์ B")

**ที่แก้** — ย้ายการตั้งโหมดจาก `AppColors.contrast = ...` ตรง ๆ ไปเป็นการ seed
`StorageService` **ก่อน** สร้างแอป (`ScreenshotHarness.launchApp(..., highContrast: true)` เรียก
`storage.saveContrast('high')` ก่อน `Get.put<StorageService>(storage, ...)`) ให้เดินตามเส้นทางเดียว
กับตอนแอปเปิดจริง (`ContrastService.restore()` อ่านเจอค่าที่ seed ไว้แล้วตั้งถูกต้องตั้งแต่ build
แรก) ลบ `setUp`/`tearDown` เดิมที่ตั้ง static field ตรง ๆ ทิ้งเพราะไม่มีผลจริงอยู่แล้ว

**ผลหลังแก้** — regenerate ภาพทั้งชุดใหม่ มีแค่ 2 ไฟล์นี้เท่านั้นที่เปลี่ยน (ไฟล์อื่นอีก 27 ใบ
ไบต์เหมือนเดิมทุกอัน ยืนยันว่าไม่กระทบอะไรนอกเหนือจากนี้) เทียบแล้วเห็นความต่างจริงตามที่เอกสาร
ข้อ 18 อธิบายไว้ เช่นปุ่ม "เริ่มทำ" บนตั๋วครัวสลับจากพื้นส้มสด/ตัวหนังสือเข้ม เป็นพื้นน้ำตาลเข้ม/
ตัวหนังสือขาวเมื่อสลับโหมด (`AppColors.fillOf`/`onColor`) และหัวคอลัมน์ "รอทำ" เข้มขึ้นชัดเจน
(`AppColors.inkOf`) — นำภาพที่ถูกต้องไปใช้แทนในหน้า Landing Page ด้วย

**บทเรียน** — ชื่อไฟล์ที่บอกว่า "ภาพนี้คือโหมด X" ไม่ได้แปลว่าภาพนั้นถูกถ่ายในโหมด X จริง
ต้องตรวจสอบด้วยว่าค่า config ที่ตั้งไว้ก่อนถ่ายภาพ **อยู่รอด** จนถึงตอนที่ widget ถูกวาดจริงหรือเปล่า
โดยเฉพาะเมื่อโค้ดแอปเองมีจุด "restore ค่าจาก storage ทับทุกครั้งที่ build" อยู่แล้วเพื่อจุดประสงค์อื่น
(กันกระพริบตอนเปิดแอป) — จุดตรวจสอบที่ง่ายและตรงที่สุดคือเทียบ `md5sum`/ไบต์ของภาพคู่ที่ควรต่างกัน
ไม่ใช่แค่ดูว่าแต่ละภาพ "เปลี่ยนไปจากที่คอมมิตไว้" หรือเปล่า

---

## 25. Order audit trail — ขยาย audit log (ข้อ 21) ให้ครอบคลุม "ใครกดสั่ง/แก้ไขออเดอร์" ไม่ใช่แค่เหตุการณ์เสี่ยง

**ปัญหา** — ผู้ใช้ (เจ้าของโปรเจกต์) ขอฟีเจอร์ audit สำหรับทั้ง financial audit และผู้จัดการร้าน
ให้ตรวจสอบได้ว่า "ใครกดสั่งหรือแก้ไขออเดอร์" — audit log เดิม (ข้อ 21) บันทึกเฉพาะเหตุการณ์ที่
เสี่ยงต่อการทุจริตเท่านั้น (ยกเลิกออเดอร์, void รายการหลังครัวทำแล้ว, แก้ส่วนลด ฯลฯ) การเปิดออเดอร์
ใหม่และการแก้ไขรายการปกติระหว่างที่ยัง pending ไม่ถูกบันทึกเลย ไม่ใช่ gap ที่มาจาก PO gap analysis
เดิม (`docs/FEATURE-GAP-ANALYSIS.md`) แต่เป็นคำขอเพิ่มเติมภายหลัง (ดู
`docs/tickets/13-order-audit-trail.md`)

**ที่เลือก**

- **เพิ่ม action type ใหม่ 6 รายการเข้า infrastructure เดิมของข้อ 21** ไม่สร้างระบบ log แยก:
  `order.create`, `order.item.add`, `order.item.edit`, `order.item.remove`, `order.move_table`,
  `order.merge` — ใช้ตาราง `audit_logs` เดียวกัน, `auditLogService.log()` เดียวกัน, เรียกภายใน
  ทรานแซกชันเดียวกับการเขียนข้อมูลจริงเสมอเหมือนเดิม
- **`order.item.remove` ใช้ `entityType: 'order'` ไม่ใช่ `'order_item'`** — ต่างจาก
  `order_item.void` (ข้อ 21) ที่ item ยังอยู่ในตาราง (แค่เปลี่ยนสถานะ) รายการที่ถูก "ลบ" จริงจะหาย
  จากตาราง `order_items` ไปเลย ผูก log กับ `entityId` ของ item ที่ไม่มีอยู่แล้วจะลิงก์ไม่ได้ จึงผูก
  กับออเดอร์แทนและเก็บชื่อ/จำนวนรายการไว้ใน `metadata` แทน
- **`order.item.edit` log เฉพาะตอนแก้ "จำนวน" เปลี่ยนจริงเท่านั้น ไม่ log ตอนแก้แค่โน้ต** — โน้ต
  (เช่น "ไม่เผ็ด") ไม่กระทบยอดเงิน/บัญชี ไม่ใช่สิ่งที่ financial/manager audit ต้องการเห็น หลักการ
  เดียวกับที่ข้อ 21 ไม่ log การแก้ชื่อพนักงานเฉยๆ — log เฉพาะสิ่งที่กระทบตัวเลขจริง
- **`order.create` ผูก actor จาก `waiterId`/`currentUserId` ของออเดอร์นั้นโดยตรง** ไม่ต้องรับ
  `user` เพิ่มเป็นพารามิเตอร์ใหม่ เพราะ `create()` มี `user` (คนที่กดสั่ง) อยู่แล้วในทุก entry
  point (route ก็ auth ไว้แล้ว)
- **เจตนาไม่ log ทุก field แบบ diff ละเอียด** — เก็บแค่ summary (ข้อความอ่านง่าย) + metadata
  สำคัญ (ชื่อเมนู/จำนวนเก่า-ใหม่/โต๊ะต้นทาง-ปลายทาง) พอสำหรับตรวจสอบ ถ้า log ทุก field แบบละเอียด
  จะ noise สูงเกินไปสำหรับ POS ร้านอาหารที่มีรายการเปลี่ยนบ่อยมาก (เหตุผลเดียวกับข้อ 21 ที่ไม่ log
  ทุกการแก้ไข)
- **Demo Mode mirror เต็มรูปแบบ** (`demo_store_orders.dart` เพิ่ม `_logAudit` เข้าไปใน
  `createOrder`/`addItems`/`updateItem`/`removeItem`/`moveOrderTable`/`mergeOrders`,
  `demo_data_sources.dart` ส่ง `actorId: _auth.currentUserId` ให้ทุกเมธอดที่เพิ่มใหม่) —
  พฤติกรรมตรงกับ backend ทุกจุดเหมือนข้อ 21

**Frontend** — ไม่ต้องแก้ UI หน้า **ประวัติการทำรายการ** เลย เพราะ filter chip สร้างจาก
`AuditLogAction.all` อัตโนมัติอยู่แล้ว (ดู `audit_log_page.dart`) แค่เพิ่ม action constant +
translation key ใหม่ 6 คู่ (TH/EN) ใน `AuditLogAction`/`audit_log_translations.dart` ก็ขึ้น
filter chip และป้ายชื่อใหม่ให้อัตโนมัติ

**ข้อเสียที่ยอมรับ** — เพิ่ม log volume ขึ้นมากพอสมควร (ทุกออเดอร์/ทุกการเพิ่ม-แก้-ลบรายการ ไม่ใช่
แค่เหตุการณ์เสี่ยง) ต่างจากหลักการเดิมของข้อ 21 ที่ตั้งใจ log แค่ "เสี่ยงจริง" เพื่อไม่ให้สัญญาณ
จมหาย — ยอมรับเทรดออฟนี้เพราะผู้ใช้ระบุชัดว่าต้องการ accountability ระดับ "ใครกดสั่ง/แก้ไข" ทุกครั้ง
ไม่ใช่แค่กรณีน่าสงสัย ถ้า log volume เยอะเกินจนหน้าจอ admin ใช้งานยาก ควรพิจารณาเพิ่ม filter ตาม
ช่วงเวลา (date picker — ยังเป็นข้อเสียที่ยอมรับค้างจากข้อ 21 เช่นกัน) เป็นลำดับถัดไป

**ยังไม่ได้ทดสอบ** — เขียน/รัน backend test ครบ (`backend/tests/audit-logs.test.js`, 224 เคสผ่าน
ทั้งหมด) แต่ฝั่ง Flutter ไม่มี Flutter SDK ในสภาพแวดล้อมที่พัฒนา (sandbox ไม่มี `flutter`/`dart`
ติดตั้ง) จึงรีวิวโค้ด Demo Mode ด้วยมือแทนการรัน `flutter analyze`/`flutter test` จริง — ควรรัน
ยืนยันอีกครั้งในสภาพแวดล้อมที่มี Flutter ก่อน merge ถ้าเป็นไปได้

## 26. PromptPay QR — generate payload เองแทนพึ่ง payment gateway, mirror อัลกอริทึมเป็น Dart คู่กับ JS

**ปัญหา** — ระหว่างตรวจ `docs/FEATURE-GAP-ANALYSIS.md` ซ้ำ พบว่า `qr` ใน `PAYMENT_METHODS`
เป็นแค่ label ให้ cashier กดยืนยันเอง เหมือน `card`/`transfer` ทุกอย่าง ไม่มีการ generate QR
ตามมาตรฐาน PromptPay (EMV QR) เลย ทั้งที่เป็น gap เดียวที่เหลืออยู่ใน 🔴 Critical (ดู
`docs/tickets/16-promptpay-qr.md`)

**ที่เลือก**

- **Generate payload เองเป็น pure function ไม่พึ่ง payment gateway/npm package ภายนอก** —
  อัลกอริทึม EMV QRCPS Merchant Presented Mode (TLV + CRC-16/CCITT-FALSE) เป็นมาตรฐานสาธารณะที่
  เสถียร ไม่เปลี่ยนบ่อย เขียนเองสั้นกว่าและไม่ต้องเพิ่ม dependency ใหม่ฝั่ง backend — อ้างอิง
  โครงสร้างจาก `dtinth/promptpay-qr` (MIT, implementation อ้างอิงที่ใช้กันแพร่หลายที่สุดสำหรับ
  PromptPay ฝั่ง JS) และตรวจ CRC ด้วย test vector มาตรฐานของ CRC-16/CCITT-FALSE ("123456789" →
  `0x29B1`) ก่อนเอามาใช้จริง
- **Backend คำนวณ payload อย่างเดียว ไม่ generate ภาพ QR** — คืนเป็น string ให้ Flutter เรนเดอร์
  เป็นภาพเองด้วย `qr_flutter` (เพิ่ม dependency ใหม่ฝั่ง Flutter) หลีกเลี่ยงการส่งไฟล์รูปภาพผ่าน
  API และให้ Flutter ควบคุม UI/ขนาดภาพได้อิสระ
- **Mirror อัลกอริทึมเป็น Dart อีกชุดสำหรับ Demo Mode** (`app/lib/core/utils/promptpay.dart`)
  แทนที่จะบังคับให้ Demo Mode เรียก backend จริง (ซึ่งไม่มีให้เรียกตามนิยามของ Demo Mode) — เป็น
  โค้ดคำนวณที่ซ้ำ Dart/JS ตามหลักการเดียวกับที่ `docs/DECISIONS.md` #2 วางไว้ (bill calculation)
  จึงคุมด้วย **golden-value test**: คำนวณ payload ตัวอย่างเดียวกัน (เบอร์ `0812345678`, ยอด 100
  บาท) จากทั้งสองฝั่งแล้วยืนยันว่าได้สตริงตรงกันเป๊ะทุกตัวอักษร (`test/core/utils/promptpay_test.dart`)
  กันกรณี implement ผิดสเปกโดยไม่รู้ตัวข้างใดข้างหนึ่ง
- **เพิ่ม field `promptPayId` แยกจาก `storeTaxId` ที่มีอยู่แล้ว** — แม้ทั้งคู่เป็น "เลขประจำร้าน"
  เหมือนกัน แต่ร้านอาจอยากใช้เบอร์โทรเป็นเลขพร้อมเพย์ ต่างจากเลขผู้เสียภาษีที่ใช้ออกใบกำกับภาษี
  (คนละความหมาย คนละ validation length) — ใช้ตาราง `settings` key-value เดิม ไม่ต้อง migration
- **ไม่มี payment gateway/callback ตรวจสอบการจ่ายอัตโนมัติ** — ตามขอบเขตที่ gap analysis แนะนำไว้
  แคชเชียร์ยังต้องเช็คสลิป/แอปธนาคารเองก่อนกดยืนยันรับชำระ เหมือนช่องทางโอน/บัตรเดิมทุกประการ ไม่ใช่
  ฟีเจอร์ใหม่ที่ต้องเรียนรู้เพิ่ม
- **ยังไม่ได้ตั้งค่าเลขพร้อมเพย์ → 400 พร้อมข้อความแจ้งชัดเจน** ไม่ใช่พังเงียบๆ หรือคืน QR ที่ใช้
  งานจริงไม่ได้ (ทั้ง backend และ Demo Mode ใช้ข้อความเดียวกัน)

**ข้อเสียที่ยอมรับ** — โค้ดคำนวณ EMV QR ซ้ำกัน 2 ภาษา (JS ฝั่ง backend, Dart ฝั่ง Demo Mode) เพิ่ม
ภาระดูแลถ้าสเปกเปลี่ยน (ไม่น่าเกิดบ่อยเพราะเป็นมาตรฐานสาธารณะที่นิ่งแล้ว) แต่ golden-value test
ป้องกันไม่ให้สองฝั่งเพี้ยนออกจากกันโดยไม่รู้ตัว — ยึดหลักการเดียวกับ #2

## 27. Audit ระดับบัญชี/การเงิน — export CSV เป็น web-only ด้วย `package:web`, ไม่ใช้ `dart:html` ที่ deprecated แล้ว

**ปัญหา** — ticket 13 (ข้อ 25) ปิดฝั่งผู้จัดการร้าน ("ใครสั่ง/แก้ไขออเดอร์") แต่ตรวจโค้ดจริงพบว่า
ฝั่งบัญชียังไม่ครบ: `menu.service.js` (แก้ราคาเมนู), `promotion.service.js` (สร้าง/แก้/ลบโปรโมชัน),
`ingredient.service.js` (ปรับสต๊อกมือ) ไม่มีการเรียก `auditLogService.log()` เลย และหน้า "ประวัติการ
ทำรายการ" ไม่มีทาง export ให้ฝ่ายบัญชี ไม่มี date range picker (ของค้างจากข้อ 21) ดู
`docs/tickets/14-financial-audit-trail.md`

**ที่เลือก**

- **เพิ่ม action type ใหม่ 5 รายการเข้า infrastructure เดิมของข้อ 21/25** ไม่สร้างระบบ log แยก:
  `menu.price_change`, `promotion.create`, `promotion.update`, `promotion.delete`,
  `ingredient.stock_adjust` — `menu.price_change` log เฉพาะตอนราคาเปลี่ยนจริง (หลักการเดียวกับ
  `order.item.edit` ในข้อ 25 ที่ไม่ log ทุก field) ส่วนโปรโมชัน/สต๊อกกระทบยอดขาย/ต้นทุนโดยตรงทุก
  ครั้งจึง log ทุก action ไม่กรอง
- **Export CSV เป็น pure function แยกจากการ generate ที่ backend** (`backend/src/core/csv.js`,
  `app/lib/core/utils/csv.dart`) — RFC 4180 พื้นฐาน (escape comma/quote/newline) + UTF-8 BOM ขึ้นต้น
  กัน Excel เปิดแล้วอักษรไทยเพี้ยน (Excel เดาเป็น ANSI ถ้าไม่มี BOM) — endpoint ใหม่
  `GET /audit-logs/export` reuse filter เดิมของ `GET /audit-logs` แต่ไม่มี pagination (ดึงทุกแถวที่
  ตรงเงื่อนไขครั้งเดียว เพราะ export ต้องได้ไฟล์ครบ ไม่ใช่แค่หน้าที่กำลังดูอยู่)
- **ดาวน์โหลดไฟล์ฝั่ง Flutter ใช้ `package:web` + `dart:js_interop` ไม่ใช้ `dart:html`** — `dart:html`
  ถูก mark `@Deprecated('Use package:web and dart:js_interop instead.')` ในเวอร์ชัน Dart SDK ที่
  Flutter 3.35.1 ล็อกไว้อยู่แล้ว การใช้จะทำให้ `flutter analyze` ไม่ผ่าน (ต้องได้ "No issues found!"
  เสมอ) จึงต้องใช้ทางที่ทีม Dart แนะนำแทนตั้งแต่แรก ไม่ใช่ hack ชั่วคราว
- **Conditional export/import แยก implementation ตาม platform ตอน compile** —
  `csv_download_web.dart` (สร้าง Blob + `<a download>` แล้วกดให้เอง) เลือกด้วย
  `if (dart.library.js_interop)` ส่วน `csv_download_stub.dart` (แพลตฟอร์มอื่น โยน
  `UnsupportedError`) เป็นค่าเริ่มต้น — ทดสอบแล้วว่า `flutter test` (รันบน Dart VM ไม่ใช่เว็บ) ได้
  `isCsvDownloadSupported == false` เสมอ ตรงกับความเป็นจริงบนแอป mobile/desktop ด้วย (build
  เป้าหมายที่ไม่ใช่เว็บก็ไม่มี `dart.library.js_interop` เหมือนกัน)
- **จำกัดขอบเขต export ไว้แค่หน้าเว็บผู้ดูแลระบบเท่านั้น** — หน้า "ประวัติการทำรายการ" อยู่ใน
  README หัวข้อ "🖥️ ผู้ดูแลระบบ (เว็บ)" อยู่แล้วตั้งแต่ต้น (เห็นเฉพาะ `admin` และเป็น flow ที่ออกแบบ
  มาสำหรับหน้าจอกว้าง) การ export ไฟล์บนมือถือ/แท็บเล็ตต้องมี `path_provider`/`share_plus` เพิ่ม ซึ่ง
  เกินความจำเป็นสำหรับ flow ที่ไม่ได้ใช้บนอุปกรณ์นั้นอยู่แล้ว
- **ปุ่มเช็ค `isCsvDownloadSupported` ก่อนเรียก backend เสมอ** — ไม่เสียเวลายิง API ถ้าดาวน์โหลด
  ไม่ได้อยู่ดี ขึ้นข้อความแจ้งชัดเจนแทนปุ่มที่กดแล้วไม่มีอะไรเกิดขึ้น
- **Demo Mode mirror เต็มรูปแบบ** (`demo_store_menu.dart`/`demo_store_promotions.dart`/
  `demo_store_ingredients.dart` เพิ่ม `_logAudit` เข้า `saveMenuItem`/`savePromotion`/
  `deletePromotion`/`adjustIngredientStock`, `demo_store_audit_logs.dart` เพิ่ม
  `auditLogExportCsv` ที่ใช้ `toCsv` ตัวเดียวกับหน้าจออื่น) — `DemoMenuDataSource`/
  `DemoIngredientDataSource`/`DemoPromotionDataSource` ต้องรับ `DemoAuthDataSource` เพิ่มเพื่อรู้
  `currentUserId` (เดิมไม่มีเพราะไม่เคย log อะไรมาก่อน)

**ข้อเสียที่ยอมรับ** — ปุ่ม export ไม่โผล่ให้กดบน mobile/desktop เลย (ไม่ใช่แค่กดแล้ว error) — ยอมรับ
เพราะหน้านี้เป็น admin/เว็บอยู่แล้วตามที่ออกแบบไว้ตั้งแต่ ticket 08 — ถ้าวันไหนต้องรองรับ mobile
export จริง ต้องเพิ่ม `path_provider` + `share_plus` เป็น dependency ใหม่แยกต่างหาก

## 28. ปิดรู audit log ที่เหลือ — เปิด/ปิดกะ, รับชำระเงิน, กรอก/ถอดโค้ดส่วนลด

**ปัญหา** — self code-review รอบใหม่ (เช็ค clean code/tech debt/state management/architecture/
spaghetti risk ทั้ง backend และ Flutter) พบว่าแม้ข้อ 21/25/27 จะปิด gap ของ audit log ไปหลายชั้นแล้ว
ก็ยังมี 3 จุดที่กระทบเงิน/บัญชีโดยตรงแต่ไม่ log เลย:

1. `shift.service.js#open/close` — `close()` คำนวณส่วนต่างเงินสด (variance) ซึ่งเป็นเหตุการณ์เสี่ยง
   ทุจริตแบบเดียวกับที่ audit log ข้อ 21 ตั้งใจครอบคลุมไว้ตั้งแต่แรก แต่ไม่เคยเรียก
   `auditLogService.log()` เลยสักจุด
2. `payment.service.js#pay()` — รับชำระเงิน (รวมการใช้แต้มสะสมลดยอด) ไม่ log ทั้งที่ `refund()` ใน
   ไฟล์เดียวกัน log ทุกครั้ง — ทำให้ตรวจสอบย้อนหลังได้แค่ "เงินคืนเท่าไหร่" แต่ไม่มี "รับเข้าเท่าไหร่
   ใครรับ"
3. `order.service.js#redeemPromotionCode/removePromotion` — เขียน `updateTotals` ตรงๆ โดยไม่มีทั้ง
   `getDb().transaction()` ห่อและไม่มี audit log ทั้งที่ method พี่น้องในไฟล์เดียวกัน
   (`applyDiscount`) มีครบทั้งสองอย่าง — เสี่ยงทั้ง data integrity (เขียนไม่ atomic กับ log) และ
   ตรวจสอบย้อนหลังไม่ได้ว่าใครกรอก/ถอดโค้ดส่วนลดออกจากบิล

**ที่เลือก**

- **เพิ่ม action type ใหม่ 4 รายการเข้า infrastructure เดิมของข้อ 21/25/27** ไม่สร้างระบบแยก:
  `shift.open`, `shift.close`, `payment.pay`, `order.promotion_redeem`, `order.promotion_remove`
  (จริง ๆ คือ 5 action แต่ `shift.open`/`shift.close` นับเป็นคู่เดียวกัน) — ทุกจุด log ทุกครั้งไม่มี
  เงื่อนไขกรอง เพราะกระทบเงิน/ยอดขายโดยตรงทุกครั้งที่เกิด ต่างจาก `menu.price_change` (ข้อ 27) ที่
  กรองเฉพาะตอนราคาเปลี่ยนจริง
- **`shift.open/close` ห่อด้วย `getDb().transaction()` เพิ่มใหม่** (เดิมเป็นแค่ single UPDATE/INSERT
  statement ไม่มี transaction เลยเพราะไม่เคยต้องเขียนคู่กับอะไร) ให้ atomic กับ audit log write
  ตามหลักการเดียวกับทุก service อื่นที่มี audit log (ถ้า log ล้มเหลว การเปิด/ปิดกะต้องล้มเหลวด้วย)
- **`redeemPromotionCode`/`removePromotion` ห่อด้วย transaction ตามแบบ `applyDiscount`** ในไฟล์
  เดียวกันทุกประการ (ทั้งสองอยู่ใน `order.service.js`) แทนที่จะสร้าง pattern ใหม่ — ต้องแก้
  `order.controller.js` ให้ส่ง `req.user` ผ่านไปด้วย (เดิมไม่เคยส่งเพราะไม่เคยต้องใช้)
- **ไม่แตะ `customer.service.js`/`table.service.js`** แม้ตรวจพบว่าก็ไม่ log เหมือนกัน — พิจารณาแล้วว่า
  ความเสี่ยงต่ำกว่ามาก (สร้างลูกค้าใหม่/เปลี่ยนสถานะโต๊ะไม่กระทบตัวเลขทางบัญชีโดยตรงเหมือน 3 จุดข้าง
  ต้น) จึงไม่รวมในรอบนี้ ทิ้งไว้เป็นของที่พิจารณาเพิ่มได้ในอนาคตถ้าจำเป็น

**ข้อเสียที่ยอมรับ** — ไม่มี ทุกจุดที่แก้เป็นการเติมของที่ขาดตาม pattern เดิมที่มีอยู่แล้วในโปรเจกต์
ไม่ได้เปลี่ยน behavior หรือ trade-off ใหม่ใดๆ ที่ผู้ใช้จะสังเกตเห็น (audit log อ่านได้เฉพาะ admin
เหมือนเดิม)

## 29. แยกไฟล์ Flutter ที่โตเกิน 400 บรรทัดอีก 3 จุดที่เอกสารไม่เคย backfill

**ปัญหา** — self code-review รอบเดียวกับข้อ 28 พบว่า `docs/CODING_STANDARDS.md` §2.2 ยังบันทึก
`demo_store.dart` ว่าแยกเป็นแค่ 7 ไฟล์ (อัปเดตล่าสุด 2026-09-07) ทั้งที่ ticket ใหม่ๆ (05/06/08/09/10/
14) เพิ่ม part file เข้าไปจนเป็น 15 ไฟล์แล้วจริง และ `demo_store_orders.dart` เองก็โตเป็น 825
บรรทัด (เกิน threshold ไปเยอะ) — นอกจากนี้ยังพบอีก 2 ไฟล์ที่โตเกิน 400 บรรทัดแบบเดียวกันแต่ไม่เคย
ถูกบันทึกไว้เลยสักครั้ง: `core/demo/demo_data_sources.dart` (846 บรรทัด รวม 14 คลาสไม่เกี่ยวข้องกัน)
และ `app/di/initial_binding.dart` (644 บรรทัด ผูก data source/repository/use case ของ 14 โดเมนรวม
กันในไฟล์เดียว)

**ที่เลือก**

- **`demo_store_orders.dart` แยกเพิ่มอีก 2 ไฟล์**: `demo_store_order_items.dart` (การเพิ่ม/แก้/ลบ/
  เปลี่ยนสถานะรายการอาหารในออเดอร์) และ `demo_store_order_promotions.dart` (resolve/redeem/remove
  โปรโมชันของออเดอร์) แยกตามความรับผิดชอบย่อยภายในโดเมนเดียวกัน (order) ไม่ใช่แยกข้ามโดเมน — ยัง
  ใช้ `part`/`part of` + `extension ... on DemoStore` แบบเดิมทุกประการ เพราะยังเป็น state เดียวกัน
  (`orders` list) ที่ต้องเข้าถึงข้ามไฟล์
- **`demo_data_sources.dart` แยกเป็น 1 part file ต่อ 1 คลาส** ด้วย `part`/`part of` เหมือนกัน แม้
  แต่ละคลาส `Demo*DataSource` จะเป็นอิสระต่อกัน ไม่ได้แชร์ state แบบ `DemoStore` — เลือก part/part-of
  แทนการแยกเป็นไฟล์ import ปกติ (หรือ `export` barrel) เพราะทั้ง 14 คลาสแชร์ helper ส่วนกลางตัวเดียว
  (`_delayed`) ที่อยากให้ยังเป็น library-private ไม่ต้อง expose เป็น public API — ใช้กลไกเดียวกับ
  `demo_store.dart` ทำให้ผู้อ่านโค้ดในโปรเจกต์ไม่ต้องเรียนรู้ pattern ใหม่
- **`initial_binding.dart` แยกตาม "ขั้นตอน" ไม่ใช่ตาม "โดเมน"** — ต่างจากอีก 2 ไฟล์ข้างต้น เพราะ
  `_bindDataSources`/`_bindRepositories`/`_bindUseCases` แต่ละเมธอดวนลูปทุกโดเมนอยู่แล้วในตัวเอง
  (แยกตามลำดับที่ต้องผูก: core → data source → repository → use case → global controller) แยก
  ตามโดเมนแทนจะทำให้แต่ละไฟล์ต้องรู้ลำดับการผูกของทุกขั้นตอนซ้ำ 14 รอบ ซับซ้อนกว่าเดิม จึงแยกเป็น
  ฟังก์ชัน top-level ธรรมดา (`bindCoreServices`/`bindDataSources`/`bindRepositories`/`bindUseCases`/
  `bindGlobalControllers`) ใน `app/di/bindings/` แทน — ไม่ใช้ `part`/`part of` เพราะแต่ละฟังก์ชัน
  พึ่งพากันผ่าน `Get.find<T>()` (service locator pattern ของ GetX) อยู่แล้ว ไม่มี private state ที่
  ต้องแชร์ข้ามไฟล์แบบ `demo_store.dart`/`demo_data_sources.dart` — คนละแนวคิดกับ
  `presentation/bindings/*_binding.dart` ที่มีอยู่แล้ว (`home_binding.dart` ฯลฯ) ซึ่งผูก controller
  ระดับหน้าจอจาก use case ที่ `InitialBinding` ผูกไว้แล้ว ไม่ใช่ composition root ระดับแอป
- **อัปเดต `docs/CODING_STANDARDS.md` §2.2 ให้ตรงกับจำนวนไฟล์/บรรทัดจริงทั้ง 3 จุด** พร้อมย้ำ
  ในตัวเอกสารเองว่าต้องเช็คด้วย `wc -l` ก่อนแก้ทุกครั้ง ไม่ใช่เดาหรือคัดลอกเลขเก่า (ต้นเหตุที่ทำให้
  เอกสารเพี้ยนไปจากโค้ดจริงในรอบนี้)

**ข้อเสียที่ยอมรับ** — ไม่มี เป็นการจัดระเบียบไฟล์ล้วนๆ ไม่เปลี่ยน behavior เลยสักจุด ยืนยันด้วย
`flutter analyze`/`dart format`/`flutter test` (311/311 ผ่านทุกครั้งหลังแยกแต่ละไฟล์) และ build
web + smoke test ด้วย headless browser ยืนยันว่า DI wiring ยังทำงานถูกต้องหลังแยก
`initial_binding.dart`

## 30. Deploy landing page ขึ้น GitHub Pages แทน Vercel

**ปัญหา** — `docs/landing/index.html` เป็นไฟล์ static เดี่ยว มีไว้ให้เปิดในเบราว์เซอร์ตรงๆ เท่านั้น
ยังไม่มี URL จริงให้แชร์ ผู้ใช้ขอให้ deploy ให้เหมือนโปรเจกต์อื่น (ExcelToGo) ที่มี live demo URL
จริงผ่าน Vercel

**ที่เลือก**

- **GitHub Pages แทน Vercel** — ต่างจาก ExcelToGo ที่เป็นแอป Next.js ต้อง build/runtime จริงจึงต้อง
  ใช้ platform ระดับ Vercel, `docs/landing/index.html` เป็น static HTML ไฟล์เดียวไม่มี build step
  เลย ไม่มี server-side logic — GitHub Pages (ฟรี ผูกกับ repo นี้โดยตรง ไม่ต้องเชื่อม account
  ภายนอกเพิ่ม) จึงพอเพียงและตรงไปตรงมากว่า
- **Deploy ผ่าน GitHub Actions (`actions/deploy-pages`) ไม่ใช้ "Deploy from a branch"** — เลือก
  source แบบ Actions เพราะ deploy เฉพาะโฟลเดอร์ `docs/landing/` เป็น root ของเว็บไซต์ได้ตรงๆ
  (URL ออกมาเป็น `https://suruchboss.github.io/PaynEat/` พอดี) ต่างจาก "Deploy from a branch →
  /docs" ที่จะเอาทั้งโฟลเดอร์ `docs/` (รวมเอกสารอื่นๆ อย่าง `DECISIONS.md`/`tickets/`) มาเป็น root
  ของเว็บไซต์ไปด้วย ซึ่งไม่ใช่สิ่งที่ตั้งใจให้คนทั่วไปเห็น
- **Trigger เฉพาะ push เข้า `main` ที่แตะ `docs/landing/**`** — ต่างจาก `ci.yml` เดิมที่รันทุก branch
  เพราะหน้านี้คือหน้าเว็บ "ที่ใช้งานจริง" ควรอัปเดตเฉพาะตอนงานถูก merge เข้า main แล้วเท่านั้น ไม่ใช่
  ทุกครั้งที่ push branch ทดลอง — มี `workflow_dispatch` ให้กดรันเองได้ด้วยเผื่อ debug
- **ต้องเปิด "Settings → Pages → Source: GitHub Actions" ด้วยมือครั้งเดียว** — GitHub REST API ไม่มี
  endpoint ให้เปิดใช้งาน Pages แบบอัตโนมัติผ่าน workflow ได้ (ต่างจากการ deploy จริงที่อัตโนมัติทุก
  ครั้งหลังจากนั้น) เป็นข้อจำกัดของแพลตฟอร์ม ไม่ใช่ทางเลือกออกแบบ

**ข้อเสียที่ยอมรับ** — ไม่มี custom domain/preview URL ต่อ PR แบบที่ Vercel ให้ฟรี (ทุก push ไป PR
ได้ preview URL แยก) — ยอมรับเพราะหน้านี้เป็นหน้าเดียวไม่มีหลายเวอร์ชันให้ preview พร้อมกันอยู่แล้ว

## 31. บั๊ก `docs/video/build.sh` ตัดฉากปิดวิดีโอหายไปเงียบๆ — แก้ด้วย freezedetect แทนเลข hardcode

**ปัญหา** — หลังเพิ่มฉาก PromptPay QR และ Audit Log เข้า `storyboard.py` (ticket 14/16 ที่ผ่านมา
ไม่เคยมีฉากพวกนี้ในวิดีโอ demo เลย) แล้วรันวิดีโอใหม่ พบว่าฉาก "สถิติ" (557 เทสต์ ฯลฯ) กับฉากปิด
(`ลองใช้งานได้เลย` + ลิงก์ GitHub) หายไปจากไฟล์ท้ายวิดีโอทั้งหมด ตรวจสอบด้วยการ burn-in timestamp
ลงเฟรมแล้วเทียบกับ timeline ทฤษฎีของ `build_video_html.py` พบว่า Chromium ตอนอัดวิดีโอ (headless,
ไม่มี GPU, ใช้ `--no-sandbox`) วาดตาม CSS animation จริงไม่ทันเวลาจริง เพราะทุกฉากมี
`backdrop-filter:blur(130px)` (`.bg-glow`) ทำงานตลอดเวลา วัดจริงพบว่าช้ากว่า timeline ได้ถึง ~16-20%
และยิ่งสะสมมากขึ้นเรื่อยๆ ตามความยาว — เดิม `build.sh` ใช้เลข hardcode (`DURATION=113.5`,
`ffmpeg -t 113.2`) คำนวณจาก timeline ทฤษฎี 112.5s บวก buffer แค่ ~1s ซึ่งไม่พอตั้งแต่ก่อนเพิ่มฉากใหม่
ด้วยซ้ำ (จึงเป็นไปได้ว่าวิดีโอที่ commit ไว้ก่อนหน้านี้ก็โดนตัดฉากปิดไปเงียบๆ เหมือนกัน แค่ไม่มีใครเช็ค
เฟรมสุดท้ายจริงจัง)

**ที่เลือก**

- **คำนวณความยาว timeline จาก `storyboard.py` โดยตรงในสคริปต์** (`sum(s['duration'] for s in
  scenes)`) แทน hardcode เลขไว้ในคอมเมนต์ — กัน bug ซ้ำเวลามีคนเพิ่ม/ลดฉากในอนาคตแล้วลืมแก้เลขนี้
- **อัดวิดีโอดิบยาวเผื่อไว้มาก** (`TOTAL + 60` วินาที) แทนที่จะคำนวณ multiplier ตายตัวจากอัตราส่วนที่
  วัดได้ในเครื่องนี้ครั้งเดียว — เพราะความช้าขึ้นอยู่กับสเปกเครื่องที่รัน ถ้า hardcode multiplier ตาม
  เครื่องนี้ (ช้า ~16-20%) เครื่องที่เร็วกว่าจะได้วิดีโอที่มีจอดำค้างท้ายยาวเกินไป เครื่องที่ช้ากว่าจะโดน
  ตัดฉากปิดหายอีกเหมือนเดิม
- **หาจุดจบจริงด้วย `ffmpeg -vf freezedetect`** — เลือก freezedetect เพราะฉากปิด/ฉากสถิติเล่นแอนิเมชัน
  reveal ครั้งเดียวจบแล้วค้างนิ่ง (ต่างจากฉาก "shot" ที่มี kenBurns ขยับต่อเนื่องทั้งฉาก จึงไม่ถูกจับเป็น
  freeze) หา `freeze_start` ตัวสุดท้ายที่ไม่มี `freeze_end` คู่กัน (แปลว่านิ่งไปจนจบไฟล์ที่อัดมา) นั่นคือ
  จุดที่ทุกแอนิเมชันในวิดีโอเล่นจบจริง เผื่อ pad ท้ายอีก 0.4s แล้วค่อยตัด — ได้ผลลัพธ์ที่ยาวพอดีอัตโนมัติ
  ไม่ว่าจะรันบนเครื่องเร็วหรือช้าแค่ไหน ไม่ว่า storyboard จะยาวขึ้นอีกแค่ไหนในอนาคต
- **fallback ถ้าไม่เจอ freeze ท้ายเลย** (เช่นเครื่องช้ากว่าที่เผื่อไว้มาก) ใช้ความยาวที่อัดมาทั้งหมดแทน
  พร้อม print warning — ยอมรับวิดีโอที่มีจอดำค้างท้ายเกินจำเป็น ดีกว่าตัดเนื้อหาจริงหายไปเงียบๆ แบบเดิม
- **เผยแพร่ไปพร้อมกัน**: แก้ bug แยกต่างหาก `${@:-th en}` ที่ quote ผิด (`"${@:-th en}"` ทำให้
  `th en` กลายเป็น argument เดียวแทนที่จะ split เป็นสองภาษา) เจอระหว่างทดสอบเรียก `./build.sh` แบบไม่ใส่
  argument (ค่า default ที่ตั้งใจให้ทำทั้งสองภาษา) — เคสนี้ไม่เคยมีใครทดสอบมาก่อนตั้งแต่เขียนสคริปต์นี้

**ข้อเสียที่ยอมรับ** — เวลารันสคริปต์นานขึ้นมาก (อัดดิบ ~170s/ภาษา แทน ~113s เดิม) เพราะต้องเผื่อ buffer
ใหญ่ + รัน freezedetect ผ่านไฟล์ทั้งหมดอีกรอบ ยอมรับได้เพราะสคริปต์นี้รันไม่บ่อย (แค่ตอน refresh
วิดีโอ demo ไม่ใช่ทุก commit/CI)

## 32. เพิ่ม `docs/PORTFOLIO-SUMMARY.md`/`.en.md` — สรุป 1 หน้าแยกจาก README สำหรับสมัครงาน/portfolio

**ปัญหา** — `README.md`/`README.en.md` ยาวมาก (ทัวร์ 5 นาที + ฟีเจอร์ครบทุกหมวด + คู่มือรันโปรเจกต์)
เหมาะกับคนที่จะ clone มารันจริง แต่ไม่เหมาะกับใช้แปะ portfolio หรือแนบสมัครงานที่ต้องการให้ผู้อ่าน
(กรรมการ/ผู้คัดเลือก) เห็นตัวเลขเด่นและจุดขายภายใน 30 วินาทีแรก ไม่ต้องเลื่อนหาเอง

**ที่เลือก**

- **แยกเป็นไฟล์ใหม่ ไม่ยัดเข้า README** — README มีหน้าที่ชัดเจนอยู่แล้ว (สอนใช้งาน/รันโปรเจกต์)
  การเพิ่มส่วน "สรุปสำหรับสมัครงาน" เข้าไปจะทำให้ผู้ใช้จริงที่มา clone repo เจอเนื้อหาที่ไม่เกี่ยวกับตัวเอง
- **คู่ TH/EN แยกไฟล์** (`.md` / `.en.md`) ตามรูปแบบเดียวกับ README — เพราะกลุ่มเป้าหมายต่างกัน
  (กรรมการ/บริษัทไทย vs. สมัครงานบริษัทต่างชาติ) และ `content_en.py` ก็ใช้หลักการเดียวกัน (เขียนใหม่
  เป็นภาษาอังกฤษเชิงธุรกิจ ไม่ใช่แปลตรงตัว)
- **ทุกตัวเลขต้องรันเช็คจริงก่อนใส่ ไม่คัดลอกจาก README/PDF เดิม** — ระหว่างเตรียมพบว่า README/PDF/
  วิดีโอเดิมอ้าง "54 API endpoints" ซึ่งเป็นเลขเก่าที่ผิดมานานแล้ว (นับ route จริงด้วย
  `grep -roE "router\.(get|post|put|patch|delete)\(" src/modules/` ได้ 82) แก้ไขให้ตรงทั้ง
  `content.py`/`content_en.py`/`storyboard.py`/PDF ทั้งสองภาษา/วิดีโอทั้งสองภาษา ไปพร้อมกัน ไม่ใช่แค่ใน
  เอกสารสรุปใหม่ไฟล์เดียว — เพื่อไม่ให้เอกสารชุดต่างๆ ของโปรเจกต์ขัดแย้งกันเอง

## 33. Ticket 15 — ผู้ช่วย AI ถามตอบข้อมูลร้าน (Ask-Your-Data) ผ่าน Claude API + tool-calling

**ปัญหา** — `docs/tickets/15-ai-ask-your-data.md` ตั้งใจให้เป็นจุดขายทางการตลาดว่า "เอา LLM มา
ประยุกต์ใช้จริง" ต่างจาก "chatbot ประดับ" ทั่วไปที่ตอบจากความรู้ทั่วไปของโมเดิลเฉยๆ ความเสี่ยงหลักคือ
LLM เดา/แต่งตัวเลขยอดขายเอง (hallucination) ซึ่งถ้าเกิดขึ้นกับข้อมูลร้านจริงจะทำลายความน่าเชื่อถือ
มากกว่าไม่มีฟีเจอร์นี้เลย

**ที่เลือก**

- **บังคับตอบผ่าน tool-calling เท่านั้น ห้ามมีทางตอบเป็นข้อความอิสระ** — เขียน manual tool loop เอง
  (ไม่ใช้ beta Tool Runner ของ SDK) เพราะ backend เป็น plain JS ไม่มี TypeScript loop เองทดสอบง่ายกว่า
  (inject fake client ได้ตรงๆ) และไม่ผูกกับ beta dependency — บังคับให้โมเดิลต้องเรียก tool
  `submit_answer` เสมอเป็นขั้นตอนสุดท้าย ผ่าน `tool_choice: {type:'tool', name:'submit_answer'}` ใน
  รอบสุดท้าย (`MAX_ROUNDS = 6`) ถ้าโมเดิลไม่ยอมเรียกเองก่อนหน้านั้น — กันทั้งการตอบมั่วแบบข้อความอิสระ
  และการวนเรียก tool ไม่จบ (ซึ่งเสียเงินจริงต่อ token)
- **แหล่งข้อมูลที่ผู้ใช้เห็น (`sources`) มาจากที่ระบบเป็นคนบันทึกเอง ไม่ใช่จากที่โมเดิลรายงานตัวเอง** —
  ทุกครั้งที่ tool ทำงานสำเร็จ ระบบ push `{tool, args}` เข้า `sourcesUsed` เอง โมเดิลไม่มีทางอ้างว่า
  ใช้ tool ที่ไม่ได้เรียกจริง เพราะ UI ไม่ได้เชื่อคำอธิบายของโมเดิล เชื่อแค่ log การเรียกจริงของระบบ
- **จำกัด tool ที่ยื่นให้โมเดิลเลือกตาม role ตั้งแต่ต้น ไม่ใช่กรองผลลัพธ์ทีหลัง** — `list_audit_log_entries`
  ยื่นให้เฉพาะ `admin` เท่านั้น (ตรงกับ `/audit-logs` เดิมที่สงวนไว้เฉพาะ admin) เพราะถ้าปล่อยให้
  manager เรียกได้ ผู้ช่วย AI จะกลายเป็นช่องทางเลี่ยงสิทธิ์ที่มีอยู่เดิมในระบบทันที
- **validate ซ้ำ input ของทุก tool ด้วย zod ก่อนเรียก handler จริงเสมอ** — SDK อาจ parse tool input ที่
  ถูกตัดตอนกลางแบบเงียบๆ ได้ ถ้าพารามิเตอร์ผิด schema (เช่น `limit` เกิน max) ส่ง tool_result แบบ
  `is_error` กลับไปให้โมเดิลแก้ไขเองแทนที่จะปล่อยให้ query พัง/คืนข้อมูลผิด
- **เก็บทุกคำถาม/คำตอบ/tool ที่เรียกไว้ในตารางใหม่ `ai_assistant_queries`** แยกจาก `audit_logs`
  เพราะคนละเรื่องกัน (audit_logs = ใครทำอะไรกับข้อมูลจริง, ตารางนี้ = ใครถาม AI อะไรและ AI ใช้ข้อมูล
  ไหนตอบ) ใช้ตารางเดียวกันนี้นับโควตาต่อวันด้วย (`AI_ASSISTANT_DAILY_LIMIT` ค่าเริ่มต้น 20) ไม่ต้องมี
  ตารางตัวนับแยก
- **ปิดฟีเจอร์อัตโนมัติถ้าไม่ตั้งค่า `ANTHROPIC_API_KEY` โดยคืน 503 ที่ endpoint แทนที่จะ throw ตอน
  server start** — ต่างจาก `JWT_SECRET` ที่ทั้งระบบพังถ้าไม่มี เพราะฟีเจอร์นี้เป็นของเสริมที่ปิดได้
  โดยไม่กระทบ POS หลัก (mirror แนวทางเดียวกับที่โปรเจกต์ Cwork ของผู้ใช้ทำกับ HR assistant ของเขา —
  "optional และปิดไว้เป็นค่าเริ่มต้น")
- **โหมดเดโม (GitHub Pages, ไม่มี backend/API key จริง) ตั้งใจไม่ปลอมคำตอบ AI ขึ้นมาเอง** — คืน error
  เดียวกับตอน backend จริงไม่ได้ตั้งค่า (`AI_ASSISTANT_DISABLED`, 503) เพราะจุดขายของทิกเก็ตนี้คือ
  "ถามได้จริง ตอบจากข้อมูลร้านจริง" การปลอมคำตอบไว้ล่วงหน้าในเดโมจะขัดกับสิ่งที่ฟีเจอร์นี้ตั้งใจพิสูจน์
  โดยตรง
- **เลือกทำเป็นแท็บนำทางแยกต่างหาก ("ผู้ช่วย AI" ไอคอน ✨) แทนการฝัง widget เข้าไปในหน้า
  Dashboard/Reports ตามที่ ticket ระบุคร่าวๆ** — เพราะสถาปัตยกรรมนำทางของแอปนี้เป็นแบบ 1 แท็บ = 1 หน้า
  เต็มอยู่แล้วทุกฟีเจอร์ (เหมือน audit log, ลูกค้า/แต้มสะสม) การฝัง chat widget ที่มีช่องพิมพ์ + คีย์บอร์ด
  เข้าไปใน `ListView` ของหน้า Dashboard จะชนกับ scroll/keyboard ของหน้านั้นเอง และ RBAC ระดับหน้าจอ
  (admin/manager เท่านั้น) ก็ implement ได้ตรงไปตรงมากว่าด้วยรูปแบบเดิมที่มีอยู่แล้ว (`destinationsForRole`)
- **โมเดลเริ่มต้น `claude-opus-5`** ตั้งค่าได้ผ่าน `AI_ASSISTANT_MODEL` — ผู้ดูแลระบบจริงปรับเป็นรุ่นอื่น
  ได้เองถ้าต้องการคุมค่าใช้จ่ายเพิ่มเติม โดยไม่ต้องแก้โค้ด
- **เพิ่ม demo ภาพเคลื่อนไหวของฟีเจอร์นี้แยกออกจากชุด golden-test screenshot เดิมโดยเจตนา** —
  `docs/ai-demo/ai-assistant-demo.gif` และ `docs/ai-demo/ai-assistant-live.png` capture มาจากการใช้งาน
  จริงผ่าน `ANTHROPIC_API_KEY` จริง (ไม่ใช่ fake client แบบในเทสต์) จึง **ไม่ reproducible แบบ
  byte-ต่อ-byte** เหมือนภาพใน `docs/screenshots/` ที่มาจาก golden test ที่ freeze เวลาไว้ — ตั้งใจไม่ปน
  สองชุดนี้เข้าด้วยกัน (ไม่ใส่ในตาราง "📸 หน้าตาแอป" หลัก) แต่แยกเป็น callout ของตัวเองในหัวข้อเดียวกัน
  ของ README ทั้งสองภาษา พร้อมคำเตือนชัดเจนว่าต่างจากภาพชุดอื่นยังไง วิธี regenerate ใหม่ในอนาคต: (1)
  ตั้ง `ANTHROPIC_API_KEY` จริงใน `backend/.env`, (2) seed ออเดอร์ตัวอย่างย้อนหลังผ่าน HTTP API จริง
  (ไม่ใช่เขียนแถวลง DB ตรงๆ เพื่อให้ business logic ที่คิดยอด/แต้มทำงานจริง) แล้วปรับ `created_at` ย้อน
  หลังของออเดอร์/payment ที่ seed ไว้ให้กระจายหลายวันด้วย `better-sqlite3` (สคริปต์ตัวอย่างไม่ได้ commit
  ไว้ในนี้เพราะเป็นสคริปต์ครั้งเดียวเฉพาะข้อมูลเดโม ไม่ใช่ tooling ถาวรของโปรเจกต์), (3) ใช้ Playwright
  บันทึกหน้าจอ (`recordVideo`) เดินสคริปต์ล็อกอิน → เปิดแท็บผู้ช่วย AI → พิมพ์คำถามตัวอย่าง → รอคำตอบจริง
  แล้วแคปหน้าจอสุดท้ายไว้เป็น PNG ด้วย, (4) ตัดคลิปด้วย `ffmpeg` เฉพาะช่วงที่เกี่ยวกับผู้ช่วย AI แล้ว
  แปลงเป็น GIF ผ่าน palette generation (`palettegen`/`paletteuse`) เพื่อคุมขนาดไฟล์

**ข้อเสียที่ยอมรับ** — `docs/PaynEat-POS-Features-*.pdf`, `docs/video/*.mp4`, `docs/landing/og-image*.png`
และ `docs/landing/social-preview.png` **ไม่ได้ regenerate ในทิกเก็ตนี้** จึงยังโชว์เลขเก่า (558 เทสต์/
82 endpoints) ค้างอยู่ในสื่อเหล่านั้น ส่วน `docs/generator/content*.py` และ `docs/video/storyboard.py`
(ต้นทางของ PDF/วิดีโอ) **ตั้งใจไม่แก้เลขในไฟล์นี้ตอนนี้เช่นกัน** เพื่อให้ต้นทางยังตรงกับสื่อที่ deploy
อยู่จริง (ไม่ใช่ปล่อยเผลอ) — ผู้ที่ regenerate สื่อเหล่านี้ครั้งถัดไปควรใช้เลขที่ถูกต้อง ณ ตอนนั้น
(574 เทสต์ / 83 endpoints ณ ตอนที่เขียนทิกเก็ตนี้) README.md/README.en.md/PORTFOLIO-SUMMARY
(ไม่ใช่ไฟล์ที่ generate เป็น binary) อัปเดตให้ตรงแล้วตามปกติ

## 34. แก้ 3 บั๊กที่เจอจาก blind test จัดเต็มทั้งแอป (5 บทบาท + มือถือ)

**ปัญหา** — ทำ blind test แบบ end-to-end จริงบน build โหมดสาธิตล่าสุด (Playwright คลิกจริงทุกบทบาท
ทั้งเดสก์ท็อปและมือถือ) เจอ 3 อย่างที่เป็นบั๊ก/โค้ดเปราะจริง (ข้อที่ 4 ที่สงสัยว่าตัวกรอง audit log
ล้นจอโดยไม่มีคำใบ้ ตรวจซ้ำแล้วพบว่ามี `HorizontalFade` (fade เงาที่ขอบ) ครอบอยู่แล้วจริงๆ — เป็นการ
ตรวจสอบที่คลาดเคลื่อนของฉันเอง ไม่ใช่บั๊ก จึงไม่มีการแก้ไขในหัวข้อนี้)

**ที่เลือก**

- **Snackbar สำเร็จ/แจ้งเตือน (`AppDialogs.success/error/info`) บังปุ่มบน AppBar กดไม่ติด ~2-3 วิ** —
  ทดสอบจริง: กด "ยืนยันและส่งครัว" แล้วรีบกดปุ่มย้อนกลับทันที กดไม่ติดเลยจนกว่า snackbar จะหายเอง
  ตอนแรกลองแก้ด้วยการเพิ่ม `margin` บนให้พ้นแนว AppBar (`kToolbarHeight + 12`) แต่ทดสอบซ้ำแล้วปุ่ม
  ยังกดไม่ติดเหมือนเดิม — ไปอ่านซอร์สแพ็กเกจ `get` (`snackbar_controller.dart`) พบว่า snackbar ที่
  `isDismissible` (ค่าเริ่มต้นของ `Get.snackbar`) ห่อด้วย `Dismissible` ซึ่งพื้นที่รับสัมผัสจริงคือ
  กรอบนอกของ `Container(margin: ...)` ทั้งก้อน **รวม margin เข้าไปด้วย** ไม่ใช่แค่ตัวการ์ดที่มองเห็น
  แปลว่าต่อให้เว้น margin ให้พ้นสายตาแค่ไหน พื้นที่ดักสัมผัสก็ยังขยายตามลงมาเท่าเดิม (เริ่มจาก y=0
  ของจอเสมอสำหรับ `SnackPosition.TOP`) จึงแก้ที่ต้นตอจริง: ปิด `isDismissible: false` (ตัด
  `Dismissible`/gesture wrapper ออกไปเลย) เพราะ toast พวกนี้ auto-dismiss ตาม `duration` อยู่แล้ว
  ไม่มีเหตุผลต้องให้ปัดปิดเองด้วยซ้ำ — ยืนยันผลจริงด้วย Playwright: กดย้อนกลับได้ทันทีแม้ snackbar
  ยังค้างอยู่บนจอ คง `margin` ไว้ด้วยเหตุผลด้านสายตาอย่างเดียว (ไม่ให้ตัวการ์ดทับข้อความ AppBar)
- **`HomeBinding.destinationsForRole` ใช้ wildcard `_` เป็นชุดสิทธิ์ของ waiter โดยไม่ตั้งใจ** — เดิม
  ไม่มี branch `UserRole.waiter =>` ตรงๆ เลย ตกไปอยู่ใน `_ => const [tables, orders, kitchen, profile]`
  ซึ่งไปตรงกับที่ backend ตั้งใจอนุญาตจริง (`order.routes.js` มี
  `authorize('admin', 'manager', 'kitchen', 'waiter')` สำหรับแก้สถานะอาหาร — เผื่อร้านเล็กที่พนักงาน
  เสิร์ฟต้องช่วยดูครัวเอง) จึง**ไม่ใช่รูรั่วสิทธิ์** แต่เป็นโค้ดเปราะที่ไม่มีเทสต์คุมกรณีนี้เลย
  (`home_destinations_test.dart` เดิมเช็ค waiter แค่ว่าไม่เห็นเมนูจัดการร้าน ไม่เคยเช็คเรื่องครัว)
  เปลี่ยนเป็น `UserRole.waiter =>` ชัดเจน และเปลี่ยน wildcard ที่เหลือให้ fail-safe ไปทาง**จำกัดสิทธิ์
  สุด** (`const [profile]`) แทนที่จะเดาสิทธิ์กว้างๆ ให้ role ที่ไม่รู้จัก/พิมพ์ผิดโดยไม่ตั้งใจ — เพิ่ม
  เทสต์ยืนยันทั้งสองเคส
- **หน้าบัญชีโชว์การ์ด "การเชื่อมต่อ" (`ไม่ได้เชื่อมต่อเรียลไทม์` + `http://localhost:3000`) แม้ใน
  โหมดสาธิต** — คนที่มาลองเดโมสาธารณะบน GitHub Pages ไม่มี backend จริงให้เชื่อมต่ออยู่แล้ว เห็น URL
  localhost ที่ไม่มีความหมายกับตัวเองพร้อมจุดสีเทาค้างตลอด เสี่ยงเข้าใจผิดว่าเว็บพัง — ครอบทั้งการ์ด
  ด้วย `if (!AppConfig.demoMode)`

**ข้อเสียที่ยอมรับ** — ไม่ได้เขียน widget test แยกสำหรับการซ่อนการ์ด "การเชื่อมต่อ" ในโหมดสาธิต เพราะ
`AppConfig.demoMode` เป็นค่าคงที่ตอน compile (`bool.fromEnvironment`) เปลี่ยนค่าระหว่างรันเทสต์เดียว
ไม่ได้ และเป็น conditional ธรรมดาที่ตรวจสอบได้ตรงไปตรงมาจากโค้ด ตรงกับแนวทางเดิมของโปรเจกต์ที่ไม่เขียน
widget test แบบ pump หน้าเต็มสำหรับทุก branch แบบนี้ (ดู `docs/CODING_STANDARDS.md` หัวข้อ 6.2)

## 35. Ticket 12 — Export รายงาน (CSV) + Z-report ปิดกะ/ปิดวัน

**ปัญหา** — หน้ารายงานดูได้แค่ในแอป ไม่มีทาง export ให้ฝ่ายบัญชี และไม่มีรายงานสรุปปิดกะ/ปิดวัน
(Z-report) แบบมาตรฐานที่ POS ทั่วไปต้องมี (`docs/tickets/12-report-export.md`)

**ที่เลือก**

- **Format ไฟล์: CSV เท่านั้น ไม่ทำ Excel (`.xlsx`)/PDF** — ทิกเก็ตยอมรับ "CSV/Excel อย่างน้อย 1
  format" และ "PDF หรือเทียบเท่า" — เลือก CSV เพราะ infra มีอยู่แล้วทั้งฝั่ง backend
  (`backend/src/core/csv.js`) และ Flutter (`app/lib/core/utils/csv.dart`, ใช้กับ audit log export
  ตั้งแต่ ticket 14) เปิดกับ Excel ได้ตรงอยู่แล้ว (มี UTF-8 BOM กันอักษรไทยเพี้ยน) ไม่ต้องเพิ่ม
  dependency ใหม่ (`xlsx`/`pdfkit` ฯลฯ) ทั้งสองฝั่ง
- **Z-report ต่อกะ vs ต่อวัน แยกกันชัดเจน ไม่ใช่ endpoint เดียวกัน** — ต่อกะ (`zReportByShift`)
  ผูกกับกะเดียวจึงมีกระทบยอดเงินสด (เงินตั้งต้น/ที่คาดไว้/ที่นับได้จริง/ส่วนต่าง) ได้แม่นยำ ต่อวัน
  (`zReportByDate`) รวมทุกกะของวันนั้นเข้าด้วยกัน **ไม่มี**กระทบยอดเงินสด เพราะอาจมีหลายกะ/
  หลายแคชเชียร์ปนกันในวันเดียว กระทบยอดเงินสดระดับวันจึงไม่มีความหมาย (ให้ผู้ใช้ดู breakdown
  ต่อกะแทนถ้าต้องการกระทบยอด)
- **Z-report ต่อกะคิดจาก `payments.shift_id` ไม่ใช่วันที่เปิดออเดอร์** — ออเดอร์อาจเปิดค้างข้ามกะได้
  (เช่น เปิดตอนกะเช้ายังไม่ปิดบิล พอถึงกะบ่ายค่อยจ่าย) การ join ผ่าน `shift_id` ของแถวการชำระเงินจึง
  ถูกต้องกว่าการเทียบวันที่เปิดออเดอร์ (คอลัมน์นี้มีอยู่แล้วจาก ticket 01 — ไม่ต้อง migrate เพิ่ม)
  รองรับ split-bill ถูกต้องด้วยเพราะนับจาก payment แต่ละแถว ไม่ใช่ order เดียวทั้งก้อน
- **UI ฝั่ง cashier: ปุ่ม "ดูใบสรุปปิดกะ" เปิดไม่ได้สำหรับกะที่ยังเปิดอยู่** — แม้ backend endpoint
  จะเรียกได้กับกะสถานะไหนก็ตาม (ไม่ error) แต่ยอดกระทบเงินสด (`expectedCash`/`countedCash`/
  `variance`) ยังเป็น `null` จนกว่าจะปิดกะ โชว์ ฿0.00 ให้ตอนกะยังไม่ปิดจะดูเหมือนกะสมดุลอยู่แล้วทั้งที่
  ยังไม่ได้กระทบยอดจริง เข้าใจผิดได้ง่าย จึง disable ปุ่มที่ `_HistoryTile` (`onTap: isOpen ? null : ...`)
  ไว้ก่อน ให้กดดูได้เฉพาะกะที่ปิดแล้วเท่านั้น
- **Export CSV ของ Demo Mode สร้างที่ฝั่ง Dart ล้วน ไม่พึ่ง backend** — เหมือนรูปแบบเดิมของ audit log
  (ticket 14): `demo_store_reports.dart` เพิ่มเมธอด `exportSummaryCsv`/`exportTopItemsCsv`/
  `exportSalesByDayCsv`/`exportZReportCsv` ที่ประกอบแถว label/value ให้ตรงกับฝั่ง backend
  (`report.service.js`) ทุกบรรทัดแล้วเรียก `toCsv()` ตัวเดียวกับที่ audit log ใช้ — คนที่มาลองเดโม
  สาธารณะ (ไม่มี backend จริง) ยัง export ได้ปกติ

**ข้อเสียที่ยอมรับ**

- **ไม่มีเทสต์ครอบคลุมกรณี `exportCsv()`/`exportZReportCsv()` สำเร็จจริงระดับ controller** — ทั้งสอง
  เมธอดเรียก `AppDialogs.error`/`downloadCsv` ตรงๆ ซึ่งต้องมี `GetMaterialApp` ที่ pump จริงถึงจะทดสอบ
  ได้ปลอดภัย (ดู `docs/CODING_STANDARDS.md` หัวข้อ 6.2) — ตรงกับรูปแบบเดิมที่
  `audit_log_controller_test.dart` ก็ไม่ครอบคลุม `exportCsv()` เช่นกัน ทดสอบเฉพาะตรรกะ/state ที่ไม่พึ่ง
  การนำทาง/snackbar
- **ยังไม่มีปุ่มดู Z-report จากกะที่ยังเปิดอยู่แบบ "พรีวิวระหว่างกะ"** — ตัดสินใจตั้งใจ (ดูด้านบน)
  ไม่ใช่ของค้าง แต่ถ้าในอนาคตอยากได้ตัวเลขยอดขายสดระหว่างกะโดยไม่ต้องกระทบเงินสด ให้แยก endpoint/UI
  ใหม่ต่างหาก ไม่ใช้ Z-report component เดิม เพื่อไม่ให้ผู้ใช้สับสนกับกระทบยอดเงินสดที่ยังไม่เกิดขึ้นจริง

## 36. Ticket 11 — Multi-branch/multi-store support

**ปัญหา** — ระบบตั้งใจ scope ไว้แค่สาขาเดียวตั้งแต่ต้น ไม่มี table `branches`, ทุก entity ไม่รู้จัก
แนวคิดสาขาเลย ทิกเก็ตนี้เองก็เตือนไว้ว่า "กระทบ architecture มากที่สุดในบรรดา ticket ทั้งหมด ควร
grill กับ PO ก่อนเริ่ม" (`docs/tickets/11-multi-branch.md`) จึงตัดสินใจ scope แบบ pragmatic MVP
ที่ตอบ acceptance criteria ครบ แต่เลื่อนความลึกบางจุดออกไปอย่างมีสติ

**ที่เลือก**

- **branch_id ผูกแค่ 4 entity: `dining_tables`, `menu_items`, `orders`, `ingredients`** — ตั้งใจไม่ผูก
  `promotions`/`customers`/`categories`/`options`/`settings`/`shifts`/`payments`/`refunds`/
  `tax_invoices`/`audit_logs` เพราะเป็นแนวคิดระดับเชน/องค์กรที่สมเหตุสมผลให้ใช้ร่วมกันทุกสาขา
  (โปรโมชันทั่วประเทศ, ลูกค้าคนเดียวกันสะสมแต้มข้ามสาขาได้, VAT/service charge/PromptPay ID ตั้งค่า
  รวมศูนย์) — รายงาน (`reports`) ยัง scope ได้เพราะ join ผ่าน `orders.branch_id` ได้อยู่แล้วโดยไม่ต้อง
  เพิ่มคอลัมน์ใหม่ ส่วน "ตั้งค่าต่อสาขา" (เช่น VAT ไม่เท่ากันแต่ละสาขา) ตั้งใจเลื่อนไปเฟสถัดไป
- **ไม่ทำ migration SQLite → PostgreSQL** — ทิกเก็ตแนะนำให้พิจารณาแต่ไม่ได้บังคับใน acceptance
  criteria repository layer (`*.repository.js`) แยกจาก service ไว้แล้วตั้งแต่ต้น จึงยังเปลี่ยนได้ไม่ยาก
  ถ้าต้องขยายจริงในอนาคต (ตามที่ตั้งใจไว้ตั้งแต่แรกดู FEATURE-GAP-ANALYSIS #11)
- **JWT พก `branchId` เป็นสาขาที่ "กำลังทำงานอยู่" ของ token นั้น (ไม่ใช่ property ถาวรของ user)** —
  `branchId: null` เป็นไปได้เฉพาะ `admin` เท่านั้น หมายถึงโหมด "ทุกสาขา" (ตอบ acceptance criteria
  "owner/admin ระดับองค์กรดูรายงานสรุปรวมทุกสาขาได้") คนอื่นมี `branchId` เป็นตัวเลขเสมอ
- **`user_branches` เป็น many-to-many แต่ admin ไม่ต้องมีแถวในนั้นเลยก็เข้าได้ทุกสาขา (bypass ตาม
  role)** — กันปัญหา "สร้างสาขาใหม่แล้วลืมเพิ่มสิทธิ์ admin" ซึ่งเป็นความผิดพลาดที่เกิดง่ายถ้าบังคับ
  ให้ admin ต้องมีแถวชัดเจนเหมือน role อื่น — `hasAccess()`/`listForUser()` เช็ค role ก่อนเสมอ
- **Login: ถ้ามีสาขาเดียวหรือเป็น admin → ได้ token ใช้งานได้ทันที ไม่ต้องเลือก — มีมากกว่า 1 สาขา
  (role อื่นที่ไม่ใช่ admin) → ได้ `pendingToken` (อายุ 5 นาที ใช้เรียกได้แค่
  `POST /auth/select-branch`) กับรายชื่อสาขาให้เลือกแทน** — **admin เข้าสาขาแรกอัตโนมัติเสมอแม้จะมี
  สิทธิ์เห็นได้ทุกสาขา ไม่ต้องเลือกก่อนเหมือน role อื่น** เหตุผลหลักคือความเข้ากันได้ย้อนหลัง:
  โปรเจกต์นี้มีเทสต์เก่ากว่า 200 เคสที่ `login('admin', ...)` แล้วใช้ token ต่อทันทีโดยไม่คาดคิดว่าจะ
  เจอ branch picker — ถ้าให้ admin เห็น branch picker ทันทีที่มี ≥2 สาขา (ซึ่งเป็นจริงตั้งแต่ seed มี
  2 สาขา) จะพังเทสต์เดิมทั้งหมด จึงออกแบบให้ admin "auto-select สาขาแรก" (คือสาขาที่ id น้อยสุด =
  สาขาเดิมที่มีข้อมูลทั้งหมดก่อนทิกเก็ตนี้) แล้วค่อยสลับไปสาขาอื่น/โหมดทุกสาขาทีหลังเองผ่าน
  `POST /auth/select-branch` — ผลข้างเคียงที่ยอมรับได้: เทสต์เดิมทั้งหมด (263 เคส) ผ่านโดยไม่ต้องแก้
  แม้แต่บรรทัดเดียว เพราะพฤติกรรม login เดิมของทุกบัญชี seed ไม่เปลี่ยนเลย
- **`POST /auth/select-branch` endpoint เดียวใช้ได้ทั้ง 2 สถานการณ์** — (1) แลก pendingToken เป็น
  token ใช้งานจริงครั้งแรกหลัง login และ (2) สลับสาขาภายหลังตอนใช้งานปกติ (ส่ง token เดิมที่ login
  แล้วเข้ามาแทน) — ผ่าน middleware ใหม่ `authenticateForBranchSelection` ที่รับ token ได้ทั้ง 2 แบบ
  แยกจาก `authenticate` ปกติที่ปฏิเสธ pendingToken ทันที (กันเอาไปเรียก endpoint อื่นก่อนเลือกสาขา)
- **`authenticate` เช็คสิทธิ์สาขาจาก DB ทุก request เหมือนที่เช็ค `is_active`/role ของ user อยู่แล้ว
  ไม่ใช่เชื่อแค่ตอน login** — ถอดสิทธิ์สาขาออกจาก `user_branches` หรือปิดสาขา (`is_active = 0`) มีผล
  ทันทีโดยไม่ต้องรอ token หมดอายุ (เหมือนหลักการเดียวกับ security review เดิม) — สาขาต้อง active อยู่
  เสมอแม้แต่ admin (ปิดสาขาแล้วต้องปิดจริงกับทุกคนรวม admin ด้วย ไม่ใช่แค่คนทั่วไป)
- **สมัครพนักงานใหม่ (`POST /users`) ผูกสาขาอัตโนมัติจากสาขาที่ผู้สร้างกำลังทำงานอยู่
  (`req.branchId`)** — ต้องทำ เพราะพนักงานที่ไม่มีสิทธิ์เข้าสาขาไหนเลยจะ login ไม่ได้ตลอดไป
  (`branches.length === 0` ถูกปฏิเสธที่ login) — ถ้าผู้สร้างเป็น admin ในโหมด "ทุกสาขา"
  (`req.branchId === null`) ต้องระบุ `branchId` มาทาง payload เอง (`resolveBranchIdForWrite` helper
  ใน `core/branchScope.js` ใช้ pattern เดียวกันกับตอนสร้าง table/menu item/ingredient)
- **Migration ฐานข้อมูลเดิม (ก่อนทิกเก็ตนี้) ไม่เสียข้อมูล** — `migrate.js#backfillDefaultBranch`
  ตรวจว่ามีแถวเก่าที่ `branch_id IS NULL` ในตารางที่ scope ไว้จริงหรือไม่ ถ้ามีจะสร้างสาขา fallback
  ชื่อ "สาขาหลัก" ให้อัตโนมัติแล้ว backfill ทุกแถวเดิม + ให้สิทธิ์ user เดิมทุกคนเข้าสาขานั้น —
  ฐานข้อมูลใหม่ล้วน (ตารางยังว่างตอน migrate ทำงาน) จะไม่สร้างสาขานี้ขึ้นมาเลย ปล่อยให้ `seed.js`
  สร้างสาขาจริง 2 สาขาแทน (กันสาขา "หลัก" ที่ไม่มีความหมายมาปนกับสาขาสาธิตในโหมดเดโม)
- **Seed 2 สาขา: "สาขาสุขุมวิท" (ข้อมูลเดิมทั้งหมดก่อนทิกเก็ตนี้) และ "สาขาทองหล่อ" (เมนู/โต๊ะ/
  วัตถุดิบชุดใหม่ธีมซีฟู้ด/ปิ้งย่างของตัวเอง)** — ให้เห็นชัดว่าสลับสาขาแล้วข้อมูลเปลี่ยนจริง ไม่ใช่
  label เฉยๆ — ผู้ใช้ seed เดิมทุกคน (`admin`/`manager`/`waiter1`/`kitchen`/`cashier`) ยังมีสิทธิ์แค่
  สาขาสุขุมวิทเหมือนเดิมทุกประการ (ไม่กระทบพฤติกรรมเดิมเลย) ยกเว้น `waiter2` ที่ตั้งใจให้มีสิทธิ์ทั้ง
  2 สาขาเป็นตัวอย่างเดียวที่จะเจอหน้าเลือกสาขาตอน login (เลือกเพราะเดิม `waiter2` ไม่ได้ถูกใช้ในเทสต์
  ใดๆ ที่คาดหวัง login สำเร็จทันที จึงเปลี่ยนแบบนี้ได้โดยไม่มีความเสี่ยง)

**ข้อเสียที่ยอมรับ**

- **ไม่ได้ตรวจว่า id ที่ระบุ (เช่น `tableId` ตอนเปิดออเดอร์) เป็นของสาขาที่ผู้ใช้กำลังทำงานอยู่จริง** —
  scope เฉพาะ list/create (การมองเห็น + การกำหนดสาขาให้ข้อมูลใหม่) ไม่ได้ทำ cross-branch guard เต็ม
  รูปแบบสำหรับ update/delete/get-by-id ทุกจุด เพราะกระทบ endpoint จำนวนมากและ acceptance criteria
  ("ข้อมูล/รายงานแยกตามสาขาไม่ปนกัน") เน้นที่การแสดงผล/รายงานไม่ปนกันเป็นหลัก ไม่ใช่การเดา id
  ข้ามสาขา — ถ้าจะทำจริงจังต้องเพิ่ม guard "แถวนี้ branch_id ตรงกับ req.branchId ไหม" ในทุก
  getById/update/remove ของ 4 module ที่ scope ไว้
- **ชื่อโต๊ะ (`dining_tables.name`) ยังต้องไม่ซ้ำกันทั้งเชน ไม่ใช่แค่ในสาขาเดียวกัน** — คอลัมน์มี
  `UNIQUE` เดี่ยวมาตั้งแต่ schema เดิม การเปลี่ยนเป็น composite `UNIQUE(name, branch_id)` ต้อง
  recreate ตาราง (SQLite ALTER TABLE แก้ constraint ตรงๆ ไม่ได้) จึงเลื่อนไว้ก่อน — ถ้าจะใช้ชื่อโต๊ะซ้ำ
  กันข้ามสาขาจริงต้องตั้งชื่อแยกกันเอง (เช่นใส่ prefix สาขา) จนกว่าจะแก้ schema
- **เลขที่ออเดอร์ (`orders.code`) และเลขคิว takeaway ยังรันต่อเนื่องรวมทุกสาขา ไม่แยกตัวนับต่อสาขา** —
  ไม่ผิดหลักการ (ยังไม่ซ้ำกันแน่นอน) แค่ไม่ใช่เลขที่เริ่มจาก 1 ใหม่ทุกสาขาทุกวันตามที่ธุรกิจจริงบางที่
  อาจต้องการ — ไม่ใช่ของค้างตามที่ acceptance criteria กำหนด (ไม่ได้พูดถึงเรื่องนี้เลย)
- **ยังไม่มีหน้าจอ "จัดการสาขา" (สร้าง/แก้ไข/มอบสิทธิ์พนักงานเข้าสาขา) ใน Flutter** — backend มี
  endpoint ครบ (`GET/POST /branches`, `PATCH /branches/:id`) แต่ acceptance criteria ของทิกเก็ตนี้
  พูดถึงแค่ "เลือกสาขาตอน login" และ "ดูรายงานแยก/รวมสาขา" ไม่ได้บังคับให้มี CRUD UI สร้างสาขาใหม่ —
  ตอนนี้สาขามาจาก seed เท่านั้น เพิ่มสาขาจริงต้องเรียก API ตรงๆ หรือรอทำหน้าจัดการในเฟสถัดไป
- **การมอบ/ถอดสิทธิ์ผู้ใช้เข้าสาขา (`user_branches`) ยังไม่มี endpoint แยกให้เรียกใช้จาก Flutter** —
  `branchRepository.addUser`/`removeUser` มีแล้วฝั่ง backend (ใช้ตอนสร้างพนักงานใหม่อัตโนมัติ) แต่ยัง
  ไม่เปิด endpoint ให้แก้ไขสิทธิ์ของพนักงานที่มีอยู่แล้วภายหลัง (เช่น เพิ่มสาขาที่ 2 ให้พนักงานเก่า)
  ต้องแก้ตรงฐานข้อมูลเองถ้าจะทำตอนนี้

**Flutter — เลือกสาขาตอน login + สลับสาขาที่หน้าโปรไฟล์**

- **`AuthController.submitLogin()` ใช้ Dart 3 sealed class + pattern matching (`LoginSuccess` /
  `LoginNeedsBranchSelection`) แทนการเช็ค flag แบบ `if (result.needsBranchSelection)`** — บังคับให้
  ทุกจุดที่ consume ผลลัพธ์ login ต้องจัดการทั้ง 2 กรณีครบ (compile error ถ้าลืมเคสใดเคสหนึ่ง) —
  `pendingToken` ตั้งใจเก็บไว้แค่ตัวแปรใน `AuthController` (ไม่ผ่าน `StorageService.saveSession`
  เลย) เพราะถ้าเก็บเป็น session จริงจะเสี่ยงหลุดไปแนบเป็น `Authorization` header ของ request อื่น
  โดยไม่ตั้งใจ (ดูหัวข้อ "JWT พก branchId" ด้านบน — pendingToken ใช้ได้แค่
  `POST /auth/select-branch` เท่านั้น)
- **`ApiClient` เปลี่ยนจาก overwrite header `Authorization` เสมอ เป็นเซ็ตเฉพาะตอนที่ request ยังไม่มี
  header นี้มาก่อน** — จำเป็นเพราะการแลก pendingToken ต้องส่ง token ที่ยังไม่ใช่ session token
  ปัจจุบัน (`storage.token` ตอนนั้นอาจเป็น null หรือ token เก่า) การ override เฉพาะ request ทำให้
  เรียก `POST /auth/select-branch` ด้วย pendingToken/token ที่ระบุเองได้โดยไม่กระทบ request อื่นที่ยัง
  ใช้ mechanism อัตโนมัติเดิม
- **โหมดสาธิต (Demo Mode) ตั้งใจให้มีแค่สาขาเดียวเสมอ ไม่ทำ branch_id ให้ `demo_store_*` ทุกไฟล์เลย**
  — `DemoAuthDataSource.login()` จึงคืน `LoginSuccess` เสมอ (ไม่มี user เดโมคนไหนเจอหน้าเลือกสาขา)
  และ `listMyBranches()` คืนสาขาสมมติสาขาเดียว ("สาขาหลัก (สาธิต)") ไว้ให้ตรง interface เฉยๆ —
  เหตุผลหลักคือขอบเขตงาน: การทำ branch_id ให้ครบทุกโดเมนของ demo store (เมนู/โต๊ะ/วัตถุดิบ/ออเดอร์/
  รายงาน ทั้งหมดใน `lib/core/demo/`) เพื่อให้สลับสาขาแล้วเห็นข้อมูลเปลี่ยนจริงแบบ backend เป็นงานแยก
  ต่างหากที่ใหญ่พอๆ กับทำทิกเก็ตนี้ซ้ำอีกรอบในโดเมนที่ไม่ต้องพึ่ง HTTP เลย และผู้ที่มาลองเดโมสาธารณะ
  ไม่มีทางสร้าง user คนที่ 2 ที่มีสิทธิ์หลายสาขาเองได้อยู่แล้ว จึงไม่คุ้มที่จะสร้างภาพลวงตาสาขาที่ 2
  ที่กดแล้วไม่มีอะไรเปลี่ยนจริง — ตามแบบแผนเดิมของโปรเจกต์ที่ซ่อนของที่ backend-only ออกจากโหมดสาธิต
  (เทียบ ticket ก่อนหน้าที่ซ่อนการ์ด "การเชื่อมต่อ" ในหน้าโปรไฟล์เมื่อ demoMode)
- **หน้าโปรไฟล์เพิ่มการ์ด "สาขาปัจจุบัน" พร้อมปุ่ม "สลับสาขา" (bottom sheet เลือกจาก
  `AuthController.myBranches`) ซ่อนไปเลยเมื่อ `AppConfig.demoMode`** — เหตุผลเดียวกับข้อบนและกับ
  การ์ดเชื่อมต่อเรียลไทม์ที่ซ่อนอยู่แล้ว — ตัวเลือก "ทุกสาขา" ในชีทนี้โชว์เฉพาะ role `admin` เท่านั้น
  (ตรงกับกติกาฝั่ง backend ว่า `branchId: null` เลือกได้เฉพาะ admin)
- **ไม่เขียนเทสต์แยกสำหรับ `DemoAuthDataSource`** — โปรเจกต์นี้ไม่เคยมี unit test ระดับ
  `Demo*DataSource` เลยสักไฟล์ (เทสต์ demo mode ทั้งหมดอยู่ที่ระดับ `DemoStore` extension ใน
  `test/core/demo_store_test.dart`) และ logic สาขาใน `DemoAuthDataSource` เป็นแค่ค่าคงที่ตายตัว
  (ไม่มี state/เงื่อนไขให้ทดสอบ) จึงไม่สร้างรูปแบบเทสต์ใหม่ที่ไม่เคยมีมาก่อนสำหรับ coverage ที่แทบ
  ไม่มีความหมาย — ทดสอบ `AuthController` (`submitBranchSelection`/`switchBranch` guard clause,
  `loadMyBranches` success path) แทน ตาม `docs/CODING_STANDARDS.md` หัวข้อ 6.2

## 37. Ticket 17 — QR สั่งอาหารเอง (QR Self-Order)

**ปัญหา** — ลูกค้าต้องเรียกพนักงานทุกครั้งที่จะสั่งเพิ่ม ไม่มีทาง "สแกน QR ที่โต๊ะแล้วสั่งเอง" แบบที่
POS คู่แข่งในตลาดไทยส่วนใหญ่มีเป็นมาตรฐานแล้ว — ตัดสินใจทำ ticket นี้ทันทีหลังระบุเป็น 1 ใน 3 gap
ที่เหลือเทียบกับตลาด (ดู `docs/FEATURE-GAP-ANALYSIS.md` #17) เพราะทำได้เร็วที่สุดโดยไม่ต้องพึ่ง
บริการภายนอก ต่างจากเชื่อมแพลตฟอร์มเดลิเวอรีหรือ payment gateway อัตโนมัติ

**ที่เลือก**

- **Endpoint สาธารณะแยกโมดูลใหม่ทั้งหมด `backend/src/modules/public-order/` ไม่มี `authenticate`
  middleware เลยแม้แต่ตัวเดียว** — ความปลอดภัยพึ่ง "การครอบครอง qrToken" ล้วนๆ (capability URL
  pattern เดียวกับลิงก์แชร์ปฏิทิน) แทนการเช็คสิทธิ์แบบ role — เลือกแบบนี้แทนการเปิด endpoint เดิม
  ให้ไม่ต้อง login เพราะ endpoint เดิมทุกตัวออกแบบมาให้พนักงานเห็นข้อมูลเต็ม (PII ลูกค้า/พนักงาน)
  ซึ่งไม่เหมาะเปิดสาธารณะตรงๆ
- **`qr_token` เป็น `crypto.randomUUID()` เก็บแยกคอลัมน์ ไม่ใช้ `dining_tables.id` ตรงๆ ในลิงก์** —
  กัน enumeration attack (ไล่เดข `/order/1`, `/order/2`, ... เพื่อเห็นออเดอร์โต๊ะอื่น) — มี unique
  index คุมที่ DB level และ endpoint `PATCH /tables/:id/qr-token/regenerate` (เฉพาะ
  admin/manager) ให้ปิดลิงก์เก่าทันทีถ้าหลุด/ถูกถ่ายรูปแอบอ้างไป
- **`public-order.service.js` เรียกใช้ `orderService.create()`/`orderService.addItems()`/
  `categoryService.list()`/`menuService.list()` เดิมตรงๆ ไม่เขียน business logic ซ้ำเลย** — ได้
  ตัดสต๊อกอัตโนมัติ/คำนวณโปรโมชัน/ยิง Socket.IO event (`ORDER_CREATED`, `KITCHEN_TICKET`,
  `TABLE_UPDATED`) ฟรีโดยไม่ต้องแตะโค้ดเดิมแม้แต่บรรทัดเดียว — เหตุผลเดียวกับที่ทุก module เดิม
  แยก service ออกจาก controller/route ไว้ตั้งแต่ต้น (`docs/CODING_STANDARDS.md` หัวข้อ 4.2)
- **Actor สังเคราะห์ `SELF_ORDER_ACTOR = { id: null, name: 'ลูกค้า (สแกน QR สั่งเอง)' }` ส่งเป็น
  `user` param ให้ `orderService`** — audit log (ดู `docs/DECISIONS.md` #21/#25) ต้องระบุที่มาของ
  รายการที่ถูกเพิ่มเข้าออเดอร์เสมอ ไม่ปล่อยเป็น `null`/`undefined` เงียบๆ ซึ่งจะทำให้อ่าน log ย้อนหลัง
  แล้วสับสนว่าใครเป็นคนสั่ง
- **Rate limit เขียนเอง (`core/rateLimit.js`, fixed-window counter, 30 ครั้ง/5 นาทีต่อ qrToken)
  แทนใช้ npm package (เช่น `express-rate-limit`)** — ตามปรัชญาเดิมของโปรเจกต์ที่เขียนเองสำหรับ
  ปัญหาเล็ก (เทียบ `Result<T>` ข้อ 3, PromptPay TLV encoder ข้อ 26) endpoint นี้เป็นจุดเดียวในระบบ
  ทั้งหมดที่ไม่มี auth คุมเลย จึงจำเป็นต้องกันสแปม/DoS ระดับพื้นฐานไว้ก่อน แต่ไม่ต้องซับซ้อนถึงขั้น
  distributed rate limit (ระบบนี้รันเซิร์ฟเวอร์เดียว ไม่มี multi-instance ให้ sync state ข้ามเครื่อง)
- **`toPublicOrderPreview()` ตัด field ที่เป็น PII ของพนักงาน/ลูกค้าออกก่อนส่งกลับ (waiterName,
  customerName, customerPhone)** — DTO เดิมของ `orderService` ออกแบบมาให้พนักงานเห็นเต็ม
  ไม่เหมาะส่งตรงๆ ให้หน้าเว็บสาธารณะที่ใครก็เปิดได้ถ้ามีลิงก์
- **Flutter: โมดูลใหม่แยกทั้งหมด `features/self_order/` ไม่ผ่าน `SessionService`/`AuthController`
  เลยแม้แต่จุดเดียว** — reuse widget เดิมที่แยก "dumb" อยู่แล้ว (`MenuItemCard`,
  `CategoryFilterBar`, `OptionSelectionSheet`, `BillSummary`, `OrderItemTile`) และ model เดิมที่
  parse `fromJson` แบบ tolerant อยู่แล้ว (`OrderModel`, `MenuItemModel`, `CategoryModel`) ตรงๆ
  เพราะ response จาก public API เป็น subset/superset ของโครงสร้างเดิม
- **Route ใช้ GetX path parameter `AppRoutes.selfOrder = '/order/:qrToken'` อ่านผ่าน
  `Get.parameters['qrToken']` ใน `onInit()`** — ยืนยันแล้วว่า default hash-routing ของแอป (ไม่มี
  `setUrlStrategy` override ที่ไหน) ทำให้ลูกค้าเข้าลิงก์ตรงจาก QR ได้โดยไม่ผ่าน `SplashPage`/
  auth-gate เลย เพราะ GetX resolve URL fragment จาก browser ตรงๆ ตั้งแต่โหลดหน้าครั้งแรก
  `InitialBinding` เป็น composition root เดียวของทั้งแอปอยู่แล้ว (ลงทะเบียนทุก
  datasource/repository/usecase ตอน startup โดยไม่สนว่าหน้าไหนโหลดก่อน) จึง `Get.find()` ใน
  `SelfOrderController` ได้ทันทีโดยไม่ต้องมี bootstrap แยก
- **ลิงก์ QR สร้างจาก `AppConfig.selfOrderLink()` อ่าน `Uri.base` เองตอน build เป็นเว็บ (ไม่ต้อง
  ตั้งค่าอะไรเพิ่ม)** — `Uri.base.origin + Uri.base.path` ให้ origin+path ของแอปที่รันอยู่จริง
  (รวม base href ตอน deploy ขึ้น subpath เช่น GitHub Pages `/app/` โดยอัตโนมัติ เพราะ browser
  resolve `<base href>` ให้แล้วก่อนที่ Dart จะอ่านค่า) ตัด query/fragment ทิ้งเองจึงไม่ปนกับ hash
  route ปัจจุบันของหน้าที่กำลังเปิดอยู่ — เผื่อกรณี build เป็นแอป native (Android/iOS/desktop) ที่ไม่มี
  `Uri.base` ที่มีความหมายไว้ด้วย ผ่าน `--dart-define=SELF_ORDER_BASE_URL=...` ตอน build
- **หน้าจัดการโต๊ะเพิ่มปุ่ม "ดู QR สั่งอาหารเอง" ในชีทเดิม (bottom sheet ที่กดค้างที่การ์ดโต๊ะ) แทนสร้าง
  หน้าแยกใหม่** — ตำแหน่งเดิมที่พนักงานคุ้นเคยอยู่แล้วสำหรับ action เกี่ยวกับโต๊ะ (เปลี่ยนสถานะ) ปุ่ม
  "เปลี่ยน QR" ซ่อนไว้เฉพาะ `admin`/`manager` (`TableController.canManageQrToken`) mirror กับ
  `manager` middleware ฝั่ง backend ตรงๆ กันพนักงานเสิร์ฟ/แคชเชียร์กดแล้วเจอ 403 เฉยๆ

**ข้อเสียที่ยอมรับ**

- **ไม่พิมพ์ QR standee/table tent จริงจากในแอป** — แสดงภาพ QR ขนาดใหญ่ + ปุ่มคัดลอกลิงก์บนจอ
  เท่านั้น ร้านต้องถ่ายภาพหน้าจอ/ใช้เครื่องมือออกแบบภายนอกไปทำป้ายเอง ไม่เพิ่ม dependency ใหม่
  (`printing`/`pdf`) สำหรับงานที่ไม่ใช่ core ของ POS
- **ยังไม่มีการแจ้งเตือนพิเศษฝั่งพนักงานว่า "ออเดอร์นี้ลูกค้าสั่งเอง"** — ขึ้นที่ผังโต๊ะ/ครัวเหมือน
  ออเดอร์ปกติทุกประการ (แยกได้จาก audit log ที่ actor เป็น "ลูกค้า (สแกน QR สั่งเอง)" เท่านั้น) —
  acceptance criteria ต้องการแค่ "ขึ้นแบบ realtime" ไม่ได้บังคับ badge/แจ้งเตือนแยก
- **โหมด native (non-web) ที่ไม่ได้ตั้งค่า `SELF_ORDER_BASE_URL` จะได้ลิงก์ที่ชี้ไป
  `AppConfig.baseUrl` (backend API host) ซึ่งไม่ใช่หน้าเว็บลูกค้าเปิดได้จริง** — ยอมรับได้เพราะ
  ช่องทางใช้งานหลักของโปรเจกต์นี้ (demo/portfolio + deploy จริง) คือ Flutter Web เพียงอย่างเดียว
  ต้องตั้งค่า `--dart-define` เองถ้าจะสร้างแอป native ไปแสดง QR ให้ลูกค้าสแกนจริง

**ปรับเพิ่มหลังรีวิว UI/UX บนขนาดจอมือถือจริง (390×844)** — ถ่ายภาพหน้าจริงด้วย
`tool/screenshots/capture_test.dart` (เคส 35-37) แล้วพบปัญหาที่มองไม่เห็นตอนอ่านโค้ด:

- **ปิดปุ่มย้อนกลับบน AppBar ของหน้าลูกค้า (`automaticallyImplyLeading: false`)** — หน้านี้เป็น
  หน้าแรกที่ลูกค้าเปิดจากการสแกน QR ไม่มีหน้าก่อนหน้าให้กลับไป ปุ่มย้อนกลับมีแต่จะพาลูกค้าหลุดออกไป
  หน้าเข้าสู่ระบบของพนักงาน ซึ่งเป็นโซนที่ลูกค้าไม่ควรไปถึงตั้งแต่แรก
- **ใช้ `CartLine` ใน `features/order/domain/entities/` ตัวเดิมของตะกร้าพนักงาน แทนการเขียน
  `SelfOrderCartLine` คู่ขนานขึ้นมาใหม่ (ลบทิ้งแล้ว)** — ตอนแรกแยกคลาสเพราะคิดว่าเป็นคนละบริบท แต่
  พอเห็นตะกร้าจริงบนมือถือถึงรู้ว่าขาดสองอย่างที่ `CartLine` มีให้อยู่แล้ว: `signature` สำหรับรวม
  บรรทัดที่เมนู/ตัวเลือก/โน้ตเหมือนกัน (ลูกค้ากดการ์ดเมนูเดิมซ้ำเป็นเรื่องปกติมาก — เดิมได้บรรทัด
  "1x" ซ้ำกันเรื่อย ๆ ต่างจากตะกร้าพนักงานในแอปเดียวกันที่รวมให้อัตโนมัติ) และ `quantity` ที่แก้ได้
  สำหรับปุ่ม +/− — ได้ทั้งสองอย่างฟรีพร้อมลบโค้ดซ้ำทิ้งไปหนึ่งไฟล์
- **ใส่ `QuantityStepper` (ตัวเดียวกับแผ่นเลือกตัวเลือก) ในแต่ละบรรทัดของตะกร้า โดยกด − ตอนจำนวน 1
  = เอาบรรทัดออกไปเลย** — ไม่ต้องมีปุ่มถังขยะแยกอีกปุ่มให้กดพลาดบนจอเล็ก (เดิมมีแต่ปุ่ม × ลบทิ้ง
  อย่างเดียว ลูกค้าที่อยากได้ 2 จานต้องกลับไปกดการ์ดเมนูใหม่)
- **หัวข้อ AppBar เปลี่ยนจากชื่อโต๊ะเปล่า ๆ ("A1") เป็น "โต๊ะ A1" (`table_number_label` ที่มีอยู่แล้ว)
  + โชว์โซนเสมอ** — เดิมบรรทัดโซน/สาขาผูกกับ `branchName != null` ซึ่งเป็น null ตลอดในโหมดสาธิตและ
  ร้านสาขาเดียว ทำให้หายไปทั้งแถบ ลูกค้าที่เพิ่งสแกนจึงไม่มีอะไรยืนยันเลยว่าสแกนถูกโต๊ะ
- **เพิ่มประโยคบอกวิธีสั่ง 1 บรรทัดใต้หัวโต๊ะ ("แตะเมนูเพื่อใส่ตะกร้า...")** — ลูกค้าไม่ได้ผ่านการ
  อบรมแบบพนักงาน หน้าจอที่มีแต่ grid เมนูล้วน ๆ ไม่ได้บอกว่าแตะแล้วเกิดอะไรขึ้น
- **ใส่ขีดจับลาก (drag handle) หัวชีทตะกร้าและชีทออเดอร์ปัจจุบัน** — ให้รู้ว่าปัดลงปิดได้ ตรงกับ
  `OptionSelectionSheet` ที่มีอยู่แล้ว (เดิมชีทสองอันนี้ไม่มีทั้ง handle และปุ่มปิด) — ยกขึ้นเป็น
  `core/widgets/sheet_handle.dart` แล้วใช้ร่วมกับชีท QR ฝั่งพนักงานด้วย
- **แยก "ลิงก์เสีย" ออกจาก "เน็ตสะดุด" ที่หน้า error (`SelfOrderController.isLinkProblem`)** — เดิม
  ทุก error ใช้ `ErrorView` ตัวเดียวกันซึ่งฮาร์ดโค้ดไอคอน Wi-Fi ขาด ลูกค้าที่สแกน QR ที่ถูกเปลี่ยนไป
  แล้วจึงเห็นภาพว่า "เน็ตตัวเองมีปัญหา" แล้วกดลองใหม่วนไปเรื่อย ๆ ทั้งที่ไม่มีวันสำเร็จ — ตอนนี้ 404
  (token ไม่มีจริง/โต๊ะปิด/QR ถูกเปลี่ยน) ขึ้นไอคอน QR และ**ไม่มีปุ่มลองใหม่** ส่วน error อื่นยังมีปุ่ม
  ให้กดเหมือนเดิม (`ErrorView` รับพารามิเตอร์ `icon` เพิ่ม ค่าเริ่มต้นเดิมจึงไม่กระทบหน้าอื่นเลย)

**บั๊กของ GetX ที่ค้นพบระหว่างทดสอบ route parameter (ยอมรับไว้ ไม่แก้ในทิกเก็ตนี้)**

`ParseRouteTree._parseParams` ของ `get 4.7.3` ใช้ `paramsMatch![i + 1]!` โดยไม่เช็ค null ถ้า regex
ของ route ไม่แมตช์กับ path ดิบที่ส่งเข้ามาจะโยน "Null check operator used on a null value" ทันที —
ทดสอบแล้วพบว่า **token จริงทุกรูปแบบปลอดภัย** (`abc123`, `demo-table-1`, UUID เต็มจาก
`crypto.randomUUID()`) และ token ว่าง (`/order/`) ก็เข้ากรณี "ลิงก์ไม่ถูกต้อง" ของเราได้ปกติ — พังเฉพาะ
URL ที่มีอักขระนอก ASCII (เช่นพิมพ์ภาษาไทยเอง), ช่องว่าง, หรือสแลชเกินหนึ่งชั้น (`/order/a/b`) ซึ่ง
เกิดได้จากการพิมพ์ URL เองเท่านั้น ไม่ใช่จากการสแกน QR ที่ระบบสร้าง (เบราว์เซอร์ percent-encode
อักขระนอก ASCII ให้อยู่แล้วตอนกดเข้าจาก address bar) — ไม่ patch GetX และไม่เปลี่ยน route เป็น query
parameter เพราะจะทำให้ URL อ่านยากขึ้นทั้งที่เส้นทางจริงไม่เคยเจอ เอกสารทัวร์ใน README จึงแนะนำให้
ทดลองด้วยตัวอักษรภาษาอังกฤษแทน

**บั๊ก layout ที่เจอพ่วงมาจากการถ่ายภาพหน้าจอมือถือ (แก้แล้ว ไม่เกี่ยวกับ QR โดยตรง)**

การถ่ายภาพหน้าจอผู้จัดการบนขนาดมือถือครั้งแรก (เดิมหน้าภาพรวมถ่ายแต่ขนาดเว็บ) ทำให้เจอ overflow
2 จุดที่ค้างอยู่มานานโดยไม่มีใครเห็น:

- **`StatCard` ในหน้าภาพรวมล้น 12px** — กริดใช้ `childAspectRatio` 1.45 เท่ากับแท็บเล็ต แต่บนมือถือ
  การ์ดกว้างแค่ ~171px ขณะที่เนื้อหาข้างในสูงคงที่ → แยกค่าของมือถือเป็น 1.2 และให้คำอธิบายใต้ตัวเลข
  ตัดบรรทัดเดียว (`maxLines: 1` + `Flexible`) กันล้นซ้ำเมื่อผู้ใช้ขยายขนาดตัวอักษร
- **กราฟรายชั่วโมงล้น 7px** — `hourly_bar_chart.dart` กันที่ให้ตัวเลขกำกับหัวแท่งไว้ 20px แบบเหมารวม
  ซึ่งไม่พอสำหรับความสูงบรรทัดจริงของฟอนต์ไทย → เปลี่ยนเป็นค่าคงที่ `_labelSlot`/`_labelGap` ที่หัก
  ออกจากความสูงแท่งตรง ๆ และครอบตัวเลขด้วย `FittedBox` กันล้นแม้ตัวเลขยาวหรือขยายขนาดตัวอักษร

## 38. PO re-verify รอบใหม่ (2026-09-19) — "พร้อมให้คนโหลดไปใช้จริงหรือยัง แม้จะฟรี"

> หมายเหตุการรวมเอกสาร: บันทึกนี้เขียนขึ้นในอีก session หนึ่งโดยตั้งเลขไว้เป็น #35 แต่เลขนั้นถูกใช้
> ไปแล้วโดย ticket 12 จึงย้ายมาเป็น #38 ตอน merge และแก้ข้อค้นพบ 2 ข้อที่ภายหลังพบว่าไม่จริง

**คำถามจากผู้ใช้** — หลังทุก ticket วิกฤต/สำคัญขึ้นสถานะ ✅ ครบ ผู้ใช้ถามตรงๆ ว่าฟีเจอร์ตอนนี้ดีพอ
จะให้คนโหลดไปใช้จริงหรือยัง โดยเฉพาะกังวลว่าจะมีคนคิดว่า "ถึงจะฟรีแต่ฟีเจอร์ไม่พอ ยอมเสียเงินซื้อ POS
เจ้าอื่นดีกว่า" — คำสั่งเดิมคือห้ามเชื่อสถานะ ✅ ที่ติ๊กไว้เฉยๆ ต้องอ่านโค้ดจริงซ้ำเสมอ (บทเรียนจาก
ticket 16 ที่เคยติ๊ก ✅ ทั้งที่ PromptPay QR เป็นของปลอม)

**วิธีตรวจ** — แบ่งงานตรวจอิสระ 3 ทาง:

1. **สุ่มตรวจ 4 ticket ที่ติ๊ก ✅ ไว้แล้ว** (shift/cash reconciliation, refund, inventory/stock,
   tax invoice) อ่าน DB schema + backend service + Flutter UI จริงทีละจุด หา failure mode แบบ
   ticket 16 (endpoint มีแต่ UI ไม่เรียก, column มีแต่ไม่มีอะไรเขียน/อ่าน) — **ผล: PASS ทั้ง 4**
2. **ตรวจ #11 multi-branch และ #12 export รายงาน/Z-report** — **ผลตอนนั้นสรุปว่า #11 ยัง
   out-of-scope และ #12 ยังเป็น gap จริง ซึ่งภายหลังพบว่าไม่จริงทั้งคู่** ทั้งสองงานถูกทำเสร็จอยู่ก่อน
   แล้วบน branch `claude/pos-restaurant-project-3djpp6` (ticket 12 ที่ commit `4e2684d` มี
   `/reports/export/*` + `/reports/z-report/*` ครบ, ticket 11 มีทั้ง backend scoping และหน้าเลือก/
   สลับสาขาใน Flutter) — การตรวจครอบคลุมแค่โค้ดบน `main` ณ ตอนนั้น จึงมองไม่เห็น branch ที่ยัง
   active อยู่ **บทเรียน: ก่อนสรุปว่างานยังไม่ได้ทำ ต้องเช็ค branch ที่ยัง active ด้วย ไม่ใช่แค่ main**
   ผลพลอยได้คือมีการเขียน full spec ของ ticket 12 ซ้ำขึ้นมาอีกฉบับ ซึ่งเก็บไว้เป็นภาคผนวกใน
   `docs/tickets/12-report-export.md` เพราะบันทึก user stories/เหตุผลเชิงออกแบบไว้ครบ
3. **ตรวจ ticket 15 (AI ask-your-data)** — RBAC, quota, graceful fallback ตอนไม่มี API key, DI/
   navigation ครบจริง **แต่เจอบั๊กจริง 1 จุด**: `runConversation()` (`ai-assistant.service.js`) ถ้า
   โมเดิลตอบข้อความเฉยๆ โดยไม่เรียก tool ใดเลย (`toolUseBlocks.length === 0`) ในรอบที่ไม่ใช่รอบ
   สุดท้าย โค้ดเดิมรับข้อความดิบนั้นเป็นคำตอบสุดท้ายทันทีโดยไม่ผ่าน `submit_answer`/zod schema —
   ขัดกับกฎข้อ 6 ใน system prompt เอง ("ต้องเรียก submit_answer เสมอ") และขัด acceptance criteria
   ของ ticket 15 เองที่บอกว่า "ห้ามเดา/แต่งคำตอบ" ไม่มีเทสต์คุม path นี้มาก่อน

**ที่แก้ทันที (ไม่ใช่แค่บันทึกไว้เฉยๆ)** — บั๊กข้อ 3 กระทบ "จุดขาย" หลักของ ticket 15 โดยตรง (ความ
น่าเชื่อถือว่า AI ไม่เดาตัวเลข) จึงแก้ในเซสชันนั้นเลย: เปลี่ยนให้ข้อความดิบที่ไม่ผ่าน `submit_answer`
ถูกป้อนกลับเข้าลูปพร้อมข้อความเตือนแทนที่จะถูกส่งกลับผู้ใช้ รอบสุดท้ายที่บังคับ
`tool_choice: submit_answer` อยู่แล้วรับประกันว่าลูปจบเสมอ พร้อมเทสต์ยืนยัน

**คำตอบ PO ต่อคำถามเดิม "พร้อมให้คนโหลดไปใช้จริงหรือยัง"** — พร้อมสำหรับร้านเดี่ยว ครบทุกฟีเจอร์
critical/high ของ POS ทั่วไป (shift, refund, printer, offline, promotion, stock, tax invoice,
PromptPay QR, audit log ทั้งสองระดับ) verified จริงไม่ใช่แค่ติ๊ก และมี AI ask-your-data เป็นจุดต่างที่
คู่แข่งฟรี/เสียเงินส่วนใหญ่ในตลาดไทยยังไม่มี — ข้อกังวลเดิมที่ว่า #12 export รายงานเป็นจุดที่ทำให้คน
ที่ต้องส่งบัญชีจริงรู้สึก "ฟรีแต่ใช้ไม่สะดวก" **ปิดไปแล้ว** (ดู #35) เช่นเดียวกับ multi-branch (#36)

---

## 39. รองรับภาษาเกาหลี (2026-09-22) — ภาษาที่ 3 ของทั้งแอปและหน้า Landing

**ที่มา** — ไม่ใช่งานที่วางแผนไว้ล่วงหน้า แต่มาจากอีเมลของเจ้าของธุรกิจเกาหลีรายหนึ่งในกรุงเทพฯ
ที่เจอโปรเจกต์นี้บน GitHub แล้วติดต่อเข้ามา จุดตั้งต้นจึงต่างจากทุก ticket ก่อนหน้า: มีคนอ่านเกาหลี
เป็นภาษาแม่กำลังจะเปิดหน้านี้อ่านจริง คำแปลที่ "พอใช้ได้" จึงไม่ผ่าน

**ตัดสินใจ 3 ข้อ (ผู้ใช้เลือกเอง ไม่ใช่เดา)**

1. **ดีไซน์แบบเกาหลีเฉพาะหน้าเกาหลี** หน้าไทย/อังกฤษคงดีไซน์เดิมไว้ — หน้า `index.ko.html`
   จึงเป็นคนละโลกภาพกับอีกสองหน้าโดยตั้งใจ: พื้นสว่างตลอด เทาอมฟ้าเย็น การ์ดมุมมนเงานุ่ม
   ตามแบบเว็บบริการเกาหลีสมัยใหม่ ไม่ใช่งาน editorial สลับฉากมืด-สว่างแบบหน้าไทย
2. **ทำ locale เกาหลีในแอปจริง** ไม่ใช่แค่แปลหน้าเว็บ — ภาพประกอบในหน้าเกาหลีจึงเป็นภาพแอป
   ภาษาเกาหลีจริงที่ถ่ายด้วย golden test เหมือนอีกสองภาษา ไม่ใช่ภาพภาษาไทยที่เอามาใช้ซ้ำ
3. **ฟีเจอร์เฉพาะไทยเขียนตามจริง + วงเล็บอธิบาย** ไม่แปลงให้เป็นของเกาหลีและไม่ตัดทิ้ง —
   พร้อมเพย์ / ปี พ.ศ. บนใบกำกับภาษี / VAT 7% ยังเรียกตามชื่อจริง แต่มีกล่องอธิบายสั้น ๆ
   ต่อท้ายว่าคืออะไรและเทียบกับของเกาหลีตรงไหน (เช่น VAT ไทย 7% ต่างจากเกาหลี 10%)
   เพราะคนอ่านคือคนเกาหลี **ที่ทำร้านอยู่ในไทย** ต้องรู้ของจริงของไทย ไม่ใช่ของที่แปลงให้คุ้นตา

**สามบั๊กที่โผล่มาเพราะเพิ่มภาษาที่ 3 — ไม่ใช่แค่งานแปล**

- `PaynEatApp._initialLocale` เขียนเป็น `saved == 'en' ? อังกฤษ : ไทย` ซึ่งถูกตอนมีสองภาษา
  แต่พอมีภาษาที่ 3 ผู้ใช้ที่เลือกเกาหลีไว้จะเปิดแอปมาเจอภาษาไทยทุกครั้ง ทั้งที่คำแปลครบ 806 คีย์
  ไม่มีอะไรฟ้องเลยเพราะโค้ดยัง compile ผ่าน — แก้ด้วยการรวมไปที่ `LocaleService.localeOf`
  จุดเดียว และเขียนเทสต์ที่ **วนตาม `supportedLocales`** ไม่ใช่ไล่เขียนทีละภาษา เพิ่มภาษาที่ 4
  แล้วลืมแก้ตัวแปลง เทสต์จะแดงเองโดยไม่ต้องมีใครจำ
- ป้ายเมนูบน `NavigationRail` ขึ้นเป็นกล่องสี่เหลี่ยมทั้งแถบ เพราะ `TextStyle` ตรงนั้นระบุแค่
  `fontFamily` (NotoSansThai ซึ่งไม่มีกลิฟฮันกึล) ไม่ได้ระบุ `fontFamilyFallback` ด้วย
- กฎ lint `text_style_needs_font_family` เดิมรับ `fontFamily` **หรือ** `fontFamilyFallback`
  อย่างใดอย่างหนึ่ง ซึ่งคือช่องที่ปล่อยบั๊กข้างบนผ่านไปได้ — แก้เป็นต้องมีครบทั้งคู่
  (`fontFamily` อย่างเดียว = ไทยรอดเกาหลีเป็นกล่อง, `fallback` อย่างเดียว = เกาหลีรอด
  ไทยหลุดไปใช้ค่า default ของแพลตฟอร์ม)

**ฟอนต์ — ทำไมต้อง subset** NotoSansKR เต็มชุดใหญ่ ~6MB ต่อน้ำหนัก ซึ่งมากกว่าตัวแอปทั้งตัว
จึง subset เหลือเฉพาะ 401 ตัวอักษรที่คำแปลใช้จริง (4 น้ำหนักรวม 256KB) แล้วเขียน
`test/core/korean_font_coverage_test.dart` อ่านตาราง cmap ของไฟล์ TTF ตรง ๆ เพื่อกันไม่ให้
มีใครเพิ่มคำแปลที่ใช้ตัวอักษรนอก subset โดยไม่รู้ตัว — เทสต์นี้พิสูจน์แล้วว่าจับได้จริงด้วยการ
แทรกอักษร `쀍` เข้าไปทดสอบ

**ชื่อเมนูไม่แปลตามภาษา** ข้อมูลเมนูมีแค่ชื่อไทยกับชื่ออังกฤษ ไม่มีชื่อเกาหลี จึงเปลี่ยนเงื่อนไข
`MenuItem.displayName` จาก "เป็นอังกฤษ" เป็น "ไม่ใช่ไทย" (`LocaleService.prefersLatinNames`)
ผู้ใช้เกาหลีจะได้ชื่ออักษรละตินแทนอักษรไทยที่อ่านไม่ออก ส่วนชื่อบนตั๋วครัว/ใบเสร็จยังเป็นค่าที่
บันทึกไว้ตอนสั่ง ไม่ขยับตามภาษา เพราะเป็นหลักฐานทางการเงิน — หน้า Landing เกาหลีจึงบอกตรง ๆ
ว่าชื่อเมนูแสดงตามที่ร้านพิมพ์เข้าไป และภาพในหน้านั้นเป็นข้อมูลตัวอย่างร้านไทยจึงเห็นชื่อไทย

**ผู้ช่วย AI** ปลดล็อกให้ตอบภาษาเกาหลีได้ด้วย (กฎข้อ 5 ใน system prompt เดิมจำกัดไว้แค่
"ไทยหรืออังกฤษ") ไม่งั้นหน้าเกาหลีจะโฆษณาฟีเจอร์ที่ผู้อ่านใช้ไม่ได้จริง

**ตัวสลับภาษาบนมือถือ** `.links` ของหน้าไทย/อังกฤษถูกซ่อนทั้งก้อนที่จอแคบกว่า 620px และ
ลิงก์ภาษาเดิมอยู่ในก้อนนั้น แปลว่าก่อนหน้านี้คนที่เปิดจากมือถือ **หาทางไปหน้าภาษาอื่นไม่เจอเลย**
จึงแยกตัวสลับภาษาออกมาเป็นกลุ่มของตัวเองที่แสดงทุกขนาดจอ พร้อมโหลดฟอนต์แบบ subset
เฉพาะตัวอักษรของป้าย ("한국어" 3 ตัว / "ไทย" 3 ตัว ~2KB) เพื่อไม่ให้ป้ายกลายเป็นกล่องสี่เหลี่ยม
บนเครื่องที่ไม่มีฟอนต์ภาษานั้น

---

## 40. ข้อมูลสาธิตสามภาษา และภาพประกอบที่อ่านออกจริง (2026-09-22)

**ที่มา** — ผู้ใช้เปิดหน้าเกาหลีบนมือถือจริงแล้วทักกลับมาสองข้อ ซึ่งถูกทั้งคู่:

> "ภาพประกอบยังติดภาษาไทย เค้าอาจจะคิดว่าเรารองรับภาษาเกาหลีจริงไหม
> และภาพประกอบที่มาแปะ ไม่คมชัดเลย"

รอบก่อนหน้าผมสรุปว่า "ชื่อเมนูเป็นข้อมูลของร้าน ไม่ใช่ข้อความของระบบ จึงไม่แปล
แล้วเขียนอธิบายไว้ในหน้าเว็บแทน" — **ข้อสรุปนั้นผิดสำหรับหน้า Landing** เพราะคนอ่าน
เห็นภาพก่อนอ่านข้อความเสมอ ภาพที่เป็นอักษรไทยทั้งใบลบล้างคำอธิบายที่เขียนไว้ทิ้งหมด
และคำถามที่เกิดขึ้นในหัวคนอ่านไม่ใช่ "ทำไมชื่อเมนูเป็นไทย" แต่เป็น "ตกลงรองรับเกาหลีจริงไหม"

**ที่แก้ — ไม่ใช่แค่ทำภาพใหม่ แต่แก้ที่ต้นทาง**

- ข้อมูลสาธิตเป็นของเราเอง ไม่ใช่ข้อมูลที่ร้านจริงกรอก จึงแปลได้และ *ควร* แปล
  (ฟิลด์ `nameEn` มีมาตั้งแต่แรกด้วยเหตุผลเดียวกัน `nameKo` แค่ทำต่อให้ครบ)
  เพิ่มชื่อเกาหลีให้หมวดหมู่ 7 หมวด เมนู 24 รายการ โซนโต๊ะ 4 โซน และตัวเลือกเสริมทุกตัว
- **ระหว่างทางพบว่าตัวเลือกเสริมไม่มีแม้แต่ `nameEn`** — หน้าอังกฤษจึงแสดง
  "เผ็ดปกติ / ไข่ดาว" เป็นอักษรไทยมาตลอดโดยไม่มีใครสังเกต เติมให้ครบทั้งสามภาษา
  ภาษาเกาหลีเป็นตัวที่ทำให้เห็นบั๊กเก่าของภาษาอังกฤษ
- รวมกติกาการเลือกชื่อไว้ที่ `LocalizedName.pick` จุดเดียว (เมนู/หมวดหมู่/โซน
  ใช้กติกาเดียวกันเป๊ะ) ลำดับถอย: ภาษาปัจจุบัน → อังกฤษ → ไทย ห้ามคืนค่าว่าง
  เพราะข้อมูลที่ร้านจริงกรอกเองจะมีแค่ชื่อเดียว
- ชื่อรายการอาหาร "ประทับ" ลงออเดอร์ตอนสั่ง ไม่ได้อ่านจากเมนูสด ๆ ทุกครั้ง
  ตั๋วครัวจึงยังเป็นไทยแม้ UI เป็นเกาหลี — เพิ่ม `DemoNames.language` ให้ชั้นข้อมูล
  รู้ภาษาปัจจุบัน (เรียก `Get.locale` เองไม่ได้ ผิด CODING_STANDARDS §4.1)
  ส่วนชื่อที่ประทับไปแล้วยังไม่ย้อนกลับมาเปลี่ยน เพราะบิลคือหลักฐานทางการเงิน
- ชิปกรองโซนต้องแยก `key` (ค่าดิบใช้กรอง) ออกจาก `label` (ค่าที่แปลแล้วใช้โชว์)
  ถ้าเอา label ไปกรองด้วย พอสลับเป็นเกาหลีจะกรองไม่เจอโต๊ะสักตัว — มีเทสต์คุมไว้

**ความคมชัด — ปัญหาคือ "สเกล" ไม่ใช่ "พิกเซล"**

ภาพจอแท็บเล็ต/เว็บถูกย่อลงเหลือราว 28% เมื่อวางในคอลัมน์กว้าง 331px บนมือถือ
ตัวหนังสือในแอปจึงเหลือราว 3px ซึ่งเพิ่มความละเอียดเท่าไรก็อ่านไม่ออกอยู่ดี
จึงแก้สามชั้นพร้อมกัน:

1. ถ่าย golden ของจอมือถือที่ `devicePixelRatio: 3` (เดิม 2) — 780px → 1170px
2. ส่งออก WebP ที่ **2 เท่าของความกว้างที่แสดงจริง** (วัดจาก DOM ไม่ใช่เดา)
   คุณภาพ 90 — ภาพจอกว้างจาก 1180px เป็น 1832px
3. บนจอแคบกว่า 760px ภาพจอกว้างไม่ย่อ แต่คงขนาดที่อ่านออก (700px) แล้วให้
   เลื่อนดูแนวนอนในกรอบของตัวเอง พร้อมบรรทัดบอกวิธีดู — ตัวหน้าเว็บเองยังไม่เลื่อนข้าง
   (ตรวจแล้วที่ 360/390/834/1440/2560px: `scrollWidth - clientWidth` ของหน้า = 0
   ทุกความกว้าง ส่วนกรอบภาพ `scrollWidth > clientWidth` เฉพาะบนจอแคบตามที่ตั้งใจ)

**GIF ผู้ช่วย AI** — บันทึกจากการเรียก Claude API จริงครั้งเดียว ถ่ายใหม่เป็นเกาหลี
ต้องมี API key จึงยังเป็นภาษาไทย แก้ด้วยการถ่ายหน้าผู้ช่วย AI ภาษาเกาหลีเป็น golden
มาวางเป็นภาพหลักก่อน (เห็นคำถามตัวอย่างและช่องพิมพ์เป็นเกาหลีครบ) แล้ววาง GIF
ต่อท้ายพร้อมคำบรรยายที่บอกตรง ๆ ว่าเป็นตัวอย่างคำถามภาษาไทย

**ฟอนต์** — subset ขยายจาก 401 เป็น 502 ตัวอักษร (4 น้ำหนัก 256KB → 317KB)
และขยายเทสต์ `korean_font_coverage_test.dart` ให้ตรวจข้อมูลสาธิตด้วย ไม่ใช่แค่คำแปล UI
รอบแรกตรวจแค่คำแปล ชื่อเมนูภาษาเกาหลีจึงหลุดออกนอก subset ได้โดยไม่มีอะไรฟ้อง
(พิสูจน์แล้วว่าเทสต์ที่ขยายแล้วจับได้จริง ด้วยการแทรก `쀍` เข้าไปในชื่อหมวดหมู่)

---

## 41. ตรวจในมุมนักลงทุนเกาหลี แล้วแก้ทั้ง 6 ข้อ (2026-09-23)

**ที่มา** — ผู้ใช้ให้สวมบท "คนเกาหลีที่จะลงทุน" แล้วเปิดดูหน้าเว็บ README และแอปจริง
โดยเน้นว่า *"เกาหลี ภาพลักษณ์ของสินค้าจะมาก่อน มองในมุม business มากกว่าฟีเจอร์ดีแค่ไหน"*
ผลตรวจพบ 6 ข้อ และรอบนี้แก้ครบทั้งหมด

**สิ่งที่การตรวจแบบนี้เจอ แต่การตรวจแบบเดิมไม่เจอ**

ทุกรอบก่อนหน้าผมตรวจ "หน้าจอที่คนในร้านใช้" — ผังโต๊ะ ครัว เก็บเงิน แดชบอร์ด
แต่ **หน้าที่ตัดสินภาพลักษณ์ของร้านคือหน้าที่ลูกค้าเปิดจากมือถือตัวเองหลังสแกน QR**
ซึ่งไม่เคยถูกถ่ายเป็นภาษาเกาหลีเลยสักครั้ง พอถ่ายมาดูก็เจอปัญหาหนักที่สุดของทั้งชุดทันที

1. **ภาพแทนเมนูใช้ "อักษรตัวแรก" ของชื่อ** — ใช้ได้เฉพาะภาษาที่ตัวอักษรเดี่ยวยังดู
   เป็นลวดลาย ไทย "ข" จาก "ข้าวผัดกุ้ง" ผ่าน แต่เกาหลีได้ "돼" จาก "돼지고기 랏나"
   ซึ่งเป็นเศษคำ ไม่ใช่ตัวอักษร ซ้ำด้วยไล่สีชมพู-ส้ม-ฟ้าสดที่อ่านเป็นแอปเด็ก
   → `MenuPlaceholder` สัญลักษณ์ช้อนส้อมบนพื้นเรียบโทนอาหาร ไม่พึ่งตัวอักษรเลย
2. **ภาษาไทยหลุดเข้าหน้าลูกค้าและใบเสร็จ** → `displayZone` ที่หน้า QR,
   `nameKo` ของชื่อร้าน/พนักงาน, และรูปแบบวันที่เกาหลี `yyyy년 M월 d일`
3. **หน้าเว็บไม่มีราคาและช่องทางติดต่อเลยสักจุด** — ตรวจด้วย regex แล้วไม่พบคำว่า
   가격/요금/문의/연락처/이메일 เลย คนที่สนใจอ่านจบแล้วไม่รู้จะติดต่อใคร
   → เพิ่มหัวข้อ 09 도입 안내 พาดหัวว่า "얼마인가요?" ตอบตรง ๆ ว่าฟรี Apache-2.0
   ไม่มีค่าเช่ารายเดือน พร้อมช่องทาง GitHub Issues และ LinkedIn
4. **README เป็นภาษาไทยล้วน** — กดปุ่ม "GitHub에서 코드 보기" แล้วอ่านไม่ออกทั้งหน้า
   ความน่าเชื่อถือที่หน้า Landing สร้างมา 8 หัวข้อหายในคลิกเดียว
   → `README.ko.md` เขียนเจาะเนื้อหาที่คนตัดสินใจซื้อต้องการ (ไม่ได้แปล 1,209 บรรทัดทั้งดุ้น
   ซึ่งจะได้เอกสารยาวที่ไม่มีใครอ่าน) และบอกตรง ๆ ว่าเอกสารเทคนิคลึกอยู่ในฉบับไทย/อังกฤษ
5. **ไม่มีหัวข้อความปลอดภัยเลย** ทั้งที่ผ่าน OWASP Top 10 แก้ครบ 7 ช่องโหว่ และมี
   `SECURITY.md` อยู่แล้ว — มีของดีแต่ไม่ได้เอามาโชว์ ทั้งที่ B2B เกาหลีถามตั้งแต่ประชุมแรก
   → หัวข้อ 08 보안과 데이터 รวมถึง**ข้อเสียที่ต้องบอก**: โปรเจกต์เริ่มจากพอร์ตโฟลิโอ
   ค่าตั้งต้นบางอย่างไม่ปลอดภัยพอ แต่ "ไม่ได้ซ่อน กันไว้ด้วยโค้ด" — โหมด production
   ที่ยังใช้รหัสผ่านเดโมจะทำให้เซิร์ฟเวอร์ไม่ยอมสตาร์ต ซึ่งตรวจสอบได้จริง
6. **จังหวะประโยคเรียบเกินไป** — 49 ประโยคลงท้าย 습니다/입니다 หมดทุกประโยค
   ไม่มีคำถามสักข้อ ไม่ผิดไวยากรณ์แต่อ่านเหมือนคู่มือที่แปลมาอย่างดี ไม่ใช่เว็บที่
   คนเกาหลีเขียนเอง → แทรกวลีจบด้วยคำนามและคำถามใน 6 จุดที่เนื้อหาเรียกร้องจังหวะนั้นจริง
   (ไม่ได้ไล่เปลี่ยนให้ครบโควตา)

**สิ่งที่จงใจไม่แก้ พร้อมเหตุผล**

- **รูปแบบวันที่ของไทย/อังกฤษ** คงเป็น `d MMM yyyy` เหมือนเดิม — รูปแบบนี้ไปโผล่บน
  ใบเสร็จที่พิมพ์ออกเครื่องจริงและบน golden ทุกใบ การเปลี่ยนควรเป็นการตัดสินใจของ
  เจ้าของร้าน ไม่ใช่ผลพลอยได้ของงานเพิ่มภาษา
- **ที่อยู่ร้านและชื่อสาขา** คงเป็นภาษาไทยทุกภาษา และมีเทสต์บังคับไว้ — ใบกำกับภาษี
  ของไทยต้องแสดงที่อยู่ตามที่จดทะเบียน ต่อให้หน้าจอเป็นเกาหลีก็ตาม
- **avatar ชื่อคน** ยังใช้อักษรตัวแรก — เกาหลีแสดงนามสกุลตัวแรกใน avatar เป็นเรื่องปกติ
  (카카오/네이버 ก็ทำ) การไปแก้ด้วยจะเป็นการแก้เกินเหตุ
- **อีเมลส่วนตัวของเจ้าของโปรเจกต์** ไม่เอาขึ้นเว็บสาธารณะ ใช้ช่องทางที่เจ้าตัวเปิดเอง
  อยู่แล้ว (GitHub Issues, LinkedIn) และเว้นคอมเมนต์ไว้ในไฟล์ให้เติมช่องทางตรงเองทีหลัง

**บทเรียนเรื่องเทสต์ที่ได้จากรอบนี้**

เทสต์ครอบคลุม glyph ที่เขียนไว้รอบก่อนมีรูโหว่ 3 จุด จนตัวอักษรไปโผล่เป็นกล่องสี่เหลี่ยม
กลางใบเสร็จ (`할□니 부□`, `2026□ 9월`, `□지은`) — ชื่อร้านใช้คีย์ `storeName` ไม่ใช่ `name`,
ชื่อพนักงานอยู่ใน `users()` ซึ่งไม่ได้อยู่ในลิสต์ที่ไล่, และ `년/월/일` มาจาก pattern ของ
`DateFormat` ไม่ได้อยู่ในข้อมูลชุดไหนเลย **บทเรียนคือต้องไล่จาก "สิ่งที่วาดบนจอ" ไม่ใช่
"ไฟล์ที่นึกออก"** ตอนนี้เทสต์เรียก `Formatters` จริงเพื่อดึงตัวอักษรในรูปแบบวันที่มาตรวจด้วย

อีกบทเรียนหนึ่ง: ตอนพิสูจน์ `korean_leak_test.dart` ว่าจับได้จริง probe รอบแรกของผม
ยิงผิดฟิลด์ (`nameKo` แทนที่จะเป็นพารามิเตอร์ zone) แล้วเทสต์ไม่แดง ซึ่งเกือบทำให้สรุปผิดว่า
"เทสต์ใช้ไม่ได้" — ต้องยิง probe ให้ถูกจุดก่อนถึงจะสรุปเรื่องคุณภาพของเทสต์ได้

---

## 42. ภาพจอกว้างอ่านออกบนมือถือครบทั้งสามหน้า + ความละเอียดภาพหน้าไทย (2026-09-23)

ต่อจาก #40 ที่ทำให้เฉพาะหน้าเกาหลี — ผู้ใช้สั่งให้ทำกับหน้าไทยและอังกฤษด้วย

- **หน้าไทยส่งออกภาพใหม่ตามความกว้างที่แสดงจริง** ภาพเทียบปุ่มเดิมถูกขยาย 4 เท่า
  (แสดง 1169px จากต้นฉบับ 294px) จอครัวถูกขยายเล็กน้อย (1232 จาก 1180)
  ภาพมือถือใช้ความละเอียด golden เดิม 780px **ไม่ขยายเป็น 920** เพราะ golden ไทย
  ถ่ายที่ DPR 2 การขยายไม่ได้รายละเอียดเพิ่ม และบนหน้านี้แสดงสูงสุดแค่ 314px
  780px จึงคมพออยู่แล้ว (2.5 เท่า) ไฟล์โตขึ้นแค่ 38KB (245→283KB) เพราะเปลี่ยนเป็น WebP
- **ภาพจอกว้างบนจอแคบกว่า 760px เลื่อนดูแนวนอนในกรอบตัวเอง** ทั้ง 3 กรอบในหน้าไทย
  และอังกฤษ (จอครัว / แดชบอร์ด / GIF สาธิต AI) ใช้ CSS ชุดเดียวกันทั้งสองหน้าเพราะ
  ส่วน device frames ของสองหน้าเหมือนกันทุกตัวอักษร (ตรวจด้วย diff แล้ว)
- **บั๊กแฝงที่โผล่ระหว่างทาง** — รอบแรกทั้งหน้าเลื่อนข้างได้ 335px บนมือถือ เพราะกฎ
  `.pin{grid-template-columns:1fr}` บนจอแคบ `1fr` เปล่า ๆ คือ `minmax(auto,1fr)` ไม่ยอมหด
  ต่ำกว่าความกว้างของเนื้อหา พอกรอบจอครัวมีภาพกว้าง 700px คอลัมน์เลยขยายตาม
  (เดสก์ท็อปเขียน `minmax(0,1fr)` ถูกอยู่แล้ว มีแค่ media query ที่ตก) แก้เป็น `minmax(0,1fr)`
  จับได้เพราะเครื่องมือตรวจวัดทั้ง "หน้าเลื่อนข้างไหม" และ "กรอบเลื่อนได้กี่กรอบ" แยกกัน —
  ถ้าวัดแค่อย่างหลังจะเห็นว่า 2 ใน 3 กรอบเลื่อนได้แล้วเข้าใจว่าเกือบเสร็จ
- สีคำใบ้ใช้ `--steel-lt` บนพื้นมืด (6.52:1) และ `--steel` บนพื้นสว่าง (5.01:1)
  ถ้าใช้สีเดียวทั้งหน้า จุดบนพื้นสว่างจะเหลือ 2.76:1 — บทเรียนเดียวกับรอบแก้ `--steel`

## 43. ใบกำกับภาษี: "มูลค่าสินค้า/บริการ" คือฐานภาษี (ยอดก่อน VAT) ไม่ใช่ค่าอาหารล้วน (2026-09-23)

**ปัญหา** — เจอจากชุด E2E (#47): ใบกำกับภาษีเต็มรูปพิมพ์สามบรรทัดที่บวกกันไม่ลง เช่นบิล
อาหาร 275 + ค่าบริการ 10% 27.50 + VAT 7% ได้ยอดจ่ายจริง 323.68 แต่ใบกำกับภาษีพิมพ์
"มูลค่าสินค้า/บริการ 275.00 · ภาษีมูลค่าเพิ่ม 21.18 · รวมทั้งสิ้น 323.68" — ค่าบริการหายไปจาก
บรรทัดแรก ทั้งที่ VAT 21.18 คิดจาก 302.50 (อาหาร + ค่าบริการ) ไม่ใช่จาก 275 ที่พิมพ์อยู่ด้านบน
สาเหตุคือ `tax-invoice.service.js#issue` เก็บ `order.subtotal` (ค่าอาหารก่อนส่วนลด/ค่าบริการ)
ลงช่อง subtotal ของใบกำกับภาษี ไม่มีเทสต์ไหนเลยที่เช็คว่าสามบรรทัดบวกกันลง (เทสต์เดิมเช็คแค่ยอดรวม)

**ที่เลือก**

- **เก็บ subtotal = `order.total − order.vat`** ทั้ง backend และ Demo Mode — ฐานภาษีที่ถูกต้องเสมอ
  ไม่ว่าจะมีค่าบริการ ส่วนลด โปรโมชัน หรือเปิดโหมด VAT รวมในราคา (ทุกโหมด total − vat คือยอดก่อน
  VAT) ไม่ต้องเพิ่มคอลัมน์ใหม่ เพราะบนใบกำกับภาษีไม่จำเป็นต้องแยกค่าบริการออกมาเป็นบรรทัดของตัวเอง
  ขอแค่ "มูลค่า + ภาษี = รวม" และภาษีเป็น 7% ของมูลค่าที่พิมพ์ — ลูกค้านิติบุคคลที่เอาใบกำกับไปเคลม
  ภาษีซื้อ นักบัญชีจะตรวจตรงนี้เป็นอย่างแรก
- **ไม่แก้ใบที่ออกไปแล้วย้อนหลัง** — ใบกำกับภาษีเป็นเอกสารที่ snapshot ไว้ ณ ตอนออก (#19) ถ้าเคยออก
  ใบผิดไปแล้วให้ยกเลิกแล้วออกใหม่ตามขั้นตอนปกติ (ยังไม่มีร้านใช้งานจริง จึงไม่มีใบค้างให้ต้องไล่แก้)
- เทสต์กันถอยหลังทั้งสองฝั่ง: `backend/tests/tax-invoices.test.js` (บิล 100 → ฐานภาษี 110, VAT 7.70,
  รวม 117.70) และกลุ่ม tax invoices ใน `app/test/core/demo_store_test.dart`

## 44. เงินสดที่คืนลูกค้าหักออกจากลิ้นชักของกะที่ "เปิดอยู่ตอนคืน" (2026-09-23)

**ปัญหา** — เจอจากชุด E2E (#47): ยอดที่คาดว่าควรอยู่ในลิ้นชักตอนปิดกะ = เงินทอนตั้งต้น + เงินสดที่
รับเข้า **ไม่ได้หักเงินสดที่คืนลูกค้าออกไปเลย** ผู้จัดการคืนเงินสด 50 บาทจากลิ้นชัก แคชเชียร์นับเงิน
ได้ตรงตามจริงทุกบาท แต่ระบบบันทึกว่าเงิน "ขาด 50 บาท" — คือกล่าวหาแคชเชียร์ว่าเงินหายทั้งที่ไม่มีเงิน
หาย ticket 01 (กะ) กับ ticket 02 (คืนเงิน) ทำแยกกันคนละรอบ เทสต์แต่ละฝั่งผ่านหมด แต่ไม่มีเทสต์ไหน
ทำทั้งสองอย่างในกะเดียวกันแล้วปิดกะ ทั้งที่ ticket 02 เขียนเหตุผลไว้เองว่าคืนเงินที่ไม่มี flow ชัดเจน
"จะกระทบยอดขาย/รายงาน/**เงินสด**ผิดพลาด"

**ที่เลือก**

- **เพิ่ม `refunds.shift_id` = กะที่เปิดอยู่ตอนคืนเงิน ไม่ใช่กะของ payment เดิม** — บิลเมื่อเช้าที่
  ลูกค้ากลับมาขอคืนตอนบ่าย เงินออกจากลิ้นชักกะบ่าย กะเช้าที่ปิดไปแล้วต้องไม่ถูกคำนวณใหม่ย้อนหลัง
  (มีเทสต์ข้ามกะแยกไว้โดยเฉพาะ) เลียนแบบ `payments.shift_id` ที่มีอยู่แล้วจาก ticket 01
- **ยอดที่คาดไว้ = เงินทอนตั้งต้น + เงินสดที่รับ − เงินสดที่คืน** (`shift.service.js` +
  `shiftRepository.cashRefundedDuring`) นับเฉพาะรีฟันด์ของ payment ที่จ่ายเป็นเงินสด — คืนผ่าน
  QR/บัตร/โอนไม่แตะลิ้นชัก
- **คืนเงินสดต้องมีกะเปิดอยู่ (409) กฎเดียวกับตอนรับเงิน** — เงินสดออกจากลิ้นชักจริงจึงต้องมีกะให้ผูก
  ไม่งั้นหายไปจากการกระทบยอดเงียบ ๆ ส่วนคืนผ่านช่องทางอื่นไม่บังคับ แต่ยังผูกกะไว้ถ้ามีกะเปิดอยู่
  ข้อความ error เพิ่มครบสามภาษา (`payment_error_refund_shift_required`)
- **migration สร้าง index ใน `migrate.js` หลัง `addColumnIfMissing` ไม่ใช่ใน `schema.sql`** —
  `schema.sql` รันก่อนเสมอ ฐานข้อมูลเดิมที่ยังไม่มีคอลัมน์จะพังตั้งแต่บรรทัด CREATE INDEX ตรวจแล้ว
  ด้วยการจำลองฐานข้อมูลรุ่นเก่า (ไม่มีคอลัมน์ + มีรีฟันด์เก่า 1 แถว) แล้วรัน migrate สองรอบ: เพิ่ม
  คอลัมน์ได้ สร้าง index ได้ ไม่พังตอนรันซ้ำ รีฟันด์เก่ายังอยู่และได้ `shift_id = NULL` ซึ่งถูกต้อง
  (ไม่เคยถูกหักจากลิ้นชักกะไหนอยู่แล้ว)

**ข้อเสียที่ยอมรับ**

- `idx_payments_shift` จาก ticket 01 ยังประกาศอยู่ใน `schema.sql` — ฐานข้อมูลที่สร้างก่อน ticket 01
  จะอัปเกรดไม่ผ่านด้วยเหตุผลเดียวกับด้านบน ไม่แก้ในรอบนี้เพราะยังไม่มีฐานข้อมูลจริงรุ่นนั้นอยู่ที่ไหน
  แต่บันทึกไว้เป็นรูปแบบที่ห้ามทำซ้ำ

## 45. ไฟล์ CSV ที่ร้านดาวน์โหลดต้องมี UTF-8 BOM จริง — ตัวถอด UTF-8 ของ Dart ตัดทิ้ง (2026-09-23)

**ปัญหา** — เจอจากชุด E2E (#47): backend ใส่ BOM ให้ทุกไฟล์ CSV ถูกต้อง (ตรวจไบต์จริงได้
`EF BB BF`) แต่ `ApiClient.getText` รับเป็นข้อความผ่าน `ResponseType.plain` แล้ว**ตัวถอด UTF-8 ของ
Dart ตัด BOM หัวข้อความทิ้งเสมอ** (ยืนยันแล้วทั้ง `utf8.decode` และแบบ stream) ไฟล์ที่
`downloadCsv` เขียนลงเครื่องจึงไม่มี BOM → Excel เดาเป็น ANSI → ภาษาไทยเพี้ยน กระทบ export ทุกตัว
(สรุปยอดขาย, เมนูขายดี, ยอดรายวัน, Z-report ทั้งต่อกะและต่อวัน, audit log) แต่**โหมดสาธิตไม่เป็น**
เพราะสร้าง CSV ฝั่ง Dart เองพร้อม BOM ไม่ผ่าน `getText` — เว็บสาธิตสาธารณะจึงดูปกติทุกอย่าง
พังเฉพาะตอนต่อ backend จริง

**ที่เลือก**

- **แก้ที่ `getText` จุดเดียว: รับเป็นไบต์ ถอดเอง แล้วคืน BOM ถ้าไบต์ต้นฉบับมี** — ไม่เติม BOM เองถ้า
  เซิร์ฟเวอร์ไม่ได้ส่งมา ได้ข้อความตรงกับที่เซิร์ฟเวอร์ส่งทุกตัวอักษรตามที่ doc comment ของเมธอดนี้
  สัญญาไว้ตั้งแต่แรก ไม่ไปแก้ที่ `downloadCsv` เพราะจุดที่ข้อมูลหายคือชั้น network
- เทสต์เร็วใน `app/test/core/network/api_client_test.dart` ใช้ Dio adapter ปลอมคุมไบต์ที่ตอบกลับ
  (มี BOM / ไม่มี BOM / ไฟล์ว่าง) ให้จับได้ทุก push โดยไม่ต้องรอ job E2E

## 46. QR สั่งเอง: กด "ส่งเข้าครัว" แล้วต้องถึงครัวจริง + ลิงก์เสียทุกแบบต้องบอกว่าลิงก์เสีย (2026-09-23)

**ปัญหา** — เจอจากชุด E2E (#47) สองจุดในฟีเจอร์ ticket 17:

1. **ลูกค้ากด "ส่งเข้าครัว" แล้วหน้าจอขึ้น "ส่งออเดอร์เข้าครัวเรียบร้อยแล้ว" (ทั้งสามภาษา) แต่ครัวไม่เห็น**
   — ออเดอร์แรกของโต๊ะว่างถูกสร้างเป็นร่าง (`open`) ซึ่งคิวครัวไม่ดึงมาแสดง (ดูแค่ `in_kitchen`/
   `served`) ลูกค้าจะนั่งรออาหารที่ไม่มีใครทำจนกว่าพนักงานจะบังเอิญเห็นที่ผังโต๊ะแล้วกดส่งเอง ขัดกับ
   acceptance criteria ของ ticket 17 และ #37 ที่เขียนว่า "ขึ้นที่ผังโต๊ะ/ครัวเหมือนออเดอร์ปกติทุกประการ"
   เทสต์ backend เดิมยังล็อกพฤติกรรมนี้ไว้ด้วย (`assert status === 'open'` ในเทสต์ชื่อ "เปิดออเดอร์ใหม่
   ให้อัตโนมัติ" — เช็คตามที่เห็น ไม่ใช่ตามที่ออกแบบ ไม่มีบันทึกไหนบอกว่าตั้งใจให้พนักงานยืนยันก่อน)
2. **ลิงก์ QR ที่ถูกตัดท้าย (เช่นตอนส่งต่อทาง LINE) ขึ้นหน้า "ไม่มีอินเทอร์เน็ต" พร้อมปุ่มลองใหม่ที่ไม่มี
   วันสำเร็จ** — backend บังคับ token เป็น UUID จึงตอบ 400 ตั้งแต่ชั้น validate แต่หน้าลูกค้า (โค้ด
   รอบแก้ UX ก่อนหน้านี้เอง) นับเป็น "ลิงก์เสีย" แค่ 404

**ที่เลือก**

- **`public-order.service.js#addItems` เรียก `orderService.sendToKitchen` ต่อทันทีถ้าออเดอร์ยังเป็นร่าง**
  — ตัวเดียวกับที่พนักงานกด จึงได้ตัดสต๊อก + ยิง `KITCHEN_TICKET` ครบโดยไม่เขียน logic ซ้ำ ออเดอร์ที่อยู่
  ในครัวแล้วไม่ต้องส่งซ้ำ (`addItems` ของออเดอร์ที่ส่งครัวแล้วตัดสต๊อกและครัวเห็นทันทีอยู่แล้ว) mirror
  เดียวกันใน `DemoSelfOrderDataSource` แก้เทสต์ที่ล็อกไว้ให้เช็ค `in_kitchen` และเช็คคิวครัวจริง
- **ย้ายกฎ "ลิงก์เสีย" ไปเป็นฟังก์ชันใน domain (`isBrokenSelfOrderLink`) = 404 หรือ `ValidationFailure`**
  — input เดียวของคำขอนี้คือ token จาก URL ถ้า validate ไม่ผ่านจึงแปลว่าลิงก์เสียเท่านั้น controller กับ
  ชุด E2E ใช้ฟังก์ชันเดียวกัน E2E จึงพิสูจน์ได้ว่า failure จริงจาก backend ของลิงก์ทั้งสองแบบถูกจัดถูกช่อง

**ข้อเสียที่ยอมรับ**

- ถ้าพนักงานกำลังร่างออเดอร์ค้างไว้ที่โต๊ะนั้น (ยังไม่กดส่ง) แล้วลูกค้ากดส่งพร้อมกัน รายการที่พนักงาน
  ร่างไว้จะถูกส่งเข้าครัวไปด้วย — ยอมรับได้เพราะเป็นรายการของโต๊ะเดียวกันและเกิดได้ยาก (พนักงานยืนรับ
  ออเดอร์อยู่ที่โต๊ะ ลูกค้าไม่น่าสแกนสั่งเองในจังหวะเดียวกัน) ดีกว่าปล่อยให้ออเดอร์ของลูกค้าค้างเงียบ ๆ
- ยังไม่มีโหมด "ให้พนักงานยืนยันก่อนเข้าครัว" สำหรับร้านที่กลัวลูกค้าแกล้งสั่ง — ถ้าต้องการต้องเป็น
  การตั้งค่าของร้านแยกต่างหาก ไม่ใช่พฤติกรรมเริ่มต้นที่ขัดกับข้อความที่ลูกค้าเห็น

## 47. ชุด E2E: โค้ดแอปตัวจริงคุยกับ backend ตัวจริง (2026-09-23)

**ปัญหา** — ก่อนหน้านี้มีเทสต์ 659 ตัว แต่แบ่งเป็นสองโลกที่ไม่เคยเจอกัน: เทสต์ backend ยิง HTTP จริง
แต่เป็นฝั่ง JavaScript ล้วน ส่วนเทสต์ Flutter ทั้งหมดวิ่งบนโหมดสาธิต (`DemoStore` ฝั่ง Dart) **ไม่มี
เทสต์ไหนเลยที่ให้โค้ดฝั่งแอปอ่าน JSON ที่ backend ส่งมาจริง** ชื่อฟิลด์ไม่ตรงกันแค่ตัวเดียว หรือกฎธุรกิจ
ที่คนละ module ตีความไม่ตรงกัน จะผ่านเทสต์ครบทุกตัวแต่พังตอนร้านเปิดใช้จริง — ร้านจริงไม่ได้รัน
โหมดสาธิต (roadmap ใน README ค้างข้อนี้ไว้ตั้งแต่ต้นว่า "เทสต์ integration ฝั่ง Flutter ยิงกับ backend จริง")

**ที่เลือก**

- **`app/test_e2e/` แยกจาก `test/`** — ต้องมี Node + dependency ของ backend ด้วย ถ้าอยู่ใน `test/`
  คนที่รัน `flutter test` เฉย ๆ จะเจอเทสต์พังเพราะไม่ได้ติดตั้ง backend รันด้วย
  `cd app && flutter test test_e2e` และมี job แยกใน CI (`e2e`) ติดตั้งทั้งสองฝั่งในเครื่องเดียว
- **เปิด backend เป็น process จริง (`node src/server.js`) บนพอร์ตสุ่ม + ฐานข้อมูล SQLite ชั่วคราวใหม่
  ทุกไฟล์เทสต์** (`support/backend_process.dart`) — server migrate + seed เองตอนบูต จึงไม่มีข้อมูลค้าง
  ข้ามรอบ ไม่ชนกับ backend ที่เปิดใช้งานอยู่บนพอร์ต 3000 ตัด `ANTHROPIC_API_KEY` ออกเสมอกันเรียก
  Claude API จริงโดยไม่ตั้งใจ เทสต์ล้มจะพิมพ์ log ของ backend ออกมาให้เห็นต้นเหตุทันที
- **ประกอบ object graph ชุดเดียวกับที่แอปประกอบตอนเปิดเครื่อง** (`support/pos_device.dart`):
  `ApiClient(tokenProvider: () => storage.token)` → `XxxRemoteDataSourceImpl` → `XxxRepositoryImpl`
  ล็อกอินผ่าน `AuthRepositoryImpl` ตัวจริง token ถูกเก็บลง `StorageService.memory()` แล้ว `ApiClient`
  หยิบไปแนบเอง ไม่มีการยัด header ในเทสต์ แต่ละบทบาทถือ "เครื่อง" ของตัวเอง (session แยกจริง)
- **ทดสอบที่ชั้น data/domain ไม่ใช่ขับหน้าจอ** — widget test ใช้ fake async และ mock `HttpClient`
  ทำให้ I/O จริงเปราะมาก ชั้นที่มีความเสี่ยงสูงสุดตอนต่อ backend จริงคือการแปลง JSON และกฎธุรกิจข้าม
  module ซึ่งชั้นนี้ครอบคลุมได้เต็ม (หน้าจอมีเทสต์ของตัวเองบนโหมดสาธิตอยู่แล้ว)
- **เขียนตามสิ่งที่ระบบสัญญาไว้ ไม่ใช่ตามที่โค้ดทำอยู่** — เช่นเช็คว่ายอดที่แอปโชว์ในตะกร้า
  (`BillCalculator`) เท่ากับที่ backend เรียกเก็บทุกสตางค์, QR พร้อมเพย์จาก backend ตรงกับอัลกอริทึม
  Dart ทุกตัวอักษรรวม CRC, ปิดกะแล้วเงินในลิ้นชักต้องตรงกับที่นับได้จริง — ถ้าไม่ตรงคือบั๊กที่ต้องแก้
  ไม่ใช่เทสต์ที่ต้องปรับให้ผ่าน เจอบั๊กจริง 5 ตัว (#43–#46) จากสองไฟล์นี้ และปรับเทสต์เองเฉพาะจุดที่
  ข้อสันนิษฐานของเทสต์ผิด (เช่น pendingToken ถูกปฏิเสธด้วย 403 ไม่ใช่ 401 ซึ่งถูกต้องอยู่แล้ว)
- **`Scenario` ร้อยขั้นตอนเป็นเรื่องเดียว** — ขั้นหลังพึ่งผลขั้นก่อน (ออเดอร์/กะที่เปิดไว้) ถ้าขั้นไหนล้ม
  ขั้นถัดไปจะรายงานว่า "ไม่ได้รันเพราะขั้น X ล้ม" แทนที่จะพ่น `LateInitializationError`

**ข้อเสียที่ยอมรับ**

- ไม่ได้ขับ UI จริงบนเบราว์เซอร์ (เช่น Playwright บน Flutter web) — Flutter web วาดลง canvas ต้องพึ่ง
  semantics tree ในการหา element ซึ่งเปราะและช้า ความเสี่ยงหลักของการต่อ backend จริงอยู่ที่ชั้นที่ชุดนี้
  ครอบคลุมแล้ว
- ขั้นตอนอิงข้อมูล seed (ชื่อพนักงาน, สาขาทองหล่อมีเมนูของตัวเอง, กะที่ seed เปิดค้างไว้) — ถ้าแก้ seed
  ต้องแก้ชุดนี้ตาม ถือเป็น fixture ตั้งใจ

## 48. ขายตามน้ำหนัก: เก็บ "กรัม" เป็นจำนวนเต็ม และคิดราคาเป็นสตางค์ก่อนปัด (2026-09-23)

**ปัญหา** — จำนวนในออเดอร์เป็นจำนวนเต็มต่อจาน เคาน์เตอร์เนื้อสดคิด "กิโลละ 1,200 ชั่งได้ 0.485 กก."
ไม่ได้ (ผู้สนใจใช้จริงรายแรก MEET365 เป็นผู้กระจายเนื้อที่มีหน้าร้านด้วย) ดู `docs/tickets/18-sell-by-weight.md`

**ที่เลือก**

- **`order_items.weight_grams` เป็น integer กรัม ไม่ใช่ quantity ทศนิยม** — หลักเดียวกับเงินที่เก็บเป็น
  สตางค์ (#2) ทศนิยมใน SQLite/JavaScript/Dart ปัดไม่ตรงกันได้ ส่วน quantity ของบรรทัดชั่งน้ำหนักคงเป็น 1
  เสมอ (หนึ่งบรรทัด = หนึ่งถุง/หนึ่งฉลาก) โค้ดเดิมที่อ่าน quantity เป็นจำนวนจาน (แยกบิล, คิวครัว, เมนูขายดี)
  จึงไม่ต้องรู้จักทศนิยมเลย
- **สูตรเดียวทุกที่: `round((ราคาต่อกก. + ตัวเลือกต่อกก.) เป็นสตางค์ × กรัม / 1000)`** —
  `order.calculator.js#lineTotalFor`, `CartLine.lineTotal`, `DemoStore._weighedLineTotal` คูณ 123.45 ×
  0.333 ตรง ๆ เป็นบาทจะได้ 41.10885 แต่ 123.45 × 100 ในเลขทศนิยมของเครื่องคือ 12344.999… ปัดผิดทาง
  ได้ จึงแปลงราคาเป็นสตางค์เต็มก่อนคูณ — ชุด E2E เช็คว่าตะกร้าในแอปกับ backend ตรงกันทุกสตางค์
- **ตัวเลือกเสริมของเมนูชั่งน้ำหนักคิดต่อกิโลกรัม** (หมักซอส +40/กก.) — คิดต่อถุงจะทำให้ถุง 100 กรัม
  กับ 2 กิโลจ่ายค่าหมักเท่ากัน
- **แก้จำนวนบรรทัดชั่งน้ำหนักไม่ได้ ต้องลบแล้วชั่งใหม่** — "2 ถุง" ของเนื้อไม่มีความหมาย แต่ละถุงหนักไม่เท่า
  กัน ตะกร้าจึงไม่รวมบรรทัดชั่งน้ำหนักแม้หนักเท่ากัน (signature ผูกกับตัวบรรทัด)
- **สต๊อกตัดเป็นกิโลกรัม** (`effectiveQuantity` = กรัม/1000) และเมนูชั่งน้ำหนักยังขายได้ตราบที่สต๊อก > 0
  (จานต้องพอ 1 ที่ แต่เนื้อ 0.3 กก. ที่เหลือยังขายได้ 0.3 กก.)
- **QR สั่งเองซ่อนเมนูชั่งน้ำหนัก** — ลูกค้าชั่งเองไม่ได้ ถ้าสั่งได้จะได้บรรทัดที่ไม่มีน้ำหนัก

**ข้อเสียที่ยอมรับ**

- ไม่อ่านน้ำหนักสดจากตาชั่งผ่านสาย — ใช้ฉลากตาชั่ง (#49) หรือพิมพ์เอง
- ใบเสร็จ/ตั๋วครัวของบิลเก่าที่ไม่มี weight_grams แสดงเหมือนเดิม (คอลัมน์ใหม่เป็น NULL)

## 49. ฉลากตาชั่ง/บาร์โค้ด: อ่านรหัสในเครื่อง ไม่ถาม backend (2026-09-23)

**ปัญหา** — ร้านเนื้อใช้ตาชั่งพิมพ์ฉลาก EAN-13 ที่ฝังน้ำหนักไว้ในรหัส ถ้าต้องพิมพ์น้ำหนักซ้ำจากฉลากจะช้าและ
พิมพ์ผิดได้ ดู `docs/tickets/19-barcode-scale.md`

**ที่เลือก**

- **`BarcodeResolver` เป็นฟังก์ชันบริสุทธิ์ฝั่ง Dart** จับคู่กับเมนูที่โหลดไว้ในเครื่องแล้ว — เครื่องสแกน
  USB/บลูทูธพิมพ์ 13 ตัว + Enter ในไม่กี่ร้อยมิลลิวินาที หน้าร้านต้องสแกนต่อได้แม้เน็ตสะดุด (#13) และ
  ไม่ต้องมี endpoint ใหม่ให้ดูแล backend มีหน้าที่แค่เก็บรหัสและกันรหัสซ้ำ
- **รูปแบบฉลากตั้งได้ที่หน้าตั้งค่า** (prefix ขึ้นต้นด้วย 2 ยาว 1–3 หลัก + PLU 4–6 หลัก ต้องเหลือหลัก
  น้ำหนัก 4–6 หลัก) ค่าเริ่มต้น `20` + 5 + 5 ตามที่ตาชั่งในไทยส่วนใหญ่ตั้งมา ตรวจทั้งฝั่งแอปและ backend
  (backend ตรวจเทียบกับค่าที่บันทึกไว้ด้วย ไม่ใช่เฉพาะตอนส่งมาคู่กัน)
- **check digit ผิด = ไม่เดา** — ฉลากขาด/เปื้อนแล้วอ่านผิดหนึ่งหลัก น้ำหนักอาจกลายเป็น 4,850 กรัม แทนที่
  จะเป็น 485 จึงแจ้งให้สแกนใหม่แทน
- **ฉลากเก็บน้ำหนัก ไม่เก็บราคา** — ราคาคิดจากราคาปัจจุบันในระบบเสมอ ตาชั่งที่ตั้งราคาเก่าไว้จึงไม่ทำให้
  คิดเงินผิด
- **บาร์โค้ดที่ลงทะเบียนไว้ตรงตัวชนะการอ่านเป็นฉลาก** — สินค้าบางตัวมีบาร์โค้ดขึ้นต้นด้วย 2 จริง ๆ
- **PLU เก็บแบบตัด 0 นำหน้า** ฉลาก `00101` กับที่ร้านพิมพ์ `101` ในฟอร์มจึงเป็นตัวเดียวกัน และห้ามซ้ำ
  ภายในสาขาเดียวกัน (สาขาอื่นใช้เลขเดียวกันได้ ตาชั่งอยู่คนละร้าน)

**ข้อเสียที่ยอมรับ**

- ไม่สแกนด้วยกล้องมือถือ (ต้องเพิ่ม dependency และสิทธิ์กล้องบนเว็บ) — เครื่องสแกนแบบคีย์บอร์ดราคา
  ไม่กี่ร้อยบาทใช้ได้ทันทีทุกแพลตฟอร์ม
- ถ้าเมนูในเครื่องเก่ากว่า backend (เพิ่งเพิ่มบาร์โค้ดจากอีกเครื่อง) ต้องรีเฟรชเมนูก่อนจึงสแกนเจอ

## 50. ขายเชื่อ/ลูกหนี้: "ใบแจ้งหนี้" คือการชำระวิธี credit และยอดค้างคำนวณสดเสมอ (2026-09-23)

**ปัญหา** — ทุกบิลต้องจ่ายครบก่อนปิด ผู้กระจายสินค้าขายลูกค้าประจำแบบเครดิต 30 วัน วางบิลรวบแล้วค่อย
รับเงิน ไม่มีที่บันทึกหนี้/ใบวางบิล/ใบเสร็จรับชำระ ดู `docs/tickets/20-b2b-credit.md`

**ที่เลือก**

- **ขายเชื่อ = แถว `payments` ที่ `method = 'credit'` + `due_date`** ไม่สร้างตาราง invoice แยก —
  ออเดอร์ปิดบิล (`paid`) ตามปกติ รายงานยอดขาย/VAT/Z-report/ใบกำกับภาษีทำงานเหมือนเดิมทุกตัว เพราะ
  ในทางบัญชียอดขายเกิดตอนส่งของ ไม่ใช่ตอนได้เงิน เงินที่ยังไม่ได้รับเป็นเรื่องของลูกหนี้ ไม่ใช่ของบิล
- **ไม่เก็บคอลัมน์ "ยอดค้าง"** — ยอดค้าง = ยอดบิล − คืนเงิน − ยอดที่ตัดด้วยใบเสร็จที่ยังไม่ยกเลิก
  คำนวณสดทุกครั้ง (`INVOICE_SELECT`) คืนเงิน/ยกเลิกใบเสร็จแล้วตัวเลขตามเองโดยไม่มีจุดให้ลืมอัปเดต
- **ใบเสร็จรับชำระตัดบิลเก่าสุดก่อน** (ครบกำหนดก่อน → ขายก่อน → id) หรือจำกัดเฉพาะบิลในใบวางบิลที่
  ลูกค้าถือมา — ตรงกับที่ฝ่ายบัญชีของลูกค้าจ่ายตามใบวางบิล ตาราง `ar_allocations` เก็บว่าใบเสร็จไหน
  ตัดบิลไหนเท่าไร จึงพิมพ์ใบเสร็จซ้ำได้รายการเดิม
- **ใบวางบิลเก็บยอด ณ วันออก** (`billing_note_items.amount`) แต่สถานะ/ยอด "ยังต้องเก็บ" คำนวณสด —
  เอกสารที่ส่งลูกค้าไปแล้วพิมพ์ซ้ำต้องได้ตัวเลขเดิม บิลหนึ่งอยู่ในใบวางบิลที่ยังไม่ยกเลิกได้ใบเดียว
- **เงินสดที่รับชำระหนี้เข้าลิ้นชักของกะที่เปิดอยู่** นับรวมในยอดที่ควรมีตอนปิดกะ (ต่อยอด #44) และ
  Z-report แยกหัวข้อ "รับชำระหนี้" ไม่ปนกับยอดขายของกะ — ถ้าไม่นับ แคชเชียร์ที่นับเงินถูกจะถูกบันทึก
  ว่าเงินเกินทุกครั้งที่ลูกค้าเอาเงินมาจ่ายหนี้ ใบเสร็จเงินสดยกเลิกได้เฉพาะตอนกะนั้นยังเปิดอยู่
- **สิทธิ์**: ขายเชื่อ/รับชำระ/ออกใบวางบิล = admin, manager, cashier (พนักงานเสิร์ฟรับเงินสดได้ปกติ แต่
  การให้ลูกค้าติดเงินร้านเป็นอีกระดับการตัดสินใจ) ตั้งวงเงิน/ยกเลิกเอกสาร = ผู้จัดการขึ้นไป ใช้แต้มสะสม
  ร่วมกับขายเชื่อไม่ได้ (หนี้ต้องเท่ากับยอดในบิลพอดี)
- **Migration สร้างตาราง `payments` ใหม่** เพราะ SQLite แก้ CHECK ของตารางเดิมไม่ได้ — ปิด
  foreign_keys ชั่วคราว คัดลอก สลับชื่อ สร้าง index คืน ตรวจว่าย้ายไปแล้วหรือยังจาก CHECK จริงด้วย regex
  (รอบแรกค้นข้อความ `'credit'` ทั้งก้อน แล้วถูกคอมเมนต์ใน schema หลอกว่าย้ายแล้ว — `migrate-credit.test.js`
  สร้างฐานข้อมูลรุ่นก่อนจริงแล้วพิสูจน์ว่าข้อมูลเงิน/refund ที่อ้างถึงไม่หายสักแถว)
- **เลขเอกสาร** `BN69-000001` / `RC69-000001` (ปี พ.ศ. 2 หลัก + เลขรันต่อปี) รูปแบบเดียวกับใบกำกับภาษี

**ข้อเสียที่ยอมรับ**

- ไม่มีดอกเบี้ย/ค่าปรับ/ใบลดหนี้แยกเอกสาร — ลดหนี้ด้วยการคืนเงินบิลขายเชื่อ (ไม่เกินยอดที่ยังค้าง)
- วงเงินเป็นระดับเชน (ลูกค้าเป็นระดับเชนอยู่แล้วตาม #36) ไม่แยกวงเงินต่อสาขา
- ใบวางบิล/ใบเสร็จดูและพิมพ์จากจอ ยังไม่มีไฟล์ PDF ส่งอีเมล

## 51. ตัดสต๊อกตอนชำระครบ สำหรับรายการที่ไม่เคยส่งครัว (2026-09-23)

**ปัญหา** — เดิมตัดสต๊อกตอน "ส่งเข้าครัว" เท่านั้น (#15) แต่เคาน์เตอร์ขายเนื้อ/ของกลับบ้านมักเปิดบิล
แล้วเก็บเงินเลยโดยไม่มีอะไรต้องให้ครัวทำ ของที่ขายออกไปจริงจึงไม่ออกจากสต๊อกเลย

**ที่เลือก** — ตอนออเดอร์ชำระครบ ตัดสต๊อกของทุกรายการที่ยังไม่ถูกยกเลิกและยังไม่เคยตัด
(`stock_deducted`) ทั้ง backend (`payment.service.js#pay`) และ Demo Mode — รายการที่ส่งครัวไปแล้วถูกตัด
ตอนส่งครัวอยู่แล้วจึงไม่ถูกตัดซ้ำ

**ข้อเสียที่ยอมรับ** — ของที่เตรียมไว้ในตะกร้าแล้วยังไม่จ่าย ไม่ถูกกันสต๊อกไว้ก่อน (เหมือนเดิม)
